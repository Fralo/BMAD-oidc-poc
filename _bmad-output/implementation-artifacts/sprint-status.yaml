# generated: 2026-05-14
# last_updated: 2026-05-18 (story 5.4 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on the `epic-5..E5S4` diff (740 lines / 5 files). Acceptance Auditor: PASS on AC1/AC2/AC3/AC5, PARTIAL on AC4 (Story 5.3's parallel landing pre-empted the +1-line shape; intent met), 16/16 failure-prevention items PASS, DoD #6 PARTIAL pre-patch (Mode-B checkboxes 1-5 left `[ ]` — fixed by P2). 8 patches applied: P1 duplicate `### Operator follow-up checklist` H3 removed; P2 Mode-B checkboxes 1-5 flipped to `[x]` with per-step annotation; P3 story frontmatter status: ready-for-dev → review → done; P4 stale "5.3 backlog" references reconciled to "5.3 done" post-merge; P5 smoke-doc Build-form header corrected from bare `docker compose up --build` to `docker compose --profile default up --build`; P6 pending-steps framing unified on "steps 6-13"; P7 D140 evidence rewritten to lead with the empirical `no service selected` observation rather than the post-merge README line-162 quote that didn't exist at the smoke baseline `fb751ec`; P8 D143 real-fix path corrected from non-existent `bff/core/error_handlers.py` to actual `services/bff/src/bff/core/errors.py:24`. 5 defers logged D144-D148 (healthcheck timing framing inconsistency 32-42s vs 90-120s, "four anomalies" prose vs 5-item parenthetical, git status entry-count vs diff --stat framing, numbering-note over-allocation in D140-D143 skip, baseline_commit frontmatter vs Run Record SHA divergence — all doc-consistency nits below patch cut-line). 8 dismissed as noise: F4 already-fixed-in-merge, F9/F10/E1 Docker version strings are real host outputs not fabricated (project narrative 2026-future-dated), F12 compose env_file line numbers no actual conflict, F13 probe-2 status hedge acceptable (got 302), E6 probe 1 `<app-root>` verified, E7 probe 2 transcript `<random>`/`<S256>` redaction intentional, A3 AC4 modified-in-place pre-empted by 5.3, A5 D142 sed workaround documented honestly. Net code-review-pass diff: doc-only across this story file + `docs/smoke-run.md` + `deferred-work.md` + this sprint-status.yaml. ZERO source/test/compose/Dockerfile/Justfile/keycloak/planning-artifact changes. Story 5.4 closes — Mode-B partial-smoke evidence remains the close-gate for the agent-completable surface; the 8 operator-pending browser steps (6-13) remain pending with the explicit Operator follow-up checklist explaining how to complete them. Epic 5 progress: 4/4 stories done (5.1 5.2 5.3 5.4); only the optional Epic 5 retrospective remains. 5-4 status: review → done. Earlier on 2026-05-18: story 5.4 dev complete → review — Mode B partial-smoke close against baseline `fb751ec` (HEAD past Story 5.2 merge; story file's `baseline_commit` was `3e3612a` at create-story time, the actual smoke ran on `fb751ec` because main has progressed; Run Record records the actual `git rev-parse HEAD`). ZERO source / test / compose / Dockerfile / Justfile / keycloak / planning-artifact changes — `git status --short` is exactly 5 doc-only entries: ` M README.md` (+2 lines: one blank + one ref line directly after Story 5.2's `docs/security-review.md` line, taking README to 34 lines), ` M _bmad-output/implementation-artifacts/deferred-work.md` (+13 lines: D140-D143), ` M sprint-status.yaml` (status flip + last_updated chain), `?? docs/smoke-run.md` (new — Checklist + Run Record + Mode-B HTTP-probe transcript + Operator follow-up checklist + Known-setup-workarounds callout), `?? 5-4-final-docker-compose-up-smoke-default-profile.md` (this story file). Mode chosen: **Mode B** (programmatic agent; dev agent has no desktop-browser capability for OAuth click-through). Bring-up: `docker compose down -v` (idempotent — no prior project state) → `docker compose --profile default up --build -d` → poll until all `(healthy)`: keycloak 42s / bff 32s / resource-server 32s post-start. Three HTTP probes all GREEN: Probe 1 `GET /` HTTP 200 + body contains `<app-root>` (1 occurrence); Probe 2 `GET /auth/login` HTTP 302 → `http://localhost:8080/realms/bmad-books/protocol/openid-connect/auth?client_id=bmad-books-bff&response_type=code&scope=openid+reading-speed:read+reading-speed:write&redirect_uri=http://localhost:8000/auth/callback&state=<random>&nonce=<random>&code_challenge=<S256>&code_challenge_method=S256` (full PKCE Authorization Code flow params surfaced); Probe 3 `GET /api/me` unauthenticated HTTP 401 + `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}`. J6 surrogate GREEN: `docker compose stop resource-server` → Exited (0); `docker compose start resource-server` → healthy in ~6s. Final `docker compose down` (no `-v` — volumes preserved for operator's Mode-A follow-up; they can re-`down -v` for fresh-state walk-through). **Four new defers logged (D140-D143)**: D140 bare `docker compose up` doesn't start the default profile because all services declare `profiles: [default, dev, e2e]` and Compose v2/v5 with `include:` excludes profiled services unless `--profile <name>` is set; canonical form is `docker compose --profile default up --build` — three places carry the stale bare-form (README line 4 + line 30, architecture line 1294, epic AC step 4); medium severity; belongs to Story 5.3 README rewrite. D141 per-service `.env` files (`services/bff/.env`, `services/resource-server/.env`) are required in addition to repo-root `.env` because `compose/app.yml` declares `env_file: ../services/<svc>/.env` on BFF + RS; first-time operator copying only the repo-root template per README errors with `env file ../services/bff/.env not found`; medium severity; three-copy setup OR consolidate to single root .env; belongs to Story 5.3 README rewrite or fast-follow setup-hygiene story. D142 default profile leaves `AUTH_TYPE=none` on the RS (archetype synthetic-admin — JWT signature / scope / iss / aud NOT validated) because `services/resource-server/.env.example:57` has `#AUTH_TYPE=oidc_bearer` commented out and only `compose/app.e2e.yml:53` activates the override for the e2e profile; the canonical prod-shaped bring-up is auth-degraded vs. e2e — J3/J4/J6 journeys would functionally pass but the scope-enforcement surface PRD §8 + Story 5.2 §5 attest to is NOT exercised; medium-to-high severity (security degradation in the documented prod-shaped path); fix: add a `compose/app.default.yml` overlay mirroring the e2e overlay's AUTH_TYPE override OR uncomment in `.env.example`; belongs to a fast-follow security-correctness story. D143 `/api/me` 401 envelope `message` wording `"Session expired or not present"` (smoke doc first-draft placeholder used different wording — `"Your session has expired or you are not signed in."`); not a defect, doc-consistency nit only; nit severity; belongs to documentation-consistency cleanup. **Smoke doc structure**: Prerequisites block + "Known setup workarounds" callout (D140/D141/D142 mitigation surfaced ABOVE the checklist so an operator hits the workarounds before they type the steps) + 13-step ordered checklist (each step's copy / credentials / math byte-for-byte from epic AC — testuser/testpassword + freshuser/freshpassword, `Dune` 688p + `Dune Messiah` rename + status to-read→reading→delete, /settings speed=30 → `Saved` pulse → reload persists, 600-page book → Estimate `≈ 20 h` → switch to speed=60 → Re-estimate `≈ 10 h`, freshuser precondition `Set your reading speed in Settings to enable estimates` with clickable Settings link, J5 `/login?return_to=%2Fbooks`, J6 `Service unavailable — try again shortly` em-dash U+2014 + stop/start resource-server + healthcheck recovery) + Run Record (date / SHA / operator / host env / Mode chosen + reason / Anomalies named + verdict / Optional screenshots field) + Mode-B HTTP-probe transcript section + Operator follow-up checklist explaining how to flip `[ ]` → `[x]` after the browser walk-through. Eight checklist steps (6, 7-J1, 8-J2, 9-J4, 10-J3-happy, 11-J3-precondition, 12-J5, 13-J6) remain `[ ]` PENDING-operator with explicit Anomalies callout naming each pending step. **Verdict: PASS WITH ANOMALIES** — Mode-B partial-smoke close; steps 1-5 + 3 HTTP probes + RS killswitch surrogate all green; steps 6-13 require operator browser follow-up before story moves to `done`; user accepts the partial-smoke trade-off at code-review time. **AC5 (no production diff) honored**: zero source / test / compose / Dockerfile / Justfile / keycloak / .env / planning-artifact entries in `git status`. Failure-prevention checklist items #1 (no fix-on-the-way — four findings logged as defers not patched), #3 (copy strings byte-for-byte verified pre-smoke via T1.5), #5 (no e2e profile used — default profile only), #6 (no `--abort-on-container-exit` — long-lived services), #7 (`--build` flag set on `up`), #8 (`down -v` before `up`), #9 (no `[x]` if `[ ]` — eight pending steps left `[ ]` honestly), #10 (no README rewrite — single line append), #11 (Stories 5.1 + 5.2 README lines preserved verbatim), #13 (no `--abort-on-container-exit` — confirmed), #14 (`[ ]` / `[x]` checkboxes preserved — GitHub-renderable), #15 (e2e suite NOT re-run — Story 5.1's attestation stands), #16 (`git rev-parse HEAD` recorded at smoke time — `fb751ec`, not copy-pasted from prior story) all observed. 5-4 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.4 contexted → ready-for-dev — file `5-4-final-docker-compose-up-smoke-default-profile.md` written from Epic 5 AC source [epics.md L1892-1933] + Story 5.1 + 5.2 doc-only precedent + deferred-work.md D46 §Resolution naming Story 5.4 as the canonical operator-smoke home. 5 ACs + 6 tasks. Pure-doc deliverable: ONE new file `docs/smoke-run.md` (13-step ordered checklist matching epic AC verbatim — fresh clone, .env setup, `docker compose down -v`, `docker compose up --build` default profile, healthcheck wait, browser open at http://localhost:8000, J1 testuser login, J2 add `Dune` 688p to-read → reading → edit `Dune Messiah` → delete via native confirm, J4 /settings speed=30 → Save "Saved" pulse → reload persists, J3 happy 600-page book → "Estimate" → `≈ 20 h` → switch speed to 60 → "Re-estimate" → `≈ 10 h`, J3 precondition log out + log in as `freshuser` → "Estimate" → `Set your reading speed in Settings to enable estimates` with clickable Settings link, J5 logout + URL ends with `/login` + manual `/books` → bounces to `/login?return_to=%2Fbooks`, J6 `docker compose stop resource-server` + Estimate → `Service unavailable — try again shortly` in error color → `docker compose start resource-server` + healthcheck recovery → Estimate → real duration) + ONE README reference line directly after Story 5.2's line 28 (3rd doc reference under "Architecture overview"; mirrors 5.1 + 5.2 placement convention exactly). ZERO production code / test / compose / Dockerfile / Justfile / keycloak / .env changes — failure-prevention checklist #1, #5, #6, #7, #8, #13, #15. Two execution modes documented: Mode A (operator with desktop browser, canonical) flips all 13 checkboxes to `[x]` and records verdict=PASS in Run Record; Mode B (programmatic agent, fallback for headless hosts) completes steps 1-5 bring-up + 3 curl-level HTTP probes (BFF serves `<app-root>` at GET /; /auth/login 3xx OIDC redirect; /api/me 401 unauthenticated) and leaves journey steps 7-13 as `[ ]` with explicit "PENDING — operator browser walk-through required" callout in Anomalies + verdict=PASS WITH ANOMALIES — user accepts the partial-smoke trade-off at code-review time before story moves to `done`. Math pinned verbatim: 600 pages × 60min/h ÷ 30pph = 1200min = 20h; ÷ 60pph = 600min = 10h (ceiling-div from `services/resource-server/src/resource_server/services/estimate_service.py:63` + U+2248 glyph from `services/resource-server/src/resource_server/services/duration.py` `_PREFIX`). Copy strings pinned byte-for-byte (failure-prevention #3): `Service unavailable — try again shortly` em-dash U+2014 (`estimate-cell.ts:27` `ESTIMATE_CELL_J6_COPY`), `Set your reading speed in Settings to enable estimates` with `Settings` as `routerLink="/settings"` (`estimate-cell.html:18-29` + `estimate-cell.ts:30` prefix export), `Saved` pulse (`settings-page.ts:50`), `Estimate` / `Re-estimate` / `Estimating…` button labels. Seeded users pinned (failure-prevention #2): `testuser` / `testpassword` (has reading-speed row after J4) + `freshuser` / `freshpassword` (no row — triggers J3 412 precondition path). Book status options `['to-read', 'reading', 'finished']` (`status-control.spec.ts:82`). Default-profile composition verified: `docker-compose.yml` includes `compose/infra.yml` (Keycloak + healthcheck on `:9000/health/ready`) + `compose/app.yml` (BFF + RS, both Python stdlib `:8000/health` probes); `services/bff/Dockerfile` `node:22-slim AS node-builder` → `/spa/dist/spa/browser` → `/app/static` (Story 1.14 closure of D46); test-reset endpoints inert under default profile (ENABLE_TEST_RESET=false in .env-default, no -f compose/app.e2e.yml overlay). D45 (BFF /health under KC_HOSTNAME=localhost) closed 2026-05-15 — `docker compose up --abort-on-container-exit` unblocked. 16-item failure-prevention checklist includes: no --abort-on-container-exit (#13 — would SIGTERM the stack the moment `docker compose stop resource-server` runs in J6 step 13), no e2e profile (#5 — D440 BFF_CLIENT_SECRET sharing inappropriate for prod-shaped smoke), `--build` flag required (#7 — SPA multi-stage rebuild on Dockerfile / spa/ changes), `docker compose down -v` before `up` (#8 — drops bff_data/rs_data named volumes for true fresh-state), no `[x]` if any step `[ ]` in Mode A (#9 — false attestation prevention), no rewrite of Stories 5.1 + 5.2 README lines (#11 — placement convention preservation). Load-bearing close gate: `git status --short` shows ONLY `?? docs/smoke-run.md` + ` M README.md` + ` M sprint-status.yaml` + ` M 5-4-final-docker-compose-up-smoke-default-profile.md` (AC5 anti-scope-creep). Story 5.3 runs in parallel — `5-4` does not depend on it; 5.3's full README rewrite later absorbs the 5.4 reference line in its dedicated "Prod-shaped workflow" section. 5-4 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.2 dev complete → review — pure documentation pass authored entirely in worktree `E5S2` (baseline `3e3612a`, synced from parent at dev-start with 5.1's outputs + sprint-status.yaml + deferred-work.md + README's line-26 anchor). ZERO source / test / config / production-deliverable changes — `git diff --stat` shows only `README.md` (+4) and `sprint-status.yaml` (+8/-4); `docs/security-review.md` (553 lines) is new in `?? docs/`. Document follows PRD §9 envelope: §1 token storage and transport (cites `auth/keycloak_cookie_session.py`, `models/entities/session.py`, `services/session_service.py`, `services/resource_server_client.py` + pin-tests `tests/auth/test_keycloak_cookie_session.py`, `tests/services/test_session_service.py`, `tests/services/test_resource_server_client.py`); §2 cookie attributes (HttpOnly + Secure=env + SameSite=Lax + Path=/ + 256-bit opaque value + name configurable; state-id cookie sidebar with 5-min `URLSafeTimedSerializer` ref; CSRF cookie non-HttpOnly intentional + double-submit rationale; cites `api/auth.py:158–166, 285–305, 307–314`); §3 CSRF posture (three-layer: method-exempt + double-submit `hmac.compare_digest` constant-time + Origin/Referer with userinfo-strip rejection; cites `auth/csrf.py:32, 73–99, 101–135, 159–173` + SPA `shared/http/csrf-interceptor.ts`); §4 JWT validation (PyJWT step-by-step: `kid` lookup, `algorithms=["RS256"]` pinning, `audience`, `issuer`, `exp`, `require=["iss","aud","exp","sub"]`; JWKS cache + `kid`-miss re-fetch as TTL surrogate; BFF id-token parallel with D2/D8 browser-URL caveat + nonce binding; cites `oidc_bearer.py:52,76,96,118,130` + `keycloak_cookie_session.py:196,204`); §5 scope enforcement (RS-side per architecture line 82; `require_scope` factory module-load fail-fast; per-endpoint table — GET `/v1/reading-speed` + POST `/v1/estimate` need `:read`, PUT `/v1/reading-speed` needs `:write`; sub-from-JWT-only rule; D440 BFF_CLIENT_SECRET sharing follow-up referenced; cites `oidc_bearer.py:130`, `reading_speed.py:32,42`, `estimate.py:42`); §6 standard SPA concerns (CSP value BYTE-FOR-BYTE from `security_headers.py:17–21`, directive-by-directive table, attachment-rule note + `style-src 'unsafe-inline'` Tailwind v4 carve-out; no `bypassSecurityTrust*` / `innerHTML` / `eval` grep result; dep hygiene version table for Python 3.14 + FastAPI + PyJWT ≥2.10 + Authlib + httpx + Angular 21 + Tailwind 4 + Vitest 4 + Playwright 1.49 + Keycloak 26). Accepted-Risks subsection: AR1 plaintext token columns (architecture `:1362–1368` + KEK/dedicated-store/JWE-cookie alternatives), AR2 plaintext PKCE `code_verifier` (D42 envelope), AR3 no idle timeout beyond refresh chain (architecture `:1369–1373` + explicit `last_activity_at` alternative). Threat-Model: 7 prose threats T1 XSS through T7 confused-deputy; T4 (direct RS access) intentionally accepted as OAuth contract. Known-Gaps subsection: 19 items consolidating D371, D372, D43+D64, D63, D67, D80, D82+D83, D84, D73+D74+D85, D71, D75, D79, D81, D25, D440, D441, D442, D470, plus no-CI scope decision — one over AC10's minimum-18. README updated with single new reference line directly after coverage-report.md line, mirroring Story 5.1's placement convention (31 → 33 lines). Inline path correction applied: epic AC line 1812's `services/bff/src/bff/db/models/session.py` is stale — actual canonical path is `services/bff/src/bff/models/entities/session.py`. Markdown lint sanity: 28 code fences = 14 balanced blocks; 9 H2 sections matching AC1 structure; all 31 relative `../*` links resolve. Zero new defers (Task 8.1: one Python 3.14 language-version observation on `csrf.py:103` `except ValueError, TypeError:` — parses + runs correctly in 3.14, behavior is correct, not a security defect, not in scope). All 14 failure-prevention checklist items observed. 5-2 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.1 second-pass code review at user request → still done — same three layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) re-ran on the patched diff and surfaced 4 real follow-on findings, all doc-only, all applied: P6 reconciled `invalid_input` double-listing in error-service.ts bullet (the P3 first-pass rewrite had accidentally claimed it was both uncovered and covered — dropped from the covered list; SPA relies on three cases not four); P7 re-applied `5-1: review → done` to sprint-status.yaml structured field (first attempt was clobbered by a parallel turn touching the same file when 5.2 was contexted); P8 propagated DN1's "≥90%" softening uniformly across AC5 heading, AC6 (heading + Given + Then), AC2 template, Task 1.2 path-taken, DoD #4/#5, and Completion Notes — first pass had only patched AC5 body, leaving the rest of the story self-contradictory at ">90%"; P9 AC1 line 65 SPA HTML path `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular's `@angular/build:unit-test` wraps Vitest — same fix P1 made to the report, just propagated to the AC spec wording). ~7 second-pass findings dismissed as noise. ZERO behavior change across BOTH passes — every patch on both passes landed in `docs/coverage-report.md` or this story file's AC / Task / DoD wording. All four NFR11 thresholds still substantially exceeded post-second-pass. 5-1 status: done (unchanged; the second pass is a doc-coherence cleanup, not a status transition). Earlier on 2026-05-18: story 5.1 code review pass → done — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel; Acceptance Auditor returned PASS on all 9 ACs + 12 failure-prevention items + 14 DoD items pre-patch. 6 doc-accuracy patches applied (1 was DN1 reclassified after user input — "soften the docs" rather than tighten the gate): DN1 softened `fail_under = 90` framing — updated AC5 to "≥90% with `precision = 0`" + Targets-table cites "effective floor ~89.5% rounded"; P1 SPA HTML path corrected `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular builder nests one directory deeper than raw Vitest); P2 estimate-cell.ts missing-line list reconciled (line 129 was dropped from the Notes section between two enumerations); P3 error-service.ts count-vs-range conflict resolved by listing uncovered `case` branches explicitly; P4 inlined E2E reproduce recipe rewritten as `bash -c 'set -e; trap "..." EXIT; ...'` so Ctrl-C / failure paths still tear down (matches Justfile semantics, prevents `container_name: playwright` collision on next invocation); P5 DAR typo `spec-app-app.js` → `app.spec.ts` (verified `spa/src/app/app.spec.ts` exists). ZERO source / test / config / production-deliverable changes — every patch landed in `docs/coverage-report.md` (5) or this story file's AC5 wording + DAR (2). 8 findings dismissed as noise (four-vs-five gap count, cross-metric estimate-cell "regression", HTML-index DAR attestation, E2E exit-code quote, types.ts exclude framing, BFF format nit, README placement, pytest-cov exit-code quote). No defers logged — D135 remains the highest deferred id. All four NFR11 thresholds still substantially exceeded post-patch (no behavior changed, only documentation accuracy). Epic 5 progress: 1/4 stories done [5.1]; 5.2 already contexted → ready-for-dev in parallel; 5.3 / 5.4 still backlog. 5-1 status: review → done. Earlier on 2026-05-18: story 5.2 contexted → ready-for-dev — file `5-2-security-review-document.md` written in worktree `E5S2` from PRD §9 six-topic envelope [token storage/transport, cookie attrs, CSRF, JWT validation, scope enforcement, SPA concerns] + architecture A1–A8 [lines 343–354] + architecture §Operational Details "Token storage at rest" [1362–1368] + "Idle / absolute session timeout" [1369–1373] + Story 5.1 doc-only precedent. 11 ACs + 8 tasks. Pure-documentation deliverable: ONE new file `docs/security-review.md` + ONE README reference line mirroring 5.1's placement at line 26. ZERO production code / test / infra changes (failure-prevention checklist items #1–#3, #8, #14). The document cites implementing code paths verbatim (BFF `auth/keycloak_cookie_session.py`, `auth/csrf.py`, `middleware/security_headers.py:17–21` for byte-for-byte CSP; RS `auth/oidc_bearer.py` `_validate_access_token` + `require_scope` factory; SPA `shared/http/csrf-interceptor.ts`) and pin-test files (BFF `tests/auth/test_csrf.py`, `tests/middleware/test_security_headers.py`, `tests/services/test_resource_server_client.py`; RS `tests/auth/test_oidc_bearer.py`, `tests/api/test_reading_speed.py`, `tests/api/test_estimate.py`). Accepted-risks subsection: 3 named items (token plaintext SQLite columns, PKCE verifier plaintext, no idle-session timeout beyond refresh chain) per architecture's accepted-risk envelope. Threat-model: 7 threats prose-only (XSS / CSRF / token replay / direct RS access / scope escalation / replay-after-logout / confused-deputy). Known-gaps subsection consolidates 18+ existing defers (D25 health amplification, D42 PKCE verifier plaintext, D43 + D64 PyJWT leeway=0, D63 WWW-Authenticate header, D67 empty-sub→412, D71 BFF change-me reject parity, D73 Bearer case-sensitivity, D74/D85 multi-bearer concat, D75 router-via-__all__ gate bypass, D79 non-AppException envelope, D80 refresh-rotation race, D81 BFF VALIDATION_ERROR drift, D83 httpx follow_redirects, D84 log hygiene, D369 CSP attach-trigger, D371 CSP hardening, D372 extra security headers, D440 BFF_CLIENT_SECRET shared with Playwright, D441 log-injection on sub, D442 session_not_found envelope, D470 zero-width whitespace input, no GitHub Actions CI). Path correction noted in story: epic AC line 1812's `db/models/session.py` is stale — actual canonical archetype path is `models/entities/session.py`. Load-bearing close gate: `git status` shows ONLY `docs/security-review.md` (new) + `README.md` (+1 line) + this story file + sprint-status.yaml + optionally `deferred-work.md` (expected: no new defers). 5-2 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.1 dev complete → review — pure attestation pass, ZERO source / test / config changes. All four NFR11 thresholds substantially exceeded at baseline `3e3612a`: BFF 97.26% line / 543 tests (lowest per-file `core/database.py` 84% vs ≥70% floor); RS 98.33% line / 324 tests (lowest per-file `auth/factory.py` 77%); SPA 94.71% statements / 91.16% branches / 95.74% functions / 94.52% lines / 152 tests across 19 files (lowest per-file lines `error-service.ts` 78.94% vs ≥50% floor); E2E 6 specs / 26 tests covering J1–J6 with live `e2e-up` (inlined Justfile recipe because audit host lacks `just` binary, same as 4.4 dev) exits 0 in 53.0s with all 26 green (J1×3 J2×8 J3×5 J4×5 J5×2 J6×3). Created `docs/coverage-report.md` (full snapshot + Reproduce block + Targets table + per-surface sections + Method + "Gaps closed: None"); README gained one reference line under "Architecture overview" (Story 5.3 owns full rewrite). NO `omit` / `coverage.exclude` additions — RS's 7-entry Story-3.1 omit list preserved verbatim. Four per-file gaps documented as informational follow-ups (`core/database.py` lifecycle paths, `auth/factory.py` Azure-AD branch, `error-service.ts` defensive envelope shapes, `estimate-cell.ts` signal-invariant guards) — NOT deferred work; all above per-file floors. SPA build green (255.77 kB initial / 71.97 kB transfer). No new deferred items logged. 5-1 status: in-progress → review. Earlier on 2026-05-18: story 5.1 contexted — file `5-1-coverage-audit-gap-fill.md` written from Epic 5 AC source [epics.md L1748-1798] + prior-story coverage snapshots [BFF 97.26% at b905307, SPA estimate-cell 88.23%/78.57% at story 4.3, SPA suite 152/19 at story 4.4, RS aggregate never previously measured, E2E = 26 tests in 6 files at story 4.4]; 9 ACs + 8 tasks; creates `docs/coverage-report.md` + 1-line README reference; conditionally adds gap-fill tests in source-mirroring locations (BFF `tests/<mirror>/`, RS `tests/<mirror>/`, SPA `<name>.spec.ts` colocated) ONLY if thresholds not met; epic-5 status auto-transitioned `backlog` → `in-progress` (first story in Epic 5); baseline_commit=3e3612a; load-bearing close gate is each surface's coverage command exiting 0 with aggregate at/above target (BFF/RS >90%, SPA ≥70% on statements/branches/functions/lines, per-file floors of 70%/50%) + live `just e2e-up` 26/26 green. 5-1 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 4.4 contexted — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from epic AC source + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy + Story 4.2 BFF contract + Story 4.1 format_duration table. 13 ACs + 6 tasks. Two new specs (j3-estimate.spec.ts, j6-rs-unavailable.spec.ts) + e2e/README.md addendum. NO source/compose/helper changes — the harness is complete after Story 3.6. Load-bearing close gate: live `just e2e-up` exits 0 with all 26 tests green (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3 = NFR11's ≥5 J1–J6 satisfied). 4-4 status: backlog → ready-for-dev.
# Earlier on 2026-05-18 (sprint-status as carried by epic-5 immediately before this merge): story 5.2 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel against the 5.2 merge diff. Acceptance Auditor returned PASS on AC1–AC9 + AC11 + 14/14 failure-prevention items + 12/13 DoD items; AC10 / DoD #7 returned FAIL on the load-bearing "defer IDs resolve to deferred-work.md" invariant — 7 of 19 Known-Gaps citations cited D-IDs that did not exist (D369, D371, D372, D440, D441, D442, D470; max actual at review time was D135). User selected the "backfill deferred-work.md" resolution path; 11 patches applied total (1 from decision-needed, 10 from direct findings). D1 backfilled deferred-work.md with 7 new entries under a "Deferred from: code review of 5-2-..." section preserving the same D-IDs (intentional non-sequential gap from D135 → D369). P1 corrected 4 architecture line citations systematically off by ~40 lines (`:76 → :36` Token isolation, `:80 → :40` JWKS / no hardcoded keys, `:82 → :42` Scope-enforced computation, `:83 → :43` BFF does not bypass). P2 corrected resource_server_client.py:178–224 (public wrappers) → :230–279 (_call_with_refresh) + :359–410 (_refresh_access_token). P3 relabeled auth.py:119–127 from "logout cookie clear" to its actual purpose (state-cookie clear on auth_state_invalid error response) and added real logout-clear ranges (:347–355 session + :356–364 CSRF). P4 standardized session-cookie set range to :298–305. P5 retargeted AR3 cross-reference "§9 threat 6 below" → "T6 in Threat Model Summary below". P6 corrected §3 urlsplit claim — urlsplit preserves userinfo in netloc; only .hostname strips it; csrf.py:169–170 actually checks parts.username or parts.password. P7 softened §4 JWKS-TTL equivalence — kid-miss re-fetch satisfies key-rotation half of architecture line 415 but NOT the 24h time-based TTL half. P8 added presence-only note to §4 required-claims gate (PyJWT's require enforces presence, not non-empty values; cross-references Known Gap #5 / D67). P9 in-section reference to D369 re-routed to deferred-work.md directly (D369 now exists in the registry; Known Gaps list count stays at 19, exceeding AC10 floor of 18). P10 updated Known Gap #6 (D80 refresh-token rotation race) fix narrative — prefer DB-level SELECT … FOR UPDATE for multi-replica correctness; acknowledged asyncio.Lock is single-replica only. 2 defers logged in the story (W1 D63 duplicated in deferred-work.md + W2 D84 source-line citation drift) — both pre-existing in the registry, not introduced by 5.2; not appended to deferred-work.md as new entries because they ARE deferred-work.md issues. ~12 findings dismissed as noise. Net code-review-pass diff: docs/security-review.md (citation tightening across ~12 sites), _bmad-output/implementation-artifacts/deferred-work.md (+7 new D-entries under Story 5.2 review section), this story file (Review Findings + Status flip + Change Log v1.1 entry), sprint-status.yaml (5-2 review → done + this narrative). No production source / tests / infra touched. Doc structure unchanged (still 9 H2 sections matching AC1; all relative links resolve; CSP byte-for-byte still verbatim from security_headers.py:17–21). Epic 5 progress after 5.2 done: 2/4 stories done [5.1, 5.2]; 5.3 (README polish + AI integration log) and 5.4 (smoke run) still backlog. 5-2 status: review → done. Earlier on 2026-05-18: story 5.2 dev complete → review — pure documentation pass authored entirely in worktree `E5S2` (baseline `3e3612a`, synced from parent at dev-start with 5.1's outputs + sprint-status.yaml + deferred-work.md + README's line-26 anchor). ZERO source / test / config / production-deliverable changes — `git diff --stat` shows only `README.md` (+4) and `sprint-status.yaml` (+8/-4); `docs/security-review.md` (553 lines) is new in `?? docs/`. Document follows PRD §9 envelope: §1 token storage and transport (cites `auth/keycloak_cookie_session.py`, `models/entities/session.py`, `services/session_service.py`, `services/resource_server_client.py` + pin-tests `tests/auth/test_keycloak_cookie_session.py`, `tests/services/test_session_service.py`, `tests/services/test_resource_server_client.py`); §2 cookie attributes (HttpOnly + Secure=env + SameSite=Lax + Path=/ + 256-bit opaque value + name configurable; state-id cookie sidebar with 5-min `URLSafeTimedSerializer` ref; CSRF cookie non-HttpOnly intentional + double-submit rationale; cites `api/auth.py:158–166, 285–305, 307–314`); §3 CSRF posture (three-layer: method-exempt + double-submit `hmac.compare_digest` constant-time + Origin/Referer with userinfo-strip rejection; cites `auth/csrf.py:32, 73–99, 101–135, 159–173` + SPA `shared/http/csrf-interceptor.ts`); §4 JWT validation (PyJWT step-by-step: `kid` lookup, `algorithms=["RS256"]` pinning, `audience`, `issuer`, `exp`, `require=["iss","aud","exp","sub"]`; JWKS cache + `kid`-miss re-fetch as TTL surrogate; BFF id-token parallel with D2/D8 browser-URL caveat + nonce binding; cites `oidc_bearer.py:52,76,96,118,130` + `keycloak_cookie_session.py:196,204`); §5 scope enforcement (RS-side per architecture line 82; `require_scope` factory module-load fail-fast; per-endpoint table — GET `/v1/reading-speed` + POST `/v1/estimate` need `:read`, PUT `/v1/reading-speed` needs `:write`; sub-from-JWT-only rule; D440 BFF_CLIENT_SECRET sharing follow-up referenced; cites `oidc_bearer.py:130`, `reading_speed.py:32,42`, `estimate.py:42`); §6 standard SPA concerns (CSP value BYTE-FOR-BYTE from `security_headers.py:17–21`, directive-by-directive table, attachment-rule note + `style-src 'unsafe-inline'` Tailwind v4 carve-out; no `bypassSecurityTrust*` / `innerHTML` / `eval` grep result; dep hygiene version table for Python 3.14 + FastAPI + PyJWT ≥2.10 + Authlib + httpx + Angular 21 + Tailwind 4 + Vitest 4 + Playwright 1.49 + Keycloak 26). Accepted-Risks subsection: AR1 plaintext token columns (architecture `:1362–1368` + KEK/dedicated-store/JWE-cookie alternatives), AR2 plaintext PKCE `code_verifier` (D42 envelope), AR3 no idle timeout beyond refresh chain (architecture `:1369–1373` + explicit `last_activity_at` alternative). Threat-Model: 7 prose threats T1 XSS through T7 confused-deputy; T4 (direct RS access) intentionally accepted as OAuth contract. Known-Gaps subsection: 19 items consolidating D371, D372, D43+D64, D63, D67, D80, D82+D83, D84, D73+D74+D85, D71, D75, D79, D81, D25, D440, D441, D442, D470, plus no-CI scope decision — one over AC10's minimum-18. README updated with single new reference line directly after coverage-report.md line, mirroring Story 5.1's placement convention (31 → 33 lines). Inline path correction applied: epic AC line 1812's `services/bff/src/bff/db/models/session.py` is stale — actual canonical path is `services/bff/src/bff/models/entities/session.py`. Markdown lint sanity: 28 code fences = 14 balanced blocks; 9 H2 sections matching AC1 structure; all 31 relative `../*` links resolve. Zero new defers (Task 8.1: one Python 3.14 language-version observation on `csrf.py:103` `except ValueError, TypeError:` — parses + runs correctly in 3.14, behavior is correct, not a security defect, not in scope). All 14 failure-prevention checklist items observed. 5-2 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.1 second-pass code review at user request → still done — same three layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) re-ran on the patched diff and surfaced 4 real follow-on findings, all doc-only, all applied: P6 reconciled `invalid_input` double-listing in error-service.ts bullet (the P3 first-pass rewrite had accidentally claimed it was both uncovered and covered — dropped from the covered list; SPA relies on three cases not four); P7 re-applied `5-1: review → done` to sprint-status.yaml structured field (first attempt was clobbered by a parallel turn touching the same file when 5.2 was contexted); P8 propagated DN1's "≥90%" softening uniformly across AC5 heading, AC6 (heading + Given + Then), AC2 template, Task 1.2 path-taken, DoD #4/#5, and Completion Notes — first pass had only patched AC5 body, leaving the rest of the story self-contradictory at ">90%"; P9 AC1 line 65 SPA HTML path `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular's `@angular/build:unit-test` wraps Vitest — same fix P1 made to the report, just propagated to the AC spec wording). ~7 second-pass findings dismissed as noise. ZERO behavior change across BOTH passes — every patch on both passes landed in `docs/coverage-report.md` or this story file's AC / Task / DoD wording. All four NFR11 thresholds still substantially exceeded post-second-pass. 5-1 status: done (unchanged; the second pass is a doc-coherence cleanup, not a status transition). Earlier on 2026-05-18: story 5.1 code review pass → done — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel; Acceptance Auditor returned PASS on all 9 ACs + 12 failure-prevention items + 14 DoD items pre-patch. 6 doc-accuracy patches applied (1 was DN1 reclassified after user input — "soften the docs" rather than tighten the gate): DN1 softened `fail_under = 90` framing — updated AC5 to "≥90% with `precision = 0`" + Targets-table cites "effective floor ~89.5% rounded"; P1 SPA HTML path corrected `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular builder nests one directory deeper than raw Vitest); P2 estimate-cell.ts missing-line list reconciled (line 129 was dropped from the Notes section between two enumerations); P3 error-service.ts count-vs-range conflict resolved by listing uncovered `case` branches explicitly; P4 inlined E2E reproduce recipe rewritten as `bash -c 'set -e; trap "..." EXIT; ...'` so Ctrl-C / failure paths still tear down (matches Justfile semantics, prevents `container_name: playwright` collision on next invocation); P5 DAR typo `spec-app-app.js` → `app.spec.ts` (verified `spa/src/app/app.spec.ts` exists). ZERO source / test / config / production-deliverable changes — every patch landed in `docs/coverage-report.md` (5) or this story file's AC5 wording + DAR (2). 8 findings dismissed as noise (four-vs-five gap count, cross-metric estimate-cell "regression", HTML-index DAR attestation, E2E exit-code quote, types.ts exclude framing, BFF format nit, README placement, pytest-cov exit-code quote). No defers logged — D135 remains the highest deferred id. All four NFR11 thresholds still substantially exceeded post-patch (no behavior changed, only documentation accuracy). Epic 5 progress: 1/4 stories done [5.1]; 5.2 already contexted → ready-for-dev in parallel; 5.3 / 5.4 still backlog. 5-1 status: review → done. Earlier on 2026-05-18: story 5.2 contexted → ready-for-dev — file `5-2-security-review-document.md` written in worktree `E5S2` from PRD §9 six-topic envelope [token storage/transport, cookie attrs, CSRF, JWT validation, scope enforcement, SPA concerns] + architecture A1–A8 [lines 343–354] + architecture §Operational Details "Token storage at rest" [1362–1368] + "Idle / absolute session timeout" [1369–1373] + Story 5.1 doc-only precedent. 11 ACs + 8 tasks. Pure-documentation deliverable: ONE new file `docs/security-review.md` + ONE README reference line mirroring 5.1's placement at line 26. ZERO production code / test / infra changes (failure-prevention checklist items #1–#3, #8, #14). The document cites implementing code paths verbatim (BFF `auth/keycloak_cookie_session.py`, `auth/csrf.py`, `middleware/security_headers.py:17–21` for byte-for-byte CSP; RS `auth/oidc_bearer.py` `_validate_access_token` + `require_scope` factory; SPA `shared/http/csrf-interceptor.ts`) and pin-test files (BFF `tests/auth/test_csrf.py`, `tests/middleware/test_security_headers.py`, `tests/services/test_resource_server_client.py`; RS `tests/auth/test_oidc_bearer.py`, `tests/api/test_reading_speed.py`, `tests/api/test_estimate.py`). Accepted-risks subsection: 3 named items (token plaintext SQLite columns, PKCE verifier plaintext, no idle-session timeout beyond refresh chain) per architecture's accepted-risk envelope. Threat-model: 7 threats prose-only (XSS / CSRF / token replay / direct RS access / scope escalation / replay-after-logout / confused-deputy). Known-gaps subsection consolidates 18+ existing defers (D25 health amplification, D42 PKCE verifier plaintext, D43 + D64 PyJWT leeway=0, D63 WWW-Authenticate header, D67 empty-sub→412, D71 BFF change-me reject parity, D73 Bearer case-sensitivity, D74/D85 multi-bearer concat, D75 router-via-__all__ gate bypass, D79 non-AppException envelope, D80 refresh-rotation race, D81 BFF VALIDATION_ERROR drift, D83 httpx follow_redirects, D84 log hygiene, D369 CSP attach-trigger, D371 CSP hardening, D372 extra security headers, D440 BFF_CLIENT_SECRET shared with Playwright, D441 log-injection on sub, D442 session_not_found envelope, D470 zero-width whitespace input, no GitHub Actions CI). Path correction noted in story: epic AC line 1812's `db/models/session.py` is stale — actual canonical archetype path is `models/entities/session.py`. Load-bearing close gate: `git status` shows ONLY `docs/security-review.md` (new) + `README.md` (+1 line) + this story file + sprint-status.yaml + optionally `deferred-work.md` (expected: no new defers). 5-2 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.1 dev complete → review — pure attestation pass, ZERO source / test / config changes. All four NFR11 thresholds substantially exceeded at baseline `3e3612a`: BFF 97.26% line / 543 tests (lowest per-file `core/database.py` 84% vs ≥70% floor); RS 98.33% line / 324 tests (lowest per-file `auth/factory.py` 77%); SPA 94.71% statements / 91.16% branches / 95.74% functions / 94.52% lines / 152 tests across 19 files (lowest per-file lines `error-service.ts` 78.94% vs ≥50% floor); E2E 6 specs / 26 tests covering J1–J6 with live `e2e-up` (inlined Justfile recipe because audit host lacks `just` binary, same as 4.4 dev) exits 0 in 53.0s with all 26 green (J1×3 J2×8 J3×5 J4×5 J5×2 J6×3). Created `docs/coverage-report.md` (full snapshot + Reproduce block + Targets table + per-surface sections + Method + "Gaps closed: None"); README gained one reference line under "Architecture overview" (Story 5.3 owns full rewrite). NO `omit` / `coverage.exclude` additions — RS's 7-entry Story-3.1 omit list preserved verbatim. Four per-file gaps documented as informational follow-ups (`core/database.py` lifecycle paths, `auth/factory.py` Azure-AD branch, `error-service.ts` defensive envelope shapes, `estimate-cell.ts` signal-invariant guards) — NOT deferred work; all above per-file floors. SPA build green (255.77 kB initial / 71.97 kB transfer). No new deferred items logged. 5-1 status: in-progress → review. Earlier on 2026-05-18: story 5.1 contexted — file `5-1-coverage-audit-gap-fill.md` written from Epic 5 AC source [epics.md L1748-1798] + prior-story coverage snapshots [BFF 97.26% at b905307, SPA estimate-cell 88.23%/78.57% at story 4.3, SPA suite 152/19 at story 4.4, RS aggregate never previously measured, E2E = 26 tests in 6 files at story 4.4]; 9 ACs + 8 tasks; creates `docs/coverage-report.md` + 1-line README reference; conditionally adds gap-fill tests in source-mirroring locations (BFF `tests/<mirror>/`, RS `tests/<mirror>/`, SPA `<name>.spec.ts` colocated) ONLY if thresholds not met; epic-5 status auto-transitioned `backlog` → `in-progress` (first story in Epic 5); baseline_commit=3e3612a; load-bearing close gate is each surface's coverage command exiting 0 with aggregate at/above target (BFF/RS >90%, SPA ≥70% on statements/branches/functions/lines, per-file floors of 70%/50%) + live `just e2e-up` 26/26 green. 5-1 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 4.4 contexted — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from epic AC source + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy + Story 4.2 BFF contract + Story 4.1 format_duration table. 13 ACs + 6 tasks. Two new specs (j3-estimate.spec.ts, j6-rs-unavailable.spec.ts) + e2e/README.md addendum. NO source/compose/helper changes — the harness is complete after Story 3.6. Load-bearing close gate: live `just e2e-up` exits 0 with all 26 tests green (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3 = NFR11's ≥5 J1–J6 satisfied). 4-4 status: backlog → ready-for-dev.
# Earlier on 2026-05-18: story 4.3 merged into main via no-ff merge of branch worktree-agent-a32e6a27249f65923 — 4.3 = done; both Epic 4 backend [4.1, 4.2] and SPA [4.3] halves now landed. Only 4.4 [E2E specs J3 + J6] remains in Epic 4. See YAML last_updated below for the full per-day chain.)
# project: BMAD_books
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-14
last_updated: 2026-05-18 (story 5.3 code review pass → done — `bmad-code-review` ran Blind Hunter + Edge Case Hunter + Acceptance Auditor in parallel against the README.md diff (baseline `fb751ec` → working tree at +235/-15 = 235-net-add). 15 patches applied, 1 defer logged (D136 OIDC env var enumeration vs discovery), 11 findings dismissed as noise. Material defects caught: P1 `profiles: [default]` → `[default, dev, e2e]` (verified via `compose/app.yml:57,102` + `compose/infra.yml:57`); P2 `just e2e-up` expansion was the retired `--abort-on-container-exit` single-phase form — replaced with the actual two-phase `up -d --wait keycloak bff resource-server` + `run --rm --build playwright` recipe per `Justfile:56-61` and the Justfile preamble's explicit Story 3.6 retirement note (J4 `killRs()` would SIGTERM the runner); P3 per-surface test-commands table E2E row had the same retired command AND omitted `-f compose/app.e2e.yml` (the exact foot-gun the README itself warns about 30 lines earlier) — replaced with `just e2e-up`; P4 architectural-decision range counts wrong on three of four ranges (C1–C6 → C1–C8, F1–F5 → F1–F6, I1–I7 → I1–I8 per architecture.md heading walk); P5 single-test-focus example used non-existent `test_csrf_token_mismatch` — replaced with real `test_post_header_cookie_mismatch_returns_403` from `services/bff/tests/auth/test_csrf.py:136`; P6 Keycloak realm-import causal claim was wrong (Keycloak service in `compose/infra.yml` has NO `volumes:` block — realm imports on every container (re-)creation; `-v` drops only the BFF/RS `bff_data`/`rs_data` named volumes) — Setup step 7 + Troubleshooting bullet both corrected; P7 Setup step 7 truncate list "books, sessions, reading-speeds" omitted BFF `auth_states` and conflated RS `reading_speeds` into the BFF wipe — aligned with README line 141's correct enumeration; P8 AC8 violation: line 170 inlined `≥70%` and `>90%` threshold numbers despite AC8's explicit "MUST NOT duplicate the threshold numbers" — parenthetical removed, cross-reference to `docs/coverage-report.md` retained. Internal-consistency defects: P9 `:8001` listed as host-port conflict but RS has NO host port (compose/app.yml line 60-62 "internal-only … NO `ports:` block is published") — removed and qualified for the host-direct `uv run` case; P10 cross-origin cookie wording technically wrong (the proxy makes the SPA appear same-origin to the BFF — `SameSite=Lax` HttpOnly cookies don't actually "flow naturally across origins") — reworded; P11 bare `ng serve` silently required a global `@angular/cli` install — replaced with `cd spa && npm start` using the script in `spa/package.json` ("start": "ng serve"); P12 local-against-dev E2E conflated `TEST_RESET_TOKEN` (auth bearer) with `ENABLE_TEST_RESET=true` (route mount flag) — corrected to use the e2e overlay bring-up so both vars are set; P13 Vitest single-file path missing `cd spa &&` prefix — added; P14 sprint-change-proposal entry dated 2026-05-15 cited file dated 2026-05-14 — redated entry to match file (verified file exists as `sprint-change-proposal-2026-05-14.md`); P15 Opus 4.7 vs Opus 4.7 (1M context) inconsistent — added disambiguation note at top of AI integration log explaining the (1M context) suffix marks when the Co-Authored-By trailer convention started noting the larger context window, not a model change. Dismissed 11 findings: byte-for-byte boundary diagram preserved per FP#3 (no paraphrase allowed); intentional `docs/smoke-run.md` forward link per FP#11; AI log self-reference to 2026-05-18 acceptable; "≥5 specs" is NFR11 floor wording not a count claim; misc cosmetic (`.gitignored` style, compose `include:` wording, `worktree-agent-…` ellipsis, JSON snake_case label, "triplet" applied to docs-only stories, Playwright 1.49 pin verified accurate). Post-patch verification: `wc -l README.md` = 259 (DoD #11 range 250–400 ✓); `grep -cE "^## "` = 10 H2 (AC1's 11 sections including H1 ✓); `grep -c '^```'` = 12 code fence markers = 6 balanced blocks ✓; `grep -cE "coverage-report\.md|security-review\.md|architecture\.md"` = 15 (AC12 floor 3 ✓); `grep -nE "BFF_CLIENT_SECRET=|KEYCLOAK_ADMIN_PASSWORD=|TEST_RESET_TOKEN=|change-me"` = no matches (FP#2 ✓); the remaining `:8001` and `--abort-on-container-exit` mentions in the file are now in the patched-explanation context (educational/historical) rather than as a recommended command or live port. Zero production code touched (every patch in README.md). D136 logged in deferred-work.md under new "Deferred from: code review of 5-3-readme-polish-ai-integration-log (2026-05-18)" section. Epic 5 progress: 2/4 stories done [5.1, 5.3], 1 in review [5.2], 5.4 still backlog. 5-3 status: review → done. Earlier on 2026-05-18: story 5.3 dev complete → review — pure-documentation REWRITE pass authored entirely against worktree baseline `fb751ec` (`Merge story 5.2 — security review document`). ZERO source / test / config / production-deliverable changes — `git status --short` shows only ` M README.md`, ` M sprint-status.yaml`, `?? 5-3-readme-polish-ai-integration-log.md` (new story file, not yet `git add`-ed). README grown from 33-line stub (which had: H1 + summary + Setup numbered list + E2E profile sidebar + Architecture overview with three reference lines at lines 24/26/28 + AI integration log placeholder at line 30) to **252-line 11-section structure** per AC1: Title+summary, ## Prerequisites, ## Setup, ## Architecture overview, ## Dev workflow, ## E2E workflow, ## Prod-shaped workflow, ## Per-surface test commands, ## Project structure, ## AI integration log, ## References. All 13 ACs satisfied + all 14 failure-prevention items observed. AC4 boundary diagram inlined byte-for-byte from architecture.md:1096–1121 inside a ```text fence (Option A — self-containment); zero paraphrase. AC10 AI integration log has **12 chronological entries** (well above the AC10 floor of 5) — every BMAD skill from the AC list named at least once (`bmad-create-prd` / `bmad-validate-prd` / `bmad-create-ux-design` / `bmad-create-architecture` / `bmad-check-implementation-readiness` / `bmad-create-epics-and-stories` / `bmad-create-story` / `bmad-dev-story` / `bmad-code-review` / `bmad-retrospective` / `bmad-technical-research` / `bmad-correct-course`) + **SIX notable AI-assisted decisions** (well above AC's "≥1" floor): (1) Angular v21 + Tailwind v4 starter choice via technical research, (2) backend archetype lock-in (`fastapi-archetype` Python 3.14 + uv + OTEL), (3) parallel-epic execution via `git worktree`s starting Epic 2/3, (4) coverage threshold soften (Story 5.1's DN1 — `precision=0` rounding ≥89.5% rather than tightening `fail_under=90`), (5) security review structure mirrors PRD §9 verbatim (Story 5.2 — refused STRIDE / LINDDUN / ASVS cross-walk), (6) J6 honest-failure contract (Story 4.2 — BFF never fabricates an estimate when RS is unavailable). Claude model attributions cross-verified against actual `Co-Authored-By:` trailers: Opus 4.7 (2026-05-14 early), Opus 4.7 + **Sonnet 4.6** for the code-review pass (2026-05-15 onward — "different LLM for review" convention from Story 1.10), Opus 4.7 (1M context) (2026-05-16 → 2026-05-18). AC12 verification: `grep -cE "coverage-report\\.md|security-review\\.md|architecture\\.md" README.md` returns **15 matches** (floor: 3); all three preserved reference targets present in both Architecture overview AND References sections. AC11 References has 7 internal targets (PRD / architecture / ux / epics / security-review / coverage-report / smoke-run-forward-pointing) + 2 external (`fastapi-archetype` GitHub URL + `just` task runner). Markdown lint sanity: 12 code fence markers = 6 balanced blocks (1 boundary diagram + 1 project tree + 2 dev-workflow bash + 1 E2E `just e2e-up` + 1 local E2E bash); 19/19 internal `[label](path)` links resolve to existing repo files EXCEPT `docs/smoke-run.md` (intentionally forward-pointing per failure-prevention #11 — Story 5.4 owns the file). NO secret values printed (failure-prevention #2 — env-var NAMES only for `KEYCLOAK_ADMIN_PASSWORD` / `BFF_CLIENT_SECRET` / `TEST_RESET_TOKEN`; seeded users `testuser`/`testpassword` + `freshuser`/`freshpassword` printed because they live in version-controlled `keycloak/realm-bmad-books.json`). No `python3` invocations in shell commands (CLAUDE.md compliance — the only `python3` in the file is inside the descriptive CLAUDE.md project-tree annotation noting the convention itself). One failure-prevention-#7 violation caught and reverted mid-pass: added `## What's deliberately out of scope` 12th-H2 to push line count toward the 250 floor, immediately reverted, folded the same content into the AI integration log's "AI-assisted decisions left as accepted scope" closing bullet block — no section creep in final file. Line count path: initial 211 → 220 → 223 → 227 → 238 → 242 → 248 → 252 via substantive expansions (diagram callouts explaining the three edges, two more notable decisions on archetype + worktree-parallel, Setup steps 6/7 for "open the app" + clean reset, 4-trip troubleshooting, single-test-focus iteration commands, Epic 1 story breakdown into chronological sub-bullets). Path stayed inside failure-prevention #6 ("do NOT extend the README to a tutorial") and #7 ("no extra sections beyond the 11 AC1 specifies"). Regression spot-checks (Task 8.2/8.3 BFF + SPA test runs) marked N/A per Story 5.2 precedent — diff touches zero production code so regression vector is mathematically empty. Zero new defers logged (Task 9.1 expected count: zero — AC10's "accepted scope" block consolidates existing defers D371 CSP hardening + D372 extra headers + AR1 plaintext token columns + AR3 no idle timeout + no-CI scope decision; no new defer IDs introduced). Epic 5 progress: 1/4 stories done [5.1], 2 in review [5.2, 5.3], 5.4 still backlog. 5-3 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.3 contexted → ready-for-dev — file `5-3-readme-polish-ai-integration-log.md` written from Epic 5 AC source [epics.md L1851-1890] + Story 5.1/5.2 README single-line-reference precedent + current README stub baseline at HEAD `fb751ec` (33 lines: H1+summary, Setup numbered list, E2E profile sidebar, Architecture overview with three reference lines [architecture/coverage-report/security-review], AI integration log placeholder). 13 ACs + 9 tasks. Pure-documentation REWRITE deliverable: ONE modified file `README.md` — the 33-line stub grows ~10x into the 11-section structure AC1 specifies (Title+summary, Prerequisites, Setup, Architecture overview, Dev workflow, E2E workflow, Prod-shaped workflow, Per-surface test commands, Project structure, AI integration log, References). ZERO production code / test / compose / planning-artifact changes (failure-prevention #1 + DoD #9). AC12 explicit: every reference target from the current stub (architecture.md, docs/coverage-report.md, docs/security-review.md at README lines 24/26/28) MUST survive into the new file. AC10 minimums: ≥5 chronological entries, each with ISO-8601 date + one-line summary; BMAD skills enumerated (create-prd / create-ux-design / create-architecture / check-implementation-readiness / create-epics-and-stories / create-story / dev-story / code-review / retrospective); ≥1 notable AI-assisted decision (Angular v21 + Tailwind v4 candidate per architecture.md:156, OR coverage-threshold calibration per Story 5.1's DN1 outcome); Claude model attribution cross-checked against `Co-Authored-By` trailers (Opus 4.6 → early epics; Opus 4.7 1M context → Epic 4+5). AC4 boundary-diagram has two acceptable options: (A) inline byte-for-byte from architecture.md:1096–1121 inside ```text fence, or (B) one-line link to the architecture.md anchor — pick one, do not partially paraphrase. AC7 forward-references `docs/smoke-run.md` (owned by Story 5.4 / does not exist yet) — intentional dangling link mirroring how the current README forward-pointed to 5.3 from 1.1. AC2 prerequisites: Node ≥20 LTS, Python ≥3.14 (verified against both pyproject.toml `requires-python` pins), `uv`, Docker + Compose v2.20+, `npx`, `just` (required for E2E profile due to Story 1.12 P1 foot-gun), optional `ng` CLI. AC8 per-surface test commands cite the four base commands + four coverage variants Story 5.1 already established. AC9 project-structure tree is TOP-LEVEL only — ≤25 lines, no recursion into services/spa/src; defers to architecture.md:863+ deep tree via link. Failure-prevention checklist 14 items: NO secret values printed (env-var NAMES only — admin password / BFF_CLIENT_SECRET / TEST_RESET_TOKEN never; seeded users `testuser`/`testpassword` + `freshuser`/`freshpassword` OK because they live in the version-controlled realm-bmad-books.json), do NOT paraphrase the boundary diagram (byte-for-byte if inlining), do NOT fabricate AI-log entries (each entry substantiable from git log + retro files + planning artifacts), do NOT misstate Claude model usage (cross-check Co-Authored-By trailers), do NOT extend the README to a tutorial (architecture.md / security-review.md own the conceptual treatment), do NOT add sections beyond the 11 AC1 specifies, do NOT inline the deep project tree, do NOT promise CI (PRD §4 / architecture I7 out-of-scope), do NOT silently break docs/smoke-run.md forward link (one-line "populated by Story 5.4" note required), do NOT mix worktree paths into the README (canonical name `bmad-books`; `E5S3` belongs in commit metadata only). DoD load-bearing close gate: line count in 250–400 range (target ~300), AC10 ≥5 entries + ≥1 notable decision + model attribution, AC12 ≥3 grep matches on the preserved reference targets, markdown lint balanced fences + all `[label](path)` links resolve except docs/smoke-run.md. Earlier on 2026-05-18: story 5.2 dev complete → review — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel. Acceptance Auditor returned PASS on all 13 ACs + all 22 failure-prevention checklist items; the user-authorized `Justfile` + README "Compose-runner gotcha" scope expansion (Change Log v1.1) is the only AC-list deviation. 4 patches applied (1 was a decision-needed resolved by user before applying): DN1 strict apostrophe pin (replaced tolerant regex `/Couldn[’']t.../` with literal `Couldn’t...` U+2019 in AC6-J3 generic-failure test), P1 result-regex tightened to `/≈\s*[1-9]\d*\s*[hm]/` across both j3-estimate.spec.ts and j6-rs-unavailable.spec.ts (rejects fabricated `≈ 0 m`), P2 added positive `impliedMinutes(r1/r2) > 0` guards before the strict-inequality assertion in AC3 coupling test (surface parse failures explicitly), P3 added cell-stays-in-re-estimate assertion in AC5 after the second URL invariance check (catches a regression where Re-estimate silently regresses to idle Estimate state). 6 defers logged D130-D135 in deferred-work.md (Justfile `--build` unconditional, AC9 `.first()` no PUT verification, AC7 color robustness under forced-colors/rgba, AC8 RS-recovery flake risk on httpx pool + cold endpoint, AC4 RouterLink href edge cases, AC5 URL invariance only two-point snapshot). ~25 findings dismissed as noise / already-handled. Static gates re-verified post-patch: `tsc --noEmit` exit 0; `playwright --list` reports 26 tests in 6 files unchanged (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3). Epic 4 now fully done — only optional Epic 4 retro remains. NFR11 satisfied with 6 E2E specs covering J1–J6. Earlier on 2026-05-18: story 4.4 dev complete → review — created `e2e/tests/j3-estimate.spec.ts` (5 tests) + `e2e/tests/j6-rs-unavailable.spec.ts` (3 tests) + `e2e/README.md` addendum; ZERO SPA/BFF/RS/compose/helper changes. All static gates green: tsc --noEmit exit 0, playwright --list reports 26 tests in 6 files (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3), compose config exit 0, helper-only grep clean. Live e2e-up (inlined Justfile recipe because host lacks `just` binary; `--build` flag required since playwright Dockerfile bakes specs via `COPY . .`): **26 passed (52.8s)** — NFR11's ≥5 J1-J6 satisfied with 6 specs. Regression: BFF 543/RS 324/SPA 152 unit tests green; SPA build green; SPA lint has 3 pre-existing `no-fallthrough` errors on baseline `bb15aca` in book-form/book-row/status-control (verified pre-existing; logged as D127). Two dev-time spec deviations during the first e2e-up run: AC2-alt `page.route` POST hold applied for J3 happy-path loading-state (round-trip sub-100ms outruns Playwright expect-poll — D128); AC3 second click switched from Re-estimate to Estimate because `/settings → /books` navigation destroys EstimateCell instance and resets component-local signals (D129, story-AC vs SPA-design inconsistency). Epic 4 backend + SPA + E2E now all dev-complete; only awaits code review + Epic 4 retro (optional). Earlier on 2026-05-18: story 4.4 → in-progress — dev started: authoring `e2e/tests/j3-estimate.spec.ts` [5 tests AC2-AC6-J3] + `e2e/tests/j6-rs-unavailable.spec.ts` [3 tests AC7-AC9] + `e2e/README.md` addendum. Earlier on 2026-05-18: story 4.4 contexted → ready-for-dev — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from Epic 4 AC source [epics.md L1696-1740] + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy exports + Story 4.2 BFF POST `/v1/books/{id}/estimate` contract + Story 4.1 `format_duration` boundary table; 13 ACs + 6 tasks; creates 2 spec files [j3-estimate.spec.ts, j6-rs-unavailable.spec.ts] + README addendum; NO source/compose/helper changes — harness complete after 3.6; load-bearing close gate is live `just e2e-up` exits 0 with all 26 tests green [J1×3, J2×8, J3×5, J4×5, J5×2, J6×3] = NFR11's ≥5 J1-J6 satisfied. Earlier on 2026-05-18: story 4.3 merged into main — no-ff merge of `worktree-agent-a32e6a27249f65923` brings the dev (commit 3513e66) + code-review (commit 05b8de7) work back to main; 4.3 = done. The worktree's pre-merge YAML had 4.2 = review reflecting its branch-point timestamp; main has since progressed 4.2 → done (2026-05-18 code review), preserved here. Earlier on 2026-05-18: story 4.2 code review pass: 7 patches applied (P1 empty-body → RsUnavailable centralized in _do_rs_call w/ 401 carve-out for OAuth challenges, P2 per-cookie max-age=0 assertion, P3 NFR6 headers scan, P4 assert_all_called default-True on 24 happy-path tests / kept =False on 9 negative-path tests, P5 handler docstring, P6 expired-session call_count==0, P7 retry-401 token-rotation assertion); 8 defers logged D119-D126; 543 BFF tests passing (+2 new empty-body coverage), coverage 97.26%; rsc.py at 100%; story 4.2 → done. On 2026-05-17: story 4.3 code review pass → done (1 patch applied (P1 tightened test 4 with mid-flight pessimistic-UI assertions on the Re-estimate flow); 10 adversarial findings dismissed as noise; SPA suite 152/152 green, coverage 88.23%/78.57% on estimate-cell.ts and 100%/85% on books-service.ts. Story 4.3 dev complete → review: SPA EstimateCell real component + BooksService.requestEstimate landed in worktree; one new file (estimate.types.ts), six modified; 13 net new tests; audited app-error.types.ts / error-service.ts / error-service.spec.ts — all 4.3-relevant variants and tests already in place from Story 3.5, NO edits. Story 4.3 contexted — file `4-3-spa-estimatecell-real-component-apperror-extensions-bookrow-integration.md` written from 4.2 BFF contract + 3.5 AppError/ErrorService precedent + 2.6 BookRow integration + UX-DR8/DR12-19; 20 ACs + 5 tasks. Story 4.2 dev complete → review: BFF POST /v1/books/{id}/estimate + ResourceServerClient.compute_estimate landed; 4 files modified, 21 + 22 new tests added, per-file coverage 100%, project coverage 97.26%. Story 4.2 contexted from 4.1 + 3.5 + 2.2 precedents; 15 ACs + 10 tasks. Story 4.1 code review pass: 6 patches applied (P1-P6), 6 defers logged D113-D118.)
project: BMAD_books
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Foundational Login & Identity (J1, J5)
  epic-1: done
  1-1-repo-scaffold-compose-skeleton: done
  1-2-keycloak-realm-as-code-compose-service: done
  1-3-bff-scaffold-from-archetype-baseline-health-lint-test-gates: done
  1-4-bff-session-and-auth-state-schema-alembic-migration: done
  1-5-bff-cookie-session-oidc-plugin-pkce-synthetic-idp-test-harness: done
  1-6-bff-csrf-middleware-csp-header: done
  1-7-bff-logout-endpoint-revoke-end-session-degrade-honestly: done
  1-8-spa-scaffold-tailwind-v4-design-tokens: done
  1-9-spa-authservice-interceptors-functional-guards: done
  1-10-spa-loginview-topchrome-route-table: done
  1-11-playwright-project-setup-fixtures-helpers: done
  1-12-bff-post-v1-test-reset-endpoint: done
  1-13-e2e-spec-j1-first-time-login-j5-logout: done
  1-14-bff-multi-stage-build-serves-spa-bundle: done
  epic-1-retrospective: done

  # Epic 2: Personal Book List (J2)
  epic-2: in-progress
  2-1-bff-book-sqlmodel-migration-pydantic-boundary-models: done
  2-2-bff-full-books-crud-v1-books-v1-books-id: done
  2-3-bff-extend-v1-test-reset-to-truncate-books: done
  2-4-spa-booksservice-types: done
  2-5-spa-booklist-page-bookform-add-variant-states: done
  2-6-spa-bookrow-statuscontrol-with-optimistic-ui-edit-delete: done
  2-7-e2e-spec-j2-manage-books: done
  epic-2-retrospective: done

  # Epic 3: Reading Speed Settings (J4)
  epic-3: in-progress
  3-1-rs-scaffold-from-archetype-baseline-health-rs-in-compose-default-dev: done
  3-2-rs-oidc-bearer-auth-plugin-jwks-scope-enforcement-synthetic-idp-test-harness: done
  3-3-rs-reading-speed-model-migration-v1-reading-speed-get-put-scope-gated: done
  3-4-rs-post-v1-test-reset-endpoint: done
  3-5-bff-resourceserverclient-refresh-replay-reading-speed-proxy-spa-settingsview-route: done
  3-6-e2e-spec-j4-adjust-reading-speed-compose-e2e-profile-killrs-startrs-resetstate-helpers: done
  epic-3-retrospective: optional

  # Epic 4: Reading-Time Estimate & Honest Failure (J3, J6)
  epic-4: in-progress
  4-1-rs-post-v1-estimate-endpoint-estimate-service-format-duration-helper: done
  4-2-bff-post-v1-books-id-estimate-resourceserverclient-compute-estimate: done
  4-3-spa-estimatecell-real-component-apperror-extensions-bookrow-integration: done
  4-4-e2e-specs-j3-estimate-j6-rs-unavailable: review
  epic-4-retrospective: optional

  # Epic 5: Final Coverage Push & Security Review
  epic-5: in-progress
  5-1-coverage-audit-gap-fill: done
  5-2-security-review-document: done
  5-3-readme-polish-ai-integration-log: done
  5-4-final-docker-compose-up-smoke-default-profile: done
  epic-5-retrospective: optional
