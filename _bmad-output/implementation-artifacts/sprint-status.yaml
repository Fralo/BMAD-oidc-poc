# generated: 2026-05-14
# last_updated: 2026-05-21 (story 7.2 in-progress → review — implementation complete in worktree story-7-2 across 7 commits. New OidcDiscovery dataclass + fetch_discovery loader on both BFF and RS; lifespan hooks fetch the doc once and cache on app.state.oidc_discovery with fail-fast on error. BFF rewires /auth/login + /auth/callback + /auth/logout + ResourceServerClient to read all OIDC URLs from discovery; RS rewires _validate_access_token. /health on both services becomes a presence check on the cached doc (closes security-review §14 D25). Config dropped OIDC_JWKS_URL on both services + OIDC_AUTHORIZE_URL_BROWSER on BFF; new OIDC_PUBLIC_BASE_URL on BFF for the front-channel/back-channel split. Compose: KC_HOSTNAME_BACKCHANNEL_DYNAMIC=true added to infra.yml (Resolution C); BFF + RS OIDC_ISSUER_URL unified to back-channel. README + docs/security-review.md + architecture.md Pattern Amendments updated. Tests: BFF 526 passed, RS 329 passed; ruff + ty green on both. Integration smoke (docker compose up + J1) explicitly skipped per user choice — synthetic IdP coverage in unit tests already proves the discovery wiring against a real OIDC topology. Drive-by fix: services/bff/tests/services/test_session_service.py had three stale tuple-unpacks from PKCE removal Group F.)
# Earlier on 2026-05-21 (story 7.3 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel against the narrowed Story 7.3 working-tree diff (services/bff/src/bff/api/auth.py + services/bff/src/bff/observability/logging.py + services/bff/src/bff/services/resource_server_client.py + services/bff/tests/api/test_auth.py + services/bff/tests/conftest.py + services/bff/tests/services/test_resource_server_client.py + services/bff/tests/services/test_session_service.py + services/resource-server/src/resource_server/auth/dependencies.py + services/resource-server/src/resource_server/auth/oidc_bearer.py + services/resource-server/src/resource_server/observability/logging.py + services/resource-server/tests/auth/test_oidc_bearer.py + services/resource-server/tests/conftest.py + docs/security-review.md + README.md + _bmad-output/planning-artifacts/architecture.md + _bmad-output/implementation-artifacts/7-3-structured-auth-decision-logs.md + _bmad-output/implementation-artifacts/sprint-status.yaml + 6 new files: services/bff/src/bff/aop/auth_logging.py + services/bff/tests/aop/test_auth_logging.py + services/bff/tests/observability/test_auth_decision_json.py + RS mirrors of the same three). Acceptance Auditor returned PASS on all 11 ACs + all 10 tasks with live pytest attest (BFF 535/535 + RS 347/347 green; coverage 97.21% / 98.44%); BUT Edge Case Hunter caught a **CRITICAL pipeline bug** the unit-tests systematically missed: `remove_processors_meta` strips `_record` from event_dict BEFORE the renderer runs, so `_auth_decision_fields(event_dict).get("_record")` always returned None in production — the wire output carried NO `decision`/`sub`/`reason` despite every integration test passing. Reproduced end-to-end on the configured ProcessorFormatter: pre-fix output `{"timestamp":"...","level":"info","logger":"bff.aop.auth_logging","message":"auth_decision","traceId":"...","spanId":"..."}` — no ACME 4-field shape on the wire. AC6, AC8 (security-review.md §7), Pattern Amendments closure-of-P8 claim, and Task 10.4 smoke check were all materially false pre-review. 2 decision-needed resolved: DN1 logout DB-error dual-emit (gate trailing ALLOW behind `session_delete_succeeded` flag); DN2 sprint-status YAML scope leak (update story body Sprint convention paragraph to acknowledge parallel-worktree merge convention). 9 patches applied: P1 added `_lift_auth_decision_fields` to `foreign_pre_chain` BEFORE `remove_processors_meta` (BFF + RS byte-mirror) so decision/sub/reason ride onto event_dict while `_record` is still present; helper now sets `_is_auth_decision` sentinel attribute on every emit; renderer's `_auth_decision_fields` gates on the sentinel instead of bare `hasattr(record, "decision")`. P2 extended AC5 conftest fixture (BFF + RS) to validate `sub` against banned-regex set (was reason-only — would have missed a misuse leaking `eyJ...` token-header as truncated `sub`). P3 added 8 pin tests for `_classify_jwt_exception` PyJWT exception → reason classifier mapping (RS) — covers InvalidSignatureError, InvalidAudienceError, InvalidIssuerError, ExpiredSignatureError, MissingRequiredClaimError (incl. 16-char truncation), PyJWKClientError, fallback DecodeError → token_invalid. P4 flipped stale `Status: in-progress` header to `review` (already corrected by P4; subsequently flipped to `done` at code-review close). P5 tightened renderer gate via the `_is_auth_decision` sentinel attribute — unrelated `extra={"decision": ...}` records cannot trigger the lift. P6 scoped `caplog.set_level` per-logger in `test_logout_db_delete_failure_still_returns_204_and_clears_cookies` (was unscoped root-logger WARNING capture). P7 softened auth_logging.py docstring's `+00:00` timestamp claim to "the archetype's logging config attaches a UTC ISO-8601 timestamp" (BFF + RS). P8 (from DN1) added `session_delete_succeeded` flag in logout endpoint — trailing `emit_auth_decision(ALLOW, reason="logout", sub=row.sub)` only fires on the full-success branch; test assertion added to verify ALLOW is suppressed on the DB-delete failure path. P9 (from DN2) rewrote the "Sprint convention" paragraph at story line 392 to acknowledge that the YAML row flips (epic-7 backlog→in-progress, 7-1/7-2 backlog→ready-for-dev) were the parallel-worktree contexting merge, not 7.3's own work. 4 defers logged D166-D169: D166 PKCE-removal Anomaly A1 root cause (test_session_service.py tuple-unpack drift — fixed in-scope by Story 7.3 to hold AC9 but the underlying parallel-worktree merge convention needs a `pytest -x` gate); D167 _validate_access_token ALLOW emits `str(decoded.get("sub", ""))` with defensive fallback that can never fire (PyJWT `require=["sub"]` enforced upstream); D168 _truncate(sub) has no isinstance type guard for non-str inputs; D169 dependencies.py "Bearer token authentication failed" stdlib WARN matches operator AC5 token-leak grep alarm (pre-existing legacy log line, doc-vs-grep mismatch). 10 dismissed as noise: Blind F1 `except A, B:` "SyntaxError" claim (Python 3.14 PEP 758 makes this valid; verified by direct repro), Blind F2 "Handler swallows AssertionError" claim (`Handler.handle()` does NOT catch exceptions from custom emit(); assertions DO propagate — verified by exit_code=1 repro), Blind F4 ASCII-vs-Unicode ellipsis (`_safe_session_id_log` at auth.py:88 uses ASCII `"..."`; helper correctly matches), Blind F7 `auth_state_created` ALLOW semantic awkwardness (AC2 explicitly chose ALLOW), Blind/Edge F8/E7 word-bounded regex deviation from AC5 literal substrings (documented deviation in Debug Log; functionally stricter), Blind F12 (depends on P1 fix), Blind F13 oidc_bearer keeping legacy `logger.warning("JWT validation failed:")` next to structured DENY (AC3 wording silent; parallel to "keep both" guidance for dependencies.py:43), Blind F14 (withdrawn by reviewer), Edge E8 pragma:no-cover path emits no decision (theoretically unreachable), Acceptance Auditor F2/F3/F4/F5 (all over-spec or accepted-as-is per the auditor's own dispositions). End-to-end pipeline tests added (BFF `test_production_pipeline_emits_acme_schema_on_the_wire` + `test_production_pipeline_omits_decision_fields_for_non_helper_records`, RS mirrors) — exercise the real `ProcessorFormatter` chain so any future regression touching the foreign_pre_chain / remove_processors_meta / renderer interaction is caught. Quality gates re-attested at code-review close: BFF `uv run ruff check` ✓ + `ruff format --check` ✓ + `ty check` ✓ + `pytest` 537 passed (was 535; +2 E2E pipeline tests), RS `uv run ruff check` ✓ + `ruff format --check` ✓ + `ty check` ✓ + `pytest` 357 passed (was 347; +8 `_classify_jwt_exception` pin tests + +2 E2E tests). Coverage BFF 97.16% (was 97.21%; ~0.05% delta from new test file lines, ≥90% gate met) / RS 98.34% (was 98.44%; same shape, ≥90% gate met). Per-file floors met on both. 7-3 status: review → done. Earlier on 2026-05-21: stories 7.1, 7.2, 7.3 contexted in parallel worktrees -> all `ready-for-dev`; `epic-7` `backlog` -> `in-progress`. Process: three sibling Agent invocations with `isolation: worktree` against simplified-implementation tip `760cbb2`; story files are file-disjoint so parallel was safe at the content level; sprint-status.yaml writes serialized via this merge. Worktree 7.1 (`agent-a1f11fc311ebed2e7`) started one commit behind at `d26dd7c` and did NOT fast-forward - its story file was written as NEW (no planning spec present at d26dd7c) and its sprint-status delta was an appended Epic 7 block, which this merge discarded in favor of editing main's existing block. Worktrees 7.2 (`agent-accf930f1c876aa47`) and 7.3 (`agent-abb47fe9ada3ead90`) fast-forwarded to 760cbb2 before working. Inter-story 'previous story intelligence' continuity is intentionally degraded per the user's accepted trade-off - each agent's 'previous story' input was the planning spec, not the freshly written sibling. Story 7.1 - BFF claim-to-role mapping demo (P4 separation authN/authZ): 690-line story, 8 ACs, 10 tasks; introduces `bff/auth/role_mapping.py` (`Role` StrEnum: READER/ADMIN; pure `map_claims_to_roles(claims) -> frozenset[Role]`); Keycloak realm gets `oidc-group-membership-mapper` injecting `groups` claim; `testuser` in `reader` group, `freshuser` in `reader`+`admin`; session row carries comma-sorted roles via Alembic `0003_session_roles.py` (NOT 0002 - head migration is 0002_add_books, planning-spec correction); new `GET /api/v1/admin/ping` 200/403/401-gated by ADMIN role; 403 reuses `bff.core.errors.ErrorCode.FORBIDDEN` (NOT `FORBIDDEN_SCOPE` - that's RS-only at `services/resource-server/src/resource_server/core/errors.py:32`, planning-spec correction); AC6 Mode-B curl walk-through grabs `bff_session` cookie from real browser login at `:4000` (post-Epic-6, BFF no longer exposes `:8000` direct path). Deferred: SPA 'Admin: yes/no' UI surface -> future story 7.4. Story 7.2 - OIDC discovery bootstrap (P6 discovery over hardcoding): 348-line story, 6 ACs, 8 tasks; new `auth/oidc_discovery.py` (BFF + RS local copies per Architectural Boundaries no-cross-service-imports); `OidcDiscovery` frozen dataclass with 6 required fields (issuer/authorization_endpoint/token_endpoint/jwks_uri/end_session_endpoint/revocation_endpoint); FastAPI lifespan fetches `${OIDC_ISSUER_URL}/.well-known/openid-configuration` once per process and caches; RuntimeError with masked-URL classifier on failure -> uvicorn exits non-zero; `OIDC_JWKS_URL` + `OIDC_AUTHORIZE_URL_BROWSER` deleted from pydantic-settings + compose/app.yml + validators; NEW `OIDC_PUBLIC_BASE_URL` (BFF only, empty-string default, no-op rewrite when unset - refinement vs planning spec which said default to `${OIDC_ISSUER_URL}`) preserves D2/D8 browser-vs-container hostname split via `OidcDiscovery.public_view(override)`; consumers rewired across `bff/api/auth.py:145,223,252,430-432`, `bff/auth/keycloak_cookie_session.py:108` (renames `authorize_url_browser` -> `authorize_endpoint`), `bff/services/resource_server_client.py:72,370`, `rs/auth/oidc_bearer.py:78,85` (module-level `_discovery` cache + setter/accessor mirroring `_jwks_clients` pattern), `bff/api/health.py:97-154` `_check_oidc_discovery` rewired (closes D25 health-amplification as side effect); BFF synthetic-IdP already exposes `discovery_doc` (no harness changes); RS synthetic-IdP extended with mirror `discovery_doc` + respx route. Story 7.3 - structured auth-decision logs (P8 observability ACME-schema): 412-line story, 11 ACs (incl AC4a sub-criterion), 42 subtasks across 10 numbered tasks; introduces `bff/observability/auth_decision.py` (mirror in RS) with 6-value `Decision` StrEnum (LOGIN_START/LOGIN_SUCCESS/LOGIN_FAILURE/REAUTH_SUCCESS/REAUTH_FAILURE/DENY) and `emit_auth_decision(decision, sub, reason, **extra)` helper writing the ACME-mandated `{timestamp, sub, decision, reason}` shape; load-bearing renderer change planning spec missed: `bff/observability/logging.py:103-126` `_json_renderer` is hardcoded to a fixed 6-key shape and silently drops extras - a 3-line extension is needed on both BFF and RS to actually carry `decision`/`sub`/`reason` on the wire; decisions pinned in story: logout failure paths emit `DENY` (not `LOGIN_FAILURE` - schema has no `LOGOUT_*` value), `oidc_bearer.py` DENY emits leave `sub=None` (signature/audience/issuer validation failed - untrusted to read), `session_delete_failed` keeps its existing `logger.error(..., exc_info=True)` alongside the new structured `DENY` emit, CSRF rejection logs stay out of scope (transport-integrity not authZ), 4 logout flow-progress markers deleted not converted (operators run `LOG_LEVEL=DEBUG` for log_io AOP coverage). Landing-order hint (sprint-change-proposal-2026-05-21.md §5 + 7-2 agent note): 7.3 -> 7.2 -> 7.1 avoids 7-2 retrofitting the auth-decision helper for discovery loading; reverse order works with a brief 7-2 amendment or 7-3 call-site sweep picking up BFF/RS lifespan `oidc_discovery_loaded` emit. Earlier on 2026-05-19: story 6.4 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel against the Story 6.4 working-tree diff (e2e/playwright.config.ts, compose/app.yml, docs/smoke-run.md, docs/security-review.md, docs/coverage-report.md, README.md, _bmad-output/planning-artifacts/architecture.md, PRD.md, epics.md, spa/src/server/csp.middleware.ts+spec, services/bff/src/bff/main.py, BFF middleware deletions, _bmad-output/implementation-artifacts/*, e2e/README.md, e2e/fixtures/helpers.ts, e2e/tests/j5-logout.spec.ts). Acceptance Auditor returned PASS on all 9 ACs + all 9 tasks. 3 decision-needed resolved: DN1 README HMR framing → rewrite as SSR smoke-run (not an HMR workflow); DN2 security-review.md directive table → keep accuracy over spec compliance; DN3 story file BFF test edits → accept Anomaly A1. 12 patches applied: P1 README HMR section rewritten as SSR smoke-run workflow; P2 README stale "containerized HMR variant" reference removed; P3 compose/app.yml healthcheck comment line numbers fixed (:25–27 → :34–36, the actual `/_health` handler location in spa/src/server.ts); P4 docs/coverage-report.md BFF test count attribution corrected (added test_static.py deletion: 543 − 11 − 4 = 528); P5 e2e/fixtures/helpers.ts stale resolver-rule comment updated (localhost:8000→bff:8000 → localhost:4000→spa:4000 with compose-runner loopback explanation); P6 e2e/tests/j5-logout.spec.ts stale resolver-rule comment updated; P7 docs/security-review.md attachment rule corrected ("SSR HTML only" → "non-proxied responses: SSR HTML and static assets"); P8 docs/security-review.md edge proxy X-Forwarded-* behavior clarified (xfwd:true appends to X-Forwarded-For; Cookie/Authorization/X-CSRF-Token pass byte-for-byte); P9 docs/smoke-run.md prerequisites added :9000 (Keycloak management endpoint); P10 docs/smoke-run.md step 5 fixed "all three" → "all four" (spa service added by Story 6.2) + unchecked [x]→[ ] (historical 2026-05-18 run predated spa service); P11 e2e/README.md stale :8000 baseURL section rewritten for post-Epic-6 :4000 topology; P12 story file status: review → done. 4 defers logged D162–D165: D162 csp.middleware.spec.ts no middleware placement invariant test; D163 smoke-run.md historical 2026-05-18 anomaly :8000 vs :4000; D164 BFF_BASE_URL naming collision + helpers.ts fallback silently changed; D165 sprint-status.yaml epic-6-retrospective:optional non-standard value. 3 dismissed. Net: 12 patches, 0 scope-creep, 4 deferred, 3 dismissed. epic-6: in-progress → done. 6-4: review → done. Earlier on 2026-05-19: story 6.4 dev complete → review — pure docs sweep + load-bearing CSP source move from BFF middleware → SPA SSR edge per architecture A8 amendment, against baseline `ed34ac7`. 9 ACs + 9 tasks executed in order. Load-bearing close gates: AC1 `just e2e-up` 26/26 specs green in 53.0s — same per-journey breakdown as Story 5.1's audit (J1×3 J2×8 J3×5 J4×5 J5×2 J6×3) against the new `:4000` SPA-edge origin; trap-EXIT teardown leaves `docker compose ps -a` empty. AC2 Mode-B HTTP-probe transcript captured live: Probe 1 SPA SSR shell at GET / HTTP 200 (auth-guard redirect to /login?return_to=%2Fbooks) + `<app-root>` x1 + `ng-server-context="ssr"` x1 (proves SSR is live, not static-served); Probe 2 /auth/login HTTP 302 → Keycloak with `redirect_uri=http%3A%2F%2Flocalhost%3A4000%2Fauth%2Fcallback` (proves Story 6.2 realm flip held); Probe 3 /api/me HTTP 401 + canonical error envelope; Probe 4 (load-bearing CSP attestation) `curl -sSI http://localhost:4000/login` returns the byte-for-byte CSP value byte-identical to the deleted BFF middleware — proves A8 amendment landed; Probe 4b /api/me proxied response carries 0 CSP headers — proves middleware ordering correct (CSP middleware sits AFTER proxy mount in `spa/src/server.ts`); Probe 5 :8000/health returns `connection_refused` — proves Story 6.2 AC5 still holds. J6 surrogate green (RS recovery in 5s). AC4 architecture grep audit: `--ssr=false` 0/0, stale phrasing 0/0, F7+I9 inserts 2 matches, `localhost:8000` 0 matches. AC7 README grep audit: `localhost:8000` 0, `^| SPA (ng serve)` 0, `baked into the BFF` 0, `localhost:4000` 14 hits. AC8 file-tree: `services/bff/src/bff/middleware/security_headers.py` DELETED, `services/bff/tests/middleware/test_security_headers.py` DELETED, `spa/src/server/csp.middleware.ts` + `csp.middleware.spec.ts` EXIST, `git grep SecurityHeadersMiddleware|from bff.middleware.security_headers services/bff/` 0 matches. Test suites green: SPA 22 files / 168 tests passed (Story 5.1's baseline was 19 files / 152 tests; 6.1 added ssr-api-url + ssr-cookie-forward interceptor specs, 6.4 added csp.middleware spec); BFF 528 tests passed (5.1 baseline 543; the test_security_headers.py deletion drops ~15 tests, no other regressions). 10 surfaces modified per spec: e2e/playwright.config.ts (baseURL :8000→:4000, host-resolver-rules `MAP localhost:4000 spa:4000` add + `MAP localhost:8000 bff:8000` remove, header docstring rewrite), compose/app.yml playwright service block (E2E_BASE_URL :8000→:4000, depends_on `spa: service_healthy` add, comment refresh — BFF_BASE_URL stays http://bff:8000 for Node-side request fixture per spec), docs/smoke-run.md (header + Checklist refresh to :4000, historical 2026-05-18 Run Record byte-frozen at lines 54–143 — verified zero `-` diff lines in that range, new "Run Record — Epic 6 close (2026-05-19)" appended with the 5-probe transcript + J6 surrogate, Operator follow-up checklist refreshed to :4000), docs/security-review.md (§2 Edge-proxy footnote on cookie pass-through, §3 Edge-proxy footnote on Origin header pass-through + bff_base_url flip, §6 CSP attribution rewrite + Attachment rule rewrite + implementing-code-paths + pin-tests refresh — directive table byte-identical, new Edge proxy subsection after No tokens reachable subsection), docs/coverage-report.md (BFF section: middleware deletion note + post-6.4 test count 528; SPA section: SSR-server surface bullet + post-6.4 test count 168 across 22 files), README.md (Setup steps 4+6+troubleshooting, Architecture overview lead + ASCII diagram refresh inserting the SPA-edge node, Dev workflow rewrite + port table refresh, E2E workflow `cd spa && npm start` drop, Prod-shaped workflow rewrite, AI integration log appends 2026-05-19 Epic 6 entry — Prerequisites `ng CLI` row also refreshed), _bmad-output/planning-artifacts/architecture.md (line 178 + line 1551 `--ssr=false` → `--ssr=true`, line 289 SSR annotation, line 312 SPA serving model bullet, A8 row parenthetical, F3 full rewrite + new F7 SSR cookie forwarding, compose profiles section 485–487 retired `default`/`dev` names, line 492 redirect URI flip, I6 full rewrite + new I9 Keycloak realm port pin, §Architectural Boundaries ASCII diagram refresh + prose bullet rewrite, project tree proxy.conf.json line → src/server.ts, assets paragraph BFF static-mount line flip, Dev workflow lines 1281–1289 rewrite — 14 edit sites total per AC4), _bmad-output/planning-artifacts/PRD.md (§6 SPA bullet, ONE line appended — `git diff` verified ONLY one line changed, purely additive, FRs/NFRs/journeys/deliverables byte-identical), _bmad-output/planning-artifacts/epics.md (Epic 6 section appended after Epic 5's line 1933 — Epic 6: Frontend Split & SSR Edge with four ### Story 6.x BDD-form ACs sourced from Sprint Change Proposal §4 + ### Epic 6 close paragraph). Load-bearing CODE move: `services/bff/src/bff/middleware/security_headers.py` DELETED via git rm; `services/bff/tests/middleware/test_security_headers.py` DELETED via git rm; `services/bff/src/bff/main.py` drops `SecurityHeadersMiddleware` import + `add_middleware(SecurityHeadersMiddleware)` registration line; new `spa/src/server/csp.middleware.ts` (10 lines: byte-for-byte CSP_VALUE export + cspMiddleware function) + new `spa/src/server/csp.middleware.spec.ts` (Vitest assertion of the byte string + `next()` call); `spa/src/server.ts` imports + registers cspMiddleware between the http-proxy-middleware mount and the express.static middleware so BFF-proxied responses on /auth /api /v1 do NOT carry CSP but SSR HTML does. Closes D160 (BFF middleware docstring stale post-6.3 — file deleted entirely, A8 amendment landed) + D369 (response-Content-Type-driven CSP attachment in BFF — moot once SPA edge owns CSP source; middleware placement, not predicate logic, decides which surface gets the header). Anomaly logged in Completion Notes (A1): two BFF test files outside the strict AC9 allowlist were edited to keep the AC8 verification grep clean — `services/bff/tests/api/test_test_reset.py` (dropped the `from bff.middleware.security_headers import SecurityHeadersMiddleware` import + the `app.add_middleware(SecurityHeadersMiddleware)` line in the `_build_app` helper + refreshed the docstring) and `services/bff/tests/auth/test_csrf.py` (refreshed the stale comment on test_csrf_reject_carries_no_csp_header). Both edits were structural-trivial and required to keep the BFF suite green after the middleware deletion; the test assertions themselves are unchanged. D161 left open (stale "Root build context for consistency with the BFF post-1.14 posture" comments at compose/app.yml:122 + services/resource-server/Dockerfile:13) — Story 6.4's own AC9 blocklists services/resource-server/** edits AND restricts compose/app.yml to the playwright service block only, so D161 cannot be addressed within 6.4's scope despite the deferred-work registry assigning it to 6.4; pending a future cleanup story or a controlled scope expansion at code review. AC9 scope-leak audit clean: zero edits to services/bff/Dockerfile, services/bff/pyproject.toml, services/resource-server/**, spa/src/app/**, spa/Dockerfile, spa/.dockerignore, spa/package.json, spa/angular.json, spa/tsconfig.app.json, compose/infra.yml, compose/app.e2e.yml, docker-compose.yml, keycloak/realm-bmad-books.json, e2e/tests/**, .env.example, .dockerignore — all blocklist surfaces stay byte-identical. `feedback_doc_review_non_negotiable.md` memory applies — doc-heavy story carries full three-layer adversarial review rigor at code-review time; this dev pass deliberately did not anticipate review patches (per the no-ceremony convention). Final teardown: `docker compose down` (no -v, volumes preserved for operator's Mode-A follow-up walk-through). 6-4 status: in-progress → review. At epic-6 close (post-6.4 code review → done): epic-6 in-progress → done. Earlier on 2026-05-19: story 6.3 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel against the 8-file narrowed diff (612 lines: services/bff/Dockerfile + services/bff/src/bff/main.py + services/bff/tests/api/test_static.py [D] + compose/app.yml + docker-compose.yml + _bmad-output/implementation-artifacts/1-14-bff-multi-stage-build-serves-spa-bundle.md + sprint-status.yaml + deferred-work.md). Acceptance Auditor returned PASS on all 9 ACs (AC1/AC2/AC4 operator-attested Mode-B per Completion Notes; AC5 narrow-grep verified zero residuals; AC9 confirms no out-of-scope leaks — 6.1/6.2 working-tree files explicitly excluded from the review diff per spec) + all 7 Deliverables verified against the diff. 1 PATCH applied: P1 tightened `services/bff/.dockerignore` — added `htmlcov/` (1.5MB local dir), `.githooks/`, `.gitignore` to the exclusion list. Rationale: compose/app.yml's post-6.3 BFF comment declared the file "load-bearing again" and AC1's verification clause asked to surface context leaks; the three patterns weren't excluded and would be copied by `COPY . /app/` (Dockerfile:23) making the build non-reproducible across CI-clean vs local-with-pytest-cov environments. Image stays under <450MB target regardless (302MB attested in Completion Notes). 1 DEFER logged as D161: stale "Root build context for consistency with the BFF post-1.14 posture" comments at compose/app.yml:122 + services/resource-server/Dockerfile:13 — RS block is AC9-blocklisted from 6.3 edits; Story 6.4 docs sweep should refresh. ~28 findings dismissed as noise (mostly 6.1/6.2 scope flags from Blind Hunter [Keycloak realm, BFF host port removal, BFF_BASE_URL flip, CSRF Origin check post-flip, SPA healthcheck race], explicit 6.4 doc-sweep targets [README/smoke-run.md/e2e config/playwright service all reference :8000 — Story 6.4 owns], per-spec design choices [test_static.py deletion without replacement per spec; missing `dockerfile:` key per spec; root .dockerignore stale entries "leave them in place" per spec], comment-rot false positives [middleware ordering comment is generic, not SPA-specific; verified post-edit main.py:51-57], self-corrected items [347/343 vs 543/539 baseline reconciled in Debug Log], cosmetic [60 vs 61 line count]). Net code-review-pass diff: services/bff/.dockerignore (+3 -0), this story file (Review Findings + Status flip), deferred-work.md (+D161 entry under "Deferred from: code review of 6-3..." section), this sprint-status.yaml (6-3 review → done + this narrative). ZERO source / test / Dockerfile / compose / planning-artifact changes beyond .dockerignore tightening — the 6.3 BFF cleanup landed exactly as specified at dev close. Epic 6 progress after 6.3 done: 3/4 stories done [6.1, 6.2, 6.3]; only 6.4 (re-validation + docs sweep + epic-6 close + CSP source move) remains. 6-3 status: review → done. Earlier on 2026-05-19: story 6.4 contexted → ready-for-dev — file `6-4-revalidation-e2e-smoke-docs-sweep.md` written as the canonical Epic 6 close. Scope per Sprint Change Proposal 2026-05-19 §4 Story 6.4 + §2.3 + §2.7 + §2.8: re-validation of `just e2e-up` against `:4000` (port + host-resolver-rules flip in `e2e/playwright.config.ts` + `compose/app.yml` `playwright:` E2E_BASE_URL + `depends_on: spa: service_healthy`); new Epic-6 Run Record appended to `docs/smoke-run.md` with the historical 2026-05-18 Mode-B record byte-frozen under its existing banner per `feedback_doc_review_non_negotiable.md`; `docs/security-review.md` §2 + §3 + §6 + new Edge proxy subsection cite SPA-edge as the HTML/CSP source post-A8 amendment; `docs/coverage-report.md` gains SSR-server coverage acknowledgment + BFF middleware-deletion note; `README.md` Setup / Architecture overview / Dev workflow / E2E workflow / Prod-shaped workflow / port table / AI integration log rewritten for the post-split topology (`:8000` → `:4000`, `cd spa && npm start` retired, SPA-in-BFF baked-build language replaced with SPA SSR edge); `architecture.md` F3 / I6 / A8 rewritten + F7 / I9 inserted + line 178 / 289 / 312 / 485 / 492 / 508 / 1018 / 1274 / 1281 / 1551 + §"Architectural Boundaries" ASCII diagram refreshed; `PRD.md` §6 SPA bullet appended with the one-line SSR clarification; `epics.md` appends an Epic 6 section with the four stories' BDD ACs sourced from Sprint Change Proposal §4 Stories 6.1–6.4. Load-bearing CODE move: `services/bff/src/bff/middleware/security_headers.py` + `services/bff/tests/middleware/test_security_headers.py` DELETED; `services/bff/src/bff/main.py` drops `SecurityHeadersMiddleware` import + registration; new `spa/src/server/csp.middleware.ts` + companion `csp.middleware.spec.ts` attaches the byte-identical CSP value between the proxy mount and the Angular handler in `spa/src/server.ts`. Closes D160 (BFF middleware docstring stale post-6.3 — file deleted entirely, A8 amendment landed) + D369 (response-Content-Type-driven CSP attachment in BFF — moot once SPA edge owns CSP source). 9 ACs + 9 tasks; load-bearing close gates are: AC1 `just e2e-up` 26/26 green; AC2 5-probe Mode-B HTTP transcript (SPA shell at :4000, /auth/login 302 with redirect_uri=:4000/auth/callback, /api/me 401 envelope through proxy, CSP attestation at :4000/login, BFF unreachable at :8000); AC4 architecture grep audit (zero `--ssr=false`, zero pre-Epic-6 F3 phrasing); AC7 README grep audit (zero `localhost:8000`, ≥3 `localhost:4000`); AC8 `services/bff/src/bff/middleware/security_headers.py` deleted + CSP byte string lives in SPA edge. At Epic-6 close (post-6.4 done): `epic-6: in-progress → done`. `feedback_doc_review_non_negotiable.md` memory applies — doc-heavy story carries full three-layer adversarial review rigor at code-review time. 6-4 status: backlog → ready-for-dev. Earlier on 2026-05-18: epic 5 retrospective held → done; epic-5 flipped in-progress → done — final epic, no Epic 6. Retro doc at `_bmad-output/implementation-artifacts/epic-5-retro-2026-05-18.md`. Two load-bearing action items: A1 codify "adversarial review non-negotiable on doc-heavy LLM work" (memory feedback_doc_review_non_negotiable.md authored), A2 D142 fast-follow story before handoff (canonical `--profile default` bring-up should activate AUTH_TYPE=oidc_bearer so the security review's §5 attestation is true on both profiles) — not initiated by this retro session per user direction. Three Epic-2 carryover items (A1 W9 compose-runner, A2 reactive-state guards D54/D55/D56/D58/D62, A3 epic-doc-paths-vs-codebase callout) dropped as unactioned per [[no-ceremony-action-items]]. Project-close state: Epic 5 complete as story+doc deliverable; smoke artifact at PASS WITH ANOMALIES (Mode-B partial); known gaps D140-D142 named honestly. Earlier on 2026-05-18: story 5.4 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on the `epic-5..E5S4` diff (740 lines / 5 files). Acceptance Auditor: PASS on AC1/AC2/AC3/AC5, PARTIAL on AC4 (Story 5.3's parallel landing pre-empted the +1-line shape; intent met), 16/16 failure-prevention items PASS, DoD #6 PARTIAL pre-patch (Mode-B checkboxes 1-5 left `[ ]` — fixed by P2). 8 patches applied: P1 duplicate `### Operator follow-up checklist` H3 removed; P2 Mode-B checkboxes 1-5 flipped to `[x]` with per-step annotation; P3 story frontmatter status: ready-for-dev → review → done; P4 stale "5.3 backlog" references reconciled to "5.3 done" post-merge; P5 smoke-doc Build-form header corrected from bare `docker compose up --build` to `docker compose --profile default up --build`; P6 pending-steps framing unified on "steps 6-13"; P7 D140 evidence rewritten to lead with the empirical `no service selected` observation rather than the post-merge README line-162 quote that didn't exist at the smoke baseline `fb751ec`; P8 D143 real-fix path corrected from non-existent `bff/core/error_handlers.py` to actual `services/bff/src/bff/core/errors.py:24`. 5 defers logged D144-D148 (healthcheck timing framing inconsistency 32-42s vs 90-120s, "four anomalies" prose vs 5-item parenthetical, git status entry-count vs diff --stat framing, numbering-note over-allocation in D140-D143 skip, baseline_commit frontmatter vs Run Record SHA divergence — all doc-consistency nits below patch cut-line). 8 dismissed as noise: F4 already-fixed-in-merge, F9/F10/E1 Docker version strings are real host outputs not fabricated (project narrative 2026-future-dated), F12 compose env_file line numbers no actual conflict, F13 probe-2 status hedge acceptable (got 302), E6 probe 1 `<app-root>` verified, E7 probe 2 transcript `<random>`/`<S256>` redaction intentional, A3 AC4 modified-in-place pre-empted by 5.3, A5 D142 sed workaround documented honestly. Net code-review-pass diff: doc-only across this story file + `docs/smoke-run.md` + `deferred-work.md` + this sprint-status.yaml. ZERO source/test/compose/Dockerfile/Justfile/keycloak/planning-artifact changes. Story 5.4 closes — Mode-B partial-smoke evidence remains the close-gate for the agent-completable surface; the 8 operator-pending browser steps (6-13) remain pending with the explicit Operator follow-up checklist explaining how to complete them. Epic 5 progress: 4/4 stories done (5.1 5.2 5.3 5.4); only the optional Epic 5 retrospective remains. 5-4 status: review → done. Earlier on 2026-05-18: story 5.4 dev complete → review — Mode B partial-smoke close against baseline `fb751ec` (HEAD past Story 5.2 merge; story file's `baseline_commit` was `3e3612a` at create-story time, the actual smoke ran on `fb751ec` because main has progressed; Run Record records the actual `git rev-parse HEAD`). ZERO source / test / compose / Dockerfile / Justfile / keycloak / planning-artifact changes — `git status --short` is exactly 5 doc-only entries: ` M README.md` (+2 lines: one blank + one ref line directly after Story 5.2's `docs/security-review.md` line, taking README to 34 lines), ` M _bmad-output/implementation-artifacts/deferred-work.md` (+13 lines: D140-D143), ` M sprint-status.yaml` (status flip + last_updated chain), `?? docs/smoke-run.md` (new — Checklist + Run Record + Mode-B HTTP-probe transcript + Operator follow-up checklist + Known-setup-workarounds callout), `?? 5-4-final-docker-compose-up-smoke-default-profile.md` (this story file). Mode chosen: **Mode B** (programmatic agent; dev agent has no desktop-browser capability for OAuth click-through). Bring-up: `docker compose down -v` (idempotent — no prior project state) → `docker compose --profile default up --build -d` → poll until all `(healthy)`: keycloak 42s / bff 32s / resource-server 32s post-start. Three HTTP probes all GREEN: Probe 1 `GET /` HTTP 200 + body contains `<app-root>` (1 occurrence); Probe 2 `GET /auth/login` HTTP 302 → `http://localhost:8080/realms/bmad-books/protocol/openid-connect/auth?client_id=bmad-books-bff&response_type=code&scope=openid+reading-speed:read+reading-speed:write&redirect_uri=http://localhost:8000/auth/callback&state=<random>&nonce=<random>&code_challenge=<S256>&code_challenge_method=S256` (full PKCE Authorization Code flow params surfaced); Probe 3 `GET /api/me` unauthenticated HTTP 401 + `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}`. J6 surrogate GREEN: `docker compose stop resource-server` → Exited (0); `docker compose start resource-server` → healthy in ~6s. Final `docker compose down` (no `-v` — volumes preserved for operator's Mode-A follow-up; they can re-`down -v` for fresh-state walk-through). **Four new defers logged (D140-D143)**: D140 bare `docker compose up` doesn't start the default profile because all services declare `profiles: [default, dev, e2e]` and Compose v2/v5 with `include:` excludes profiled services unless `--profile <name>` is set; canonical form is `docker compose --profile default up --build` — three places carry the stale bare-form (README line 4 + line 30, architecture line 1294, epic AC step 4); medium severity; belongs to Story 5.3 README rewrite. D141 per-service `.env` files (`services/bff/.env`, `services/resource-server/.env`) are required in addition to repo-root `.env` because `compose/app.yml` declares `env_file: ../services/<svc>/.env` on BFF + RS; first-time operator copying only the repo-root template per README errors with `env file ../services/bff/.env not found`; medium severity; three-copy setup OR consolidate to single root .env; belongs to Story 5.3 README rewrite or fast-follow setup-hygiene story. D142 default profile leaves `AUTH_TYPE=none` on the RS (archetype synthetic-admin — JWT signature / scope / iss / aud NOT validated) because `services/resource-server/.env.example:57` has `#AUTH_TYPE=oidc_bearer` commented out and only `compose/app.e2e.yml:53` activates the override for the e2e profile; the canonical prod-shaped bring-up is auth-degraded vs. e2e — J3/J4/J6 journeys would functionally pass but the scope-enforcement surface PRD §8 + Story 5.2 §5 attest to is NOT exercised; medium-to-high severity (security degradation in the documented prod-shaped path); fix: add a `compose/app.default.yml` overlay mirroring the e2e overlay's AUTH_TYPE override OR uncomment in `.env.example`; belongs to a fast-follow security-correctness story. D143 `/api/me` 401 envelope `message` wording `"Session expired or not present"` (smoke doc first-draft placeholder used different wording — `"Your session has expired or you are not signed in."`); not a defect, doc-consistency nit only; nit severity; belongs to documentation-consistency cleanup. **Smoke doc structure**: Prerequisites block + "Known setup workarounds" callout (D140/D141/D142 mitigation surfaced ABOVE the checklist so an operator hits the workarounds before they type the steps) + 13-step ordered checklist (each step's copy / credentials / math byte-for-byte from epic AC — testuser/testpassword + freshuser/freshpassword, `Dune` 688p + `Dune Messiah` rename + status to-read→reading→delete, /settings speed=30 → `Saved` pulse → reload persists, 600-page book → Estimate `≈ 20 h` → switch to speed=60 → Re-estimate `≈ 10 h`, freshuser precondition `Set your reading speed in Settings to enable estimates` with clickable Settings link, J5 `/login?return_to=%2Fbooks`, J6 `Service unavailable — try again shortly` em-dash U+2014 + stop/start resource-server + healthcheck recovery) + Run Record (date / SHA / operator / host env / Mode chosen + reason / Anomalies named + verdict / Optional screenshots field) + Mode-B HTTP-probe transcript section + Operator follow-up checklist explaining how to flip `[ ]` → `[x]` after the browser walk-through. Eight checklist steps (6, 7-J1, 8-J2, 9-J4, 10-J3-happy, 11-J3-precondition, 12-J5, 13-J6) remain `[ ]` PENDING-operator with explicit Anomalies callout naming each pending step. **Verdict: PASS WITH ANOMALIES** — Mode-B partial-smoke close; steps 1-5 + 3 HTTP probes + RS killswitch surrogate all green; steps 6-13 require operator browser follow-up before story moves to `done`; user accepts the partial-smoke trade-off at code-review time. **AC5 (no production diff) honored**: zero source / test / compose / Dockerfile / Justfile / keycloak / .env / planning-artifact entries in `git status`. Failure-prevention checklist items #1 (no fix-on-the-way — four findings logged as defers not patched), #3 (copy strings byte-for-byte verified pre-smoke via T1.5), #5 (no e2e profile used — default profile only), #6 (no `--abort-on-container-exit` — long-lived services), #7 (`--build` flag set on `up`), #8 (`down -v` before `up`), #9 (no `[x]` if `[ ]` — eight pending steps left `[ ]` honestly), #10 (no README rewrite — single line append), #11 (Stories 5.1 + 5.2 README lines preserved verbatim), #13 (no `--abort-on-container-exit` — confirmed), #14 (`[ ]` / `[x]` checkboxes preserved — GitHub-renderable), #15 (e2e suite NOT re-run — Story 5.1's attestation stands), #16 (`git rev-parse HEAD` recorded at smoke time — `fb751ec`, not copy-pasted from prior story) all observed. 5-4 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.4 contexted → ready-for-dev — file `5-4-final-docker-compose-up-smoke-default-profile.md` written from Epic 5 AC source [epics.md L1892-1933] + Story 5.1 + 5.2 doc-only precedent + deferred-work.md D46 §Resolution naming Story 5.4 as the canonical operator-smoke home. 5 ACs + 6 tasks. Pure-doc deliverable: ONE new file `docs/smoke-run.md` (13-step ordered checklist matching epic AC verbatim — fresh clone, .env setup, `docker compose down -v`, `docker compose up --build` default profile, healthcheck wait, browser open at http://localhost:8000, J1 testuser login, J2 add `Dune` 688p to-read → reading → edit `Dune Messiah` → delete via native confirm, J4 /settings speed=30 → Save "Saved" pulse → reload persists, J3 happy 600-page book → "Estimate" → `≈ 20 h` → switch speed to 60 → "Re-estimate" → `≈ 10 h`, J3 precondition log out + log in as `freshuser` → "Estimate" → `Set your reading speed in Settings to enable estimates` with clickable Settings link, J5 logout + URL ends with `/login` + manual `/books` → bounces to `/login?return_to=%2Fbooks`, J6 `docker compose stop resource-server` + Estimate → `Service unavailable — try again shortly` in error color → `docker compose start resource-server` + healthcheck recovery → Estimate → real duration) + ONE README reference line directly after Story 5.2's line 28 (3rd doc reference under "Architecture overview"; mirrors 5.1 + 5.2 placement convention exactly). ZERO production code / test / compose / Dockerfile / Justfile / keycloak / .env changes — failure-prevention checklist #1, #5, #6, #7, #8, #13, #15. Two execution modes documented: Mode A (operator with desktop browser, canonical) flips all 13 checkboxes to `[x]` and records verdict=PASS in Run Record; Mode B (programmatic agent, fallback for headless hosts) completes steps 1-5 bring-up + 3 curl-level HTTP probes (BFF serves `<app-root>` at GET /; /auth/login 3xx OIDC redirect; /api/me 401 unauthenticated) and leaves journey steps 7-13 as `[ ]` with explicit "PENDING — operator browser walk-through required" callout in Anomalies + verdict=PASS WITH ANOMALIES — user accepts the partial-smoke trade-off at code-review time before story moves to `done`. Math pinned verbatim: 600 pages × 60min/h ÷ 30pph = 1200min = 20h; ÷ 60pph = 600min = 10h (ceiling-div from `services/resource-server/src/resource_server/services/estimate_service.py:63` + U+2248 glyph from `services/resource-server/src/resource_server/services/duration.py` `_PREFIX`). Copy strings pinned byte-for-byte (failure-prevention #3): `Service unavailable — try again shortly` em-dash U+2014 (`estimate-cell.ts:27` `ESTIMATE_CELL_J6_COPY`), `Set your reading speed in Settings to enable estimates` with `Settings` as `routerLink="/settings"` (`estimate-cell.html:18-29` + `estimate-cell.ts:30` prefix export), `Saved` pulse (`settings-page.ts:50`), `Estimate` / `Re-estimate` / `Estimating…` button labels. Seeded users pinned (failure-prevention #2): `testuser` / `testpassword` (has reading-speed row after J4) + `freshuser` / `freshpassword` (no row — triggers J3 412 precondition path). Book status options `['to-read', 'reading', 'finished']` (`status-control.spec.ts:82`). Default-profile composition verified: `docker-compose.yml` includes `compose/infra.yml` (Keycloak + healthcheck on `:9000/health/ready`) + `compose/app.yml` (BFF + RS, both Python stdlib `:8000/health` probes); `services/bff/Dockerfile` `node:22-slim AS node-builder` → `/spa/dist/spa/browser` → `/app/static` (Story 1.14 closure of D46); test-reset endpoints inert under default profile (ENABLE_TEST_RESET=false in .env-default, no -f compose/app.e2e.yml overlay). D45 (BFF /health under KC_HOSTNAME=localhost) closed 2026-05-15 — `docker compose up --abort-on-container-exit` unblocked. 16-item failure-prevention checklist includes: no --abort-on-container-exit (#13 — would SIGTERM the stack the moment `docker compose stop resource-server` runs in J6 step 13), no e2e profile (#5 — D440 BFF_CLIENT_SECRET sharing inappropriate for prod-shaped smoke), `--build` flag required (#7 — SPA multi-stage rebuild on Dockerfile / spa/ changes), `docker compose down -v` before `up` (#8 — drops bff_data/rs_data named volumes for true fresh-state), no `[x]` if any step `[ ]` in Mode A (#9 — false attestation prevention), no rewrite of Stories 5.1 + 5.2 README lines (#11 — placement convention preservation). Load-bearing close gate: `git status --short` shows ONLY `?? docs/smoke-run.md` + ` M README.md` + ` M sprint-status.yaml` + ` M 5-4-final-docker-compose-up-smoke-default-profile.md` (AC5 anti-scope-creep). Story 5.3 runs in parallel — `5-4` does not depend on it; 5.3's full README rewrite later absorbs the 5.4 reference line in its dedicated "Prod-shaped workflow" section. 5-4 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.2 dev complete → review — pure documentation pass authored entirely in worktree `E5S2` (baseline `3e3612a`, synced from parent at dev-start with 5.1's outputs + sprint-status.yaml + deferred-work.md + README's line-26 anchor). ZERO source / test / config / production-deliverable changes — `git diff --stat` shows only `README.md` (+4) and `sprint-status.yaml` (+8/-4); `docs/security-review.md` (553 lines) is new in `?? docs/`. Document follows PRD §9 envelope: §1 token storage and transport (cites `auth/keycloak_cookie_session.py`, `models/entities/session.py`, `services/session_service.py`, `services/resource_server_client.py` + pin-tests `tests/auth/test_keycloak_cookie_session.py`, `tests/services/test_session_service.py`, `tests/services/test_resource_server_client.py`); §2 cookie attributes (HttpOnly + Secure=env + SameSite=Lax + Path=/ + 256-bit opaque value + name configurable; state-id cookie sidebar with 5-min `URLSafeTimedSerializer` ref; CSRF cookie non-HttpOnly intentional + double-submit rationale; cites `api/auth.py:158–166, 285–305, 307–314`); §3 CSRF posture (three-layer: method-exempt + double-submit `hmac.compare_digest` constant-time + Origin/Referer with userinfo-strip rejection; cites `auth/csrf.py:32, 73–99, 101–135, 159–173` + SPA `shared/http/csrf-interceptor.ts`); §4 JWT validation (PyJWT step-by-step: `kid` lookup, `algorithms=["RS256"]` pinning, `audience`, `issuer`, `exp`, `require=["iss","aud","exp","sub"]`; JWKS cache + `kid`-miss re-fetch as TTL surrogate; BFF id-token parallel with D2/D8 browser-URL caveat + nonce binding; cites `oidc_bearer.py:52,76,96,118,130` + `keycloak_cookie_session.py:196,204`); §5 scope enforcement (RS-side per architecture line 82; `require_scope` factory module-load fail-fast; per-endpoint table — GET `/v1/reading-speed` + POST `/v1/estimate` need `:read`, PUT `/v1/reading-speed` needs `:write`; sub-from-JWT-only rule; D440 BFF_CLIENT_SECRET sharing follow-up referenced; cites `oidc_bearer.py:130`, `reading_speed.py:32,42`, `estimate.py:42`); §6 standard SPA concerns (CSP value BYTE-FOR-BYTE from `security_headers.py:17–21`, directive-by-directive table, attachment-rule note + `style-src 'unsafe-inline'` Tailwind v4 carve-out; no `bypassSecurityTrust*` / `innerHTML` / `eval` grep result; dep hygiene version table for Python 3.14 + FastAPI + PyJWT ≥2.10 + Authlib + httpx + Angular 21 + Tailwind 4 + Vitest 4 + Playwright 1.49 + Keycloak 26). Accepted-Risks subsection: AR1 plaintext token columns (architecture `:1362–1368` + KEK/dedicated-store/JWE-cookie alternatives), AR2 plaintext PKCE `code_verifier` (D42 envelope), AR3 no idle timeout beyond refresh chain (architecture `:1369–1373` + explicit `last_activity_at` alternative). Threat-Model: 7 prose threats T1 XSS through T7 confused-deputy; T4 (direct RS access) intentionally accepted as OAuth contract. Known-Gaps subsection: 19 items consolidating D371, D372, D43+D64, D63, D67, D80, D82+D83, D84, D73+D74+D85, D71, D75, D79, D81, D25, D440, D441, D442, D470, plus no-CI scope decision — one over AC10's minimum-18. README updated with single new reference line directly after coverage-report.md line, mirroring Story 5.1's placement convention (31 → 33 lines). Inline path correction applied: epic AC line 1812's `services/bff/src/bff/db/models/session.py` is stale — actual canonical path is `services/bff/src/bff/models/entities/session.py`. Markdown lint sanity: 28 code fences = 14 balanced blocks; 9 H2 sections matching AC1 structure; all 31 relative `../*` links resolve. Zero new defers (Task 8.1: one Python 3.14 language-version observation on `csrf.py:103` `except ValueError, TypeError:` — parses + runs correctly in 3.14, behavior is correct, not a security defect, not in scope). All 14 failure-prevention checklist items observed. 5-2 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.1 second-pass code review at user request → still done — same three layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) re-ran on the patched diff and surfaced 4 real follow-on findings, all doc-only, all applied: P6 reconciled `invalid_input` double-listing in error-service.ts bullet (the P3 first-pass rewrite had accidentally claimed it was both uncovered and covered — dropped from the covered list; SPA relies on three cases not four); P7 re-applied `5-1: review → done` to sprint-status.yaml structured field (first attempt was clobbered by a parallel turn touching the same file when 5.2 was contexted); P8 propagated DN1's "≥90%" softening uniformly across AC5 heading, AC6 (heading + Given + Then), AC2 template, Task 1.2 path-taken, DoD #4/#5, and Completion Notes — first pass had only patched AC5 body, leaving the rest of the story self-contradictory at ">90%"; P9 AC1 line 65 SPA HTML path `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular's `@angular/build:unit-test` wraps Vitest — same fix P1 made to the report, just propagated to the AC spec wording). ~7 second-pass findings dismissed as noise. ZERO behavior change across BOTH passes — every patch on both passes landed in `docs/coverage-report.md` or this story file's AC / Task / DoD wording. All four NFR11 thresholds still substantially exceeded post-second-pass. 5-1 status: done (unchanged; the second pass is a doc-coherence cleanup, not a status transition). Earlier on 2026-05-18: story 5.1 code review pass → done — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel; Acceptance Auditor returned PASS on all 9 ACs + 12 failure-prevention items + 14 DoD items pre-patch. 6 doc-accuracy patches applied (1 was DN1 reclassified after user input — "soften the docs" rather than tighten the gate): DN1 softened `fail_under = 90` framing — updated AC5 to "≥90% with `precision = 0`" + Targets-table cites "effective floor ~89.5% rounded"; P1 SPA HTML path corrected `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular builder nests one directory deeper than raw Vitest); P2 estimate-cell.ts missing-line list reconciled (line 129 was dropped from the Notes section between two enumerations); P3 error-service.ts count-vs-range conflict resolved by listing uncovered `case` branches explicitly; P4 inlined E2E reproduce recipe rewritten as `bash -c 'set -e; trap "..." EXIT; ...'` so Ctrl-C / failure paths still tear down (matches Justfile semantics, prevents `container_name: playwright` collision on next invocation); P5 DAR typo `spec-app-app.js` → `app.spec.ts` (verified `spa/src/app/app.spec.ts` exists). ZERO source / test / config / production-deliverable changes — every patch landed in `docs/coverage-report.md` (5) or this story file's AC5 wording + DAR (2). 8 findings dismissed as noise (four-vs-five gap count, cross-metric estimate-cell "regression", HTML-index DAR attestation, E2E exit-code quote, types.ts exclude framing, BFF format nit, README placement, pytest-cov exit-code quote). No defers logged — D135 remains the highest deferred id. All four NFR11 thresholds still substantially exceeded post-patch (no behavior changed, only documentation accuracy). Epic 5 progress: 1/4 stories done [5.1]; 5.2 already contexted → ready-for-dev in parallel; 5.3 / 5.4 still backlog. 5-1 status: review → done. Earlier on 2026-05-18: story 5.2 contexted → ready-for-dev — file `5-2-security-review-document.md` written in worktree `E5S2` from PRD §9 six-topic envelope [token storage/transport, cookie attrs, CSRF, JWT validation, scope enforcement, SPA concerns] + architecture A1–A8 [lines 343–354] + architecture §Operational Details "Token storage at rest" [1362–1368] + "Idle / absolute session timeout" [1369–1373] + Story 5.1 doc-only precedent. 11 ACs + 8 tasks. Pure-documentation deliverable: ONE new file `docs/security-review.md` + ONE README reference line mirroring 5.1's placement at line 26. ZERO production code / test / infra changes (failure-prevention checklist items #1–#3, #8, #14). The document cites implementing code paths verbatim (BFF `auth/keycloak_cookie_session.py`, `auth/csrf.py`, `middleware/security_headers.py:17–21` for byte-for-byte CSP; RS `auth/oidc_bearer.py` `_validate_access_token` + `require_scope` factory; SPA `shared/http/csrf-interceptor.ts`) and pin-test files (BFF `tests/auth/test_csrf.py`, `tests/middleware/test_security_headers.py`, `tests/services/test_resource_server_client.py`; RS `tests/auth/test_oidc_bearer.py`, `tests/api/test_reading_speed.py`, `tests/api/test_estimate.py`). Accepted-risks subsection: 3 named items (token plaintext SQLite columns, PKCE verifier plaintext, no idle-session timeout beyond refresh chain) per architecture's accepted-risk envelope. Threat-model: 7 threats prose-only (XSS / CSRF / token replay / direct RS access / scope escalation / replay-after-logout / confused-deputy). Known-gaps subsection consolidates 18+ existing defers (D25 health amplification, D42 PKCE verifier plaintext, D43 + D64 PyJWT leeway=0, D63 WWW-Authenticate header, D67 empty-sub→412, D71 BFF change-me reject parity, D73 Bearer case-sensitivity, D74/D85 multi-bearer concat, D75 router-via-__all__ gate bypass, D79 non-AppException envelope, D80 refresh-rotation race, D81 BFF VALIDATION_ERROR drift, D83 httpx follow_redirects, D84 log hygiene, D369 CSP attach-trigger, D371 CSP hardening, D372 extra security headers, D440 BFF_CLIENT_SECRET shared with Playwright, D441 log-injection on sub, D442 session_not_found envelope, D470 zero-width whitespace input, no GitHub Actions CI). Path correction noted in story: epic AC line 1812's `db/models/session.py` is stale — actual canonical archetype path is `models/entities/session.py`. Load-bearing close gate: `git status` shows ONLY `docs/security-review.md` (new) + `README.md` (+1 line) + this story file + sprint-status.yaml + optionally `deferred-work.md` (expected: no new defers). 5-2 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.1 dev complete → review — pure attestation pass, ZERO source / test / config changes. All four NFR11 thresholds substantially exceeded at baseline `3e3612a`: BFF 97.26% line / 543 tests (lowest per-file `core/database.py` 84% vs ≥70% floor); RS 98.33% line / 324 tests (lowest per-file `auth/factory.py` 77%); SPA 94.71% statements / 91.16% branches / 95.74% functions / 94.52% lines / 152 tests across 19 files (lowest per-file lines `error-service.ts` 78.94% vs ≥50% floor); E2E 6 specs / 26 tests covering J1–J6 with live `e2e-up` (inlined Justfile recipe because audit host lacks `just` binary, same as 4.4 dev) exits 0 in 53.0s with all 26 green (J1×3 J2×8 J3×5 J4×5 J5×2 J6×3). Created `docs/coverage-report.md` (full snapshot + Reproduce block + Targets table + per-surface sections + Method + "Gaps closed: None"); README gained one reference line under "Architecture overview" (Story 5.3 owns full rewrite). NO `omit` / `coverage.exclude` additions — RS's 7-entry Story-3.1 omit list preserved verbatim. Four per-file gaps documented as informational follow-ups (`core/database.py` lifecycle paths, `auth/factory.py` Azure-AD branch, `error-service.ts` defensive envelope shapes, `estimate-cell.ts` signal-invariant guards) — NOT deferred work; all above per-file floors. SPA build green (255.77 kB initial / 71.97 kB transfer). No new deferred items logged. 5-1 status: in-progress → review. Earlier on 2026-05-18: story 5.1 contexted — file `5-1-coverage-audit-gap-fill.md` written from Epic 5 AC source [epics.md L1748-1798] + prior-story coverage snapshots [BFF 97.26% at b905307, SPA estimate-cell 88.23%/78.57% at story 4.3, SPA suite 152/19 at story 4.4, RS aggregate never previously measured, E2E = 26 tests in 6 files at story 4.4]; 9 ACs + 8 tasks; creates `docs/coverage-report.md` + 1-line README reference; conditionally adds gap-fill tests in source-mirroring locations (BFF `tests/<mirror>/`, RS `tests/<mirror>/`, SPA `<name>.spec.ts` colocated) ONLY if thresholds not met; epic-5 status auto-transitioned `backlog` → `in-progress` (first story in Epic 5); baseline_commit=3e3612a; load-bearing close gate is each surface's coverage command exiting 0 with aggregate at/above target (BFF/RS >90%, SPA ≥70% on statements/branches/functions/lines, per-file floors of 70%/50%) + live `just e2e-up` 26/26 green. 5-1 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 4.4 contexted — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from epic AC source + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy + Story 4.2 BFF contract + Story 4.1 format_duration table. 13 ACs + 6 tasks. Two new specs (j3-estimate.spec.ts, j6-rs-unavailable.spec.ts) + e2e/README.md addendum. NO source/compose/helper changes — the harness is complete after Story 3.6. Load-bearing close gate: live `just e2e-up` exits 0 with all 26 tests green (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3 = NFR11's ≥5 J1–J6 satisfied). 4-4 status: backlog → ready-for-dev.
# Earlier on 2026-05-18 (sprint-status as carried by epic-5 immediately before this merge): story 5.2 code review pass → done — three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel against the 5.2 merge diff. Acceptance Auditor returned PASS on AC1–AC9 + AC11 + 14/14 failure-prevention items + 12/13 DoD items; AC10 / DoD #7 returned FAIL on the load-bearing "defer IDs resolve to deferred-work.md" invariant — 7 of 19 Known-Gaps citations cited D-IDs that did not exist (D369, D371, D372, D440, D441, D442, D470; max actual at review time was D135). User selected the "backfill deferred-work.md" resolution path; 11 patches applied total (1 from decision-needed, 10 from direct findings). D1 backfilled deferred-work.md with 7 new entries under a "Deferred from: code review of 5-2-..." section preserving the same D-IDs (intentional non-sequential gap from D135 → D369). P1 corrected 4 architecture line citations systematically off by ~40 lines (`:76 → :36` Token isolation, `:80 → :40` JWKS / no hardcoded keys, `:82 → :42` Scope-enforced computation, `:83 → :43` BFF does not bypass). P2 corrected resource_server_client.py:178–224 (public wrappers) → :230–279 (_call_with_refresh) + :359–410 (_refresh_access_token). P3 relabeled auth.py:119–127 from "logout cookie clear" to its actual purpose (state-cookie clear on auth_state_invalid error response) and added real logout-clear ranges (:347–355 session + :356–364 CSRF). P4 standardized session-cookie set range to :298–305. P5 retargeted AR3 cross-reference "§9 threat 6 below" → "T6 in Threat Model Summary below". P6 corrected §3 urlsplit claim — urlsplit preserves userinfo in netloc; only .hostname strips it; csrf.py:169–170 actually checks parts.username or parts.password. P7 softened §4 JWKS-TTL equivalence — kid-miss re-fetch satisfies key-rotation half of architecture line 415 but NOT the 24h time-based TTL half. P8 added presence-only note to §4 required-claims gate (PyJWT's require enforces presence, not non-empty values; cross-references Known Gap #5 / D67). P9 in-section reference to D369 re-routed to deferred-work.md directly (D369 now exists in the registry; Known Gaps list count stays at 19, exceeding AC10 floor of 18). P10 updated Known Gap #6 (D80 refresh-token rotation race) fix narrative — prefer DB-level SELECT … FOR UPDATE for multi-replica correctness; acknowledged asyncio.Lock is single-replica only. 2 defers logged in the story (W1 D63 duplicated in deferred-work.md + W2 D84 source-line citation drift) — both pre-existing in the registry, not introduced by 5.2; not appended to deferred-work.md as new entries because they ARE deferred-work.md issues. ~12 findings dismissed as noise. Net code-review-pass diff: docs/security-review.md (citation tightening across ~12 sites), _bmad-output/implementation-artifacts/deferred-work.md (+7 new D-entries under Story 5.2 review section), this story file (Review Findings + Status flip + Change Log v1.1 entry), sprint-status.yaml (5-2 review → done + this narrative). No production source / tests / infra touched. Doc structure unchanged (still 9 H2 sections matching AC1; all relative links resolve; CSP byte-for-byte still verbatim from security_headers.py:17–21). Epic 5 progress after 5.2 done: 2/4 stories done [5.1, 5.2]; 5.3 (README polish + AI integration log) and 5.4 (smoke run) still backlog. 5-2 status: review → done. Earlier on 2026-05-18: story 5.2 dev complete → review — pure documentation pass authored entirely in worktree `E5S2` (baseline `3e3612a`, synced from parent at dev-start with 5.1's outputs + sprint-status.yaml + deferred-work.md + README's line-26 anchor). ZERO source / test / config / production-deliverable changes — `git diff --stat` shows only `README.md` (+4) and `sprint-status.yaml` (+8/-4); `docs/security-review.md` (553 lines) is new in `?? docs/`. Document follows PRD §9 envelope: §1 token storage and transport (cites `auth/keycloak_cookie_session.py`, `models/entities/session.py`, `services/session_service.py`, `services/resource_server_client.py` + pin-tests `tests/auth/test_keycloak_cookie_session.py`, `tests/services/test_session_service.py`, `tests/services/test_resource_server_client.py`); §2 cookie attributes (HttpOnly + Secure=env + SameSite=Lax + Path=/ + 256-bit opaque value + name configurable; state-id cookie sidebar with 5-min `URLSafeTimedSerializer` ref; CSRF cookie non-HttpOnly intentional + double-submit rationale; cites `api/auth.py:158–166, 285–305, 307–314`); §3 CSRF posture (three-layer: method-exempt + double-submit `hmac.compare_digest` constant-time + Origin/Referer with userinfo-strip rejection; cites `auth/csrf.py:32, 73–99, 101–135, 159–173` + SPA `shared/http/csrf-interceptor.ts`); §4 JWT validation (PyJWT step-by-step: `kid` lookup, `algorithms=["RS256"]` pinning, `audience`, `issuer`, `exp`, `require=["iss","aud","exp","sub"]`; JWKS cache + `kid`-miss re-fetch as TTL surrogate; BFF id-token parallel with D2/D8 browser-URL caveat + nonce binding; cites `oidc_bearer.py:52,76,96,118,130` + `keycloak_cookie_session.py:196,204`); §5 scope enforcement (RS-side per architecture line 82; `require_scope` factory module-load fail-fast; per-endpoint table — GET `/v1/reading-speed` + POST `/v1/estimate` need `:read`, PUT `/v1/reading-speed` needs `:write`; sub-from-JWT-only rule; D440 BFF_CLIENT_SECRET sharing follow-up referenced; cites `oidc_bearer.py:130`, `reading_speed.py:32,42`, `estimate.py:42`); §6 standard SPA concerns (CSP value BYTE-FOR-BYTE from `security_headers.py:17–21`, directive-by-directive table, attachment-rule note + `style-src 'unsafe-inline'` Tailwind v4 carve-out; no `bypassSecurityTrust*` / `innerHTML` / `eval` grep result; dep hygiene version table for Python 3.14 + FastAPI + PyJWT ≥2.10 + Authlib + httpx + Angular 21 + Tailwind 4 + Vitest 4 + Playwright 1.49 + Keycloak 26). Accepted-Risks subsection: AR1 plaintext token columns (architecture `:1362–1368` + KEK/dedicated-store/JWE-cookie alternatives), AR2 plaintext PKCE `code_verifier` (D42 envelope), AR3 no idle timeout beyond refresh chain (architecture `:1369–1373` + explicit `last_activity_at` alternative). Threat-Model: 7 prose threats T1 XSS through T7 confused-deputy; T4 (direct RS access) intentionally accepted as OAuth contract. Known-Gaps subsection: 19 items consolidating D371, D372, D43+D64, D63, D67, D80, D82+D83, D84, D73+D74+D85, D71, D75, D79, D81, D25, D440, D441, D442, D470, plus no-CI scope decision — one over AC10's minimum-18. README updated with single new reference line directly after coverage-report.md line, mirroring Story 5.1's placement convention (31 → 33 lines). Inline path correction applied: epic AC line 1812's `services/bff/src/bff/db/models/session.py` is stale — actual canonical path is `services/bff/src/bff/models/entities/session.py`. Markdown lint sanity: 28 code fences = 14 balanced blocks; 9 H2 sections matching AC1 structure; all 31 relative `../*` links resolve. Zero new defers (Task 8.1: one Python 3.14 language-version observation on `csrf.py:103` `except ValueError, TypeError:` — parses + runs correctly in 3.14, behavior is correct, not a security defect, not in scope). All 14 failure-prevention checklist items observed. 5-2 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.1 second-pass code review at user request → still done — same three layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) re-ran on the patched diff and surfaced 4 real follow-on findings, all doc-only, all applied: P6 reconciled `invalid_input` double-listing in error-service.ts bullet (the P3 first-pass rewrite had accidentally claimed it was both uncovered and covered — dropped from the covered list; SPA relies on three cases not four); P7 re-applied `5-1: review → done` to sprint-status.yaml structured field (first attempt was clobbered by a parallel turn touching the same file when 5.2 was contexted); P8 propagated DN1's "≥90%" softening uniformly across AC5 heading, AC6 (heading + Given + Then), AC2 template, Task 1.2 path-taken, DoD #4/#5, and Completion Notes — first pass had only patched AC5 body, leaving the rest of the story self-contradictory at ">90%"; P9 AC1 line 65 SPA HTML path `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular's `@angular/build:unit-test` wraps Vitest — same fix P1 made to the report, just propagated to the AC spec wording). ~7 second-pass findings dismissed as noise. ZERO behavior change across BOTH passes — every patch on both passes landed in `docs/coverage-report.md` or this story file's AC / Task / DoD wording. All four NFR11 thresholds still substantially exceeded post-second-pass. 5-1 status: done (unchanged; the second pass is a doc-coherence cleanup, not a status transition). Earlier on 2026-05-18: story 5.1 code review pass → done — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel; Acceptance Auditor returned PASS on all 9 ACs + 12 failure-prevention items + 14 DoD items pre-patch. 6 doc-accuracy patches applied (1 was DN1 reclassified after user input — "soften the docs" rather than tighten the gate): DN1 softened `fail_under = 90` framing — updated AC5 to "≥90% with `precision = 0`" + Targets-table cites "effective floor ~89.5% rounded"; P1 SPA HTML path corrected `spa/coverage/index.html` → `spa/coverage/spa/index.html` (Angular builder nests one directory deeper than raw Vitest); P2 estimate-cell.ts missing-line list reconciled (line 129 was dropped from the Notes section between two enumerations); P3 error-service.ts count-vs-range conflict resolved by listing uncovered `case` branches explicitly; P4 inlined E2E reproduce recipe rewritten as `bash -c 'set -e; trap "..." EXIT; ...'` so Ctrl-C / failure paths still tear down (matches Justfile semantics, prevents `container_name: playwright` collision on next invocation); P5 DAR typo `spec-app-app.js` → `app.spec.ts` (verified `spa/src/app/app.spec.ts` exists). ZERO source / test / config / production-deliverable changes — every patch landed in `docs/coverage-report.md` (5) or this story file's AC5 wording + DAR (2). 8 findings dismissed as noise (four-vs-five gap count, cross-metric estimate-cell "regression", HTML-index DAR attestation, E2E exit-code quote, types.ts exclude framing, BFF format nit, README placement, pytest-cov exit-code quote). No defers logged — D135 remains the highest deferred id. All four NFR11 thresholds still substantially exceeded post-patch (no behavior changed, only documentation accuracy). Epic 5 progress: 1/4 stories done [5.1]; 5.2 already contexted → ready-for-dev in parallel; 5.3 / 5.4 still backlog. 5-1 status: review → done. Earlier on 2026-05-18: story 5.2 contexted → ready-for-dev — file `5-2-security-review-document.md` written in worktree `E5S2` from PRD §9 six-topic envelope [token storage/transport, cookie attrs, CSRF, JWT validation, scope enforcement, SPA concerns] + architecture A1–A8 [lines 343–354] + architecture §Operational Details "Token storage at rest" [1362–1368] + "Idle / absolute session timeout" [1369–1373] + Story 5.1 doc-only precedent. 11 ACs + 8 tasks. Pure-documentation deliverable: ONE new file `docs/security-review.md` + ONE README reference line mirroring 5.1's placement at line 26. ZERO production code / test / infra changes (failure-prevention checklist items #1–#3, #8, #14). The document cites implementing code paths verbatim (BFF `auth/keycloak_cookie_session.py`, `auth/csrf.py`, `middleware/security_headers.py:17–21` for byte-for-byte CSP; RS `auth/oidc_bearer.py` `_validate_access_token` + `require_scope` factory; SPA `shared/http/csrf-interceptor.ts`) and pin-test files (BFF `tests/auth/test_csrf.py`, `tests/middleware/test_security_headers.py`, `tests/services/test_resource_server_client.py`; RS `tests/auth/test_oidc_bearer.py`, `tests/api/test_reading_speed.py`, `tests/api/test_estimate.py`). Accepted-risks subsection: 3 named items (token plaintext SQLite columns, PKCE verifier plaintext, no idle-session timeout beyond refresh chain) per architecture's accepted-risk envelope. Threat-model: 7 threats prose-only (XSS / CSRF / token replay / direct RS access / scope escalation / replay-after-logout / confused-deputy). Known-gaps subsection consolidates 18+ existing defers (D25 health amplification, D42 PKCE verifier plaintext, D43 + D64 PyJWT leeway=0, D63 WWW-Authenticate header, D67 empty-sub→412, D71 BFF change-me reject parity, D73 Bearer case-sensitivity, D74/D85 multi-bearer concat, D75 router-via-__all__ gate bypass, D79 non-AppException envelope, D80 refresh-rotation race, D81 BFF VALIDATION_ERROR drift, D83 httpx follow_redirects, D84 log hygiene, D369 CSP attach-trigger, D371 CSP hardening, D372 extra security headers, D440 BFF_CLIENT_SECRET shared with Playwright, D441 log-injection on sub, D442 session_not_found envelope, D470 zero-width whitespace input, no GitHub Actions CI). Path correction noted in story: epic AC line 1812's `db/models/session.py` is stale — actual canonical archetype path is `models/entities/session.py`. Load-bearing close gate: `git status` shows ONLY `docs/security-review.md` (new) + `README.md` (+1 line) + this story file + sprint-status.yaml + optionally `deferred-work.md` (expected: no new defers). 5-2 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 5.1 dev complete → review — pure attestation pass, ZERO source / test / config changes. All four NFR11 thresholds substantially exceeded at baseline `3e3612a`: BFF 97.26% line / 543 tests (lowest per-file `core/database.py` 84% vs ≥70% floor); RS 98.33% line / 324 tests (lowest per-file `auth/factory.py` 77%); SPA 94.71% statements / 91.16% branches / 95.74% functions / 94.52% lines / 152 tests across 19 files (lowest per-file lines `error-service.ts` 78.94% vs ≥50% floor); E2E 6 specs / 26 tests covering J1–J6 with live `e2e-up` (inlined Justfile recipe because audit host lacks `just` binary, same as 4.4 dev) exits 0 in 53.0s with all 26 green (J1×3 J2×8 J3×5 J4×5 J5×2 J6×3). Created `docs/coverage-report.md` (full snapshot + Reproduce block + Targets table + per-surface sections + Method + "Gaps closed: None"); README gained one reference line under "Architecture overview" (Story 5.3 owns full rewrite). NO `omit` / `coverage.exclude` additions — RS's 7-entry Story-3.1 omit list preserved verbatim. Four per-file gaps documented as informational follow-ups (`core/database.py` lifecycle paths, `auth/factory.py` Azure-AD branch, `error-service.ts` defensive envelope shapes, `estimate-cell.ts` signal-invariant guards) — NOT deferred work; all above per-file floors. SPA build green (255.77 kB initial / 71.97 kB transfer). No new deferred items logged. 5-1 status: in-progress → review. Earlier on 2026-05-18: story 5.1 contexted — file `5-1-coverage-audit-gap-fill.md` written from Epic 5 AC source [epics.md L1748-1798] + prior-story coverage snapshots [BFF 97.26% at b905307, SPA estimate-cell 88.23%/78.57% at story 4.3, SPA suite 152/19 at story 4.4, RS aggregate never previously measured, E2E = 26 tests in 6 files at story 4.4]; 9 ACs + 8 tasks; creates `docs/coverage-report.md` + 1-line README reference; conditionally adds gap-fill tests in source-mirroring locations (BFF `tests/<mirror>/`, RS `tests/<mirror>/`, SPA `<name>.spec.ts` colocated) ONLY if thresholds not met; epic-5 status auto-transitioned `backlog` → `in-progress` (first story in Epic 5); baseline_commit=3e3612a; load-bearing close gate is each surface's coverage command exiting 0 with aggregate at/above target (BFF/RS >90%, SPA ≥70% on statements/branches/functions/lines, per-file floors of 70%/50%) + live `just e2e-up` 26/26 green. 5-1 status: backlog → ready-for-dev. Earlier on 2026-05-18: story 4.4 contexted — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from epic AC source + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy + Story 4.2 BFF contract + Story 4.1 format_duration table. 13 ACs + 6 tasks. Two new specs (j3-estimate.spec.ts, j6-rs-unavailable.spec.ts) + e2e/README.md addendum. NO source/compose/helper changes — the harness is complete after Story 3.6. Load-bearing close gate: live `just e2e-up` exits 0 with all 26 tests green (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3 = NFR11's ≥5 J1–J6 satisfied). 4-4 status: backlog → ready-for-dev.
# Earlier on 2026-05-18: story 4.3 merged into main via no-ff merge of branch worktree-agent-a32e6a27249f65923 — 4.3 = done; both Epic 4 backend [4.1, 4.2] and SPA [4.3] halves now landed. Only 4.4 [E2E specs J3 + J6] remains in Epic 4. See YAML last_updated below for the full per-day chain.)
# project: BMAD_books
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-14
last_updated: 2026-05-19 (story 6.3 dev complete → review — BFF cleanup landed against working-tree state with Story 6.2 already in `review` (compose stack live + healthy at session start: bff/keycloak/resource-server/spa all healthy on the post-6.2 topology). 4 MODIFIED files + 1 DELETED file in the BFF tree, 2 MODIFIED files in compose/top-level, 1 MODIFIED file in `_bmad-output/implementation-artifacts/` (Story 1.14 frontmatter), 2 deferred-work entries logged (D159 D16-reopen + D160 security_headers.py docstring follow-up). **BFF surface deletions (Task 1–4)**: `services/bff/Dockerfile` lost Stage 0 (14 lines — `FROM node:22-slim AS node-builder` through `RUN npm run build`) + the `COPY --from=node-builder` line in Stage 2 (3 lines including 2 leading comments) — file shrank from 78 lines to 60; `services/bff/src/bff/main.py` lost 5 imports (`Path`, `Request` from fastapi, `JSONResponse`, `FileResponse`+`Response`, `StaticFiles`) + `_SPA_DIR` constant + `_register_spa` 60-line helper + conditional mount block + leading comment blocks — file shrank from 149 lines to 71 (52% reduction); `services/bff/tests/api/test_static.py` deleted via `git rm` (4 tests removed: AC7a SPA shell on /login, AC7b /assets/main.css with text/css, AC7c JSON 404 envelope on unknown path, path-traversal defence against /../../../etc/passwd). **Dockerfile Stage 1 revert (Task 2)**: bind-mount sources reverted from `services/bff/uv.lock`+`services/bff/pyproject.toml`+`services/bff/.python-version` to bare names; `COPY services/bff/ /app/` reverted to `COPY . /app/`; Stage 1 comment block reverted from "Build context is now the repo root" to "Build context is the per-service directory ... Story 6.3 reverted the Story-1.14 repo-root context — the front-end build stage that required reaching a sibling directory is gone" (intentionally avoiding the literal substring `spa/` in the comment to keep AC1's narrow grep clean — verified by `grep -nEi 'node|spa|static|node-builder|node:22-slim' services/bff/Dockerfile` returning zero matches). **Compose / top-level (Tasks 5–6)**: `compose/app.yml` BFF `build:` block reverted `context: ..` → `context: ../services/bff` + deleted `dockerfile: services/bff/Dockerfile` key (only RS retains a `dockerfile:` key — the RS stays on root context per Story 3.1 D4 closure, explicitly out-of-scope for 6.3); BFF `build:` comment block rewritten from Story-1.14 D4-closure rationale to Story-6.3 per-service-restore rationale; file-header lines 1–14 rewritten — Story 1.14 SPA-in-BFF claim replaced with "Story 6.2 splits the SPA out of the BFF image into its own `spa` SSR-edge service ... Story 6.3 finalizes the split by deleting the BFF's Story-1.14 static-serve code path and reverting the BFF build context to per-service — the BFF is now API-only"; BFF-port-removal comment rewritten from future-tense "Story 6.3 will delete" to past-tense "Story 6.3 deleted"; `grep -nE 'Story 1\.14' compose/app.yml` exits 1 (zero matches in the file header). `docker-compose.yml` lines 4–13 rewritten — top-level `compose/app.yml` description line now reads `BFF (API-only), Resource Server, SPA SSR edge` (Stories 1.3, 3.1, 6.2); trailing "Story 1.14's static-serve code path is still in the BFF image but unreachable; Story 6.3 removes it" sentence dropped; replaced with "Story 6.3 finalized the split by deleting the BFF's Story-1.14 static-serve code path and reverting the BFF build context to per-service — the BFF is now API-only". **Story 1.14 supersession (Task 7)**: `_bmad-output/implementation-artifacts/1-14-bff-multi-stage-build-serves-spa-bundle.md` frontmatter received 3 new YAML keys after `created: 2026-05-15`: `superseded_by: 6-3-bff-cleanup-supersede-story-1-14` + `superseded_on: 2026-05-19` + `supersession_rationale: <one-line description>`. Body (29710 chars) byte-identical; diff is +3 lines, frontmatter only — codified history preserved per AC8. **AC verification (Task 8) — all close-gates green**: AC1 `docker build -t bff-cleanup-test services/bff/` exits 0 with **302 MB image** (target band 250–350 MB ✓), `docker history` shows **zero** `node|npm` history entries ✓, `/app` contents are exactly `.venv` + `alembic` + `alembic.ini` + `entrypoint.sh` (no `static/`, no `node_modules/` ✓). AC2 `docker compose exec spa node -e "fetch('http://bff:8000/')"` returns `status: 404, body: {"detail":"Not Found"}` — FastAPI default 404 envelope (NOT the project envelope — intentional D16 reopen logged as D159 ✓). AC3 byte-identical BFF API surface: `/health` → 200 `{"status":"ok"}`, `/api/me` anonymous → 401 `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}` (byte-identical to Story 6.2 AC4 + Story 5.4 Run Record 2026-05-18); through SPA proxy at `:4000`: `/api/me` → same envelope (proves SPA→BFF proxy intact post-cleanup), `/auth/login` → 302 with `redirect_uri=http://localhost:4000/auth/callback` URL-decoded (confirms Story 6.2's `BFF_BASE_URL` flip still in effect), `/v1/books` anonymous → 401 same envelope. OAuth happy-path browser interaction is Mode-B partial per Story 5.4 precedent — wire-level evidence is the load-bearing close gate. AC4 `cd services/bff && uv run pytest` → **539 passed, 109 warnings in 10.06s** — pre-6.3 BFF baseline was 543 (Story 5.1 attestation, unchanged through Story 5.4); post-6.3 count = 543 − 4 (the four `test_static.py` tests) = 539 exact match ✓. Coverage: `bff/main.py` at **100% line coverage** post-edit, project total 98%. Note: story-create doc reference of "347→343" was based on Story 1.14's create-time pytest count; actual HEAD `ed34ac7` baseline was 543, so post-6.3 is 539 — delta-of-4 holds, absolute numbers shifted by Story 5.1's gap-fill tests. AC5 narrow `git grep -nE 'StaticFiles|_SPA_DIR|_register_spa|_spa_or_404|_json_404|_spa_fallback|FileResponse|/app/static|node-builder|node:22-slim' services/bff/` exits 1 (zero matches ✓); wider safety-net `git grep -nE 'SPA|spa|static' services/bff/` returned 26 lines audited — (a) substring false-positives (`lifespan`, `whitespace`, `@staticmethod`, `transparently`, `dispatch`, OTEL `span_id`/`Span` constants), (b) accurate prose mentions of the still-existing browser SPA (api/auth.py:6,306,344 CSRF cookie + login navigation; books.py:138 + reading_speed.py:113 SPA-redirect-to-/login; resource_server_client.py:111,271,323,340 SPA-redirect-flow; core/errors.py:36 SPA-renders-UX-DR12; tests/api/test_test_reset.py:456 CSP-is-SPA's-concern test comment), (c) ONE genuinely stale docstring in `services/bff/src/bff/middleware/security_headers.py:1,3,31` — Story 6.4 pickup, logged as D160. AC6 `compose/app.yml` BFF `context: ../services/bff` ✓, no `dockerfile:` key on BFF (only RS retains one — RS is out-of-scope) ✓, file-header zero "Story 1.14" matches ✓, `docker compose config --quiet` exits 0 ✓. AC7 `docker-compose.yml` header rewritten per target shape ✓. AC8 Story 1.14 frontmatter has all 6 expected YAML keys (status, story_key, created, superseded_by, superseded_on, supersession_rationale) ✓, body byte-identical (29710 chars) ✓. AC9 scope-leak audit: `git status` shows only files from the 6.3 allowlist (`services/bff/Dockerfile`, `services/bff/src/bff/main.py`, `services/bff/tests/api/test_static.py` deleted, `compose/app.yml`, `docker-compose.yml`, Story 1.14 + 6.3 + sprint-status + deferred-work artefacts); pre-existing 6.1 + 6.2 working-tree entries (`.env.example`, `compose/infra.yml`, `keycloak/realm-bmad-books.json`, `spa/Dockerfile`, all SSR interceptors, etc.) are NOT 6.3's responsibility — confirmed against AC9 blocklist. **Live in-compose verification**: rebuilt BFF service via `docker compose up -d --build bff` while 6.2's compose stack was already running; BFF container recreated and healthy in 25 s post-build; all four services (bff/keycloak/resource-server/spa) green. Two deferred items logged: D159 D16 partial-closure reopen (intentional regression — BFF emits FastAPI default 404 instead of project envelope on unknown paths; rationale in story Dev Notes; future fix is a Starlette ExceptionHandler not a catch-all route); D160 `security_headers.py` docstring is factually stale post-6.3 (Story 6.4 pickup per CSP-source-move). Three failure-prevention notes from the story explicitly verified: (1) Stage 1 bind-mount path revert is load-bearing — image build would fail if `services/bff/` prefix lingered, verified by 302 MB clean build; (2) NO replacement catch-all in main.py — verified by AC2 returning FastAPI default 404 not project envelope; (3) CSP middleware stays attached but never fires (no HTML responses leave BFF) — verified by AC3 BFF surface byte-identical + D160 logged. Zero scope leaks. Epic 6 progress: 1/4 done [6.1], 2/4 review [6.2, 6.3], 6.4 still backlog. 6-3 status: in-progress → review. Earlier on 2026-05-19: story 6.2 code review pass → done — `bmad-code-review` ran Blind Hunter + Edge Case Hunter + Acceptance Auditor in parallel against the 7-file 6.2 diff (~351 lines) vs baseline `ed34ac7`. Acceptance Auditor: 15 PASS / 0 PARTIAL / 0 FAIL / 0 NEW — all 9 ACs verified by either live probe or Dev Agent Record evidence, all 10 Tasks/45 subtasks evidenced in diff, AC9 allowlist obeyed with zero blocklist entries in the diff scope. Blind Hunter surfaced ~22 issues; Edge Case Hunter surfaced ~38 paths. After dedup + scope-filtering (most edge-case findings target `spa/src/server.ts` which is Story 6.1 territory — and many were already addressed in Story 6.1's review pass via P2/P5/P7 patches), 4 patches surfaced + 5 defers + ~50 dismissed as noise. **4 patches applied:** P1 SPA healthcheck `.catch` error logging — both `spa/Dockerfile:69` and `compose/app.yml` spa healthcheck CMD now `.catch(e => { console.error('healthcheck:', e && e.message || e); process.exit(1); })` so `docker inspect <container> --format '{{json .State.Health.Log}}'` surfaces the actual error message instead of opaque "unhealthy"; P2 `init: true` on spa service in `compose/app.yml` — Docker's tini-style PID-1 wrapper so `docker stop` / `docker compose down` forwards SIGTERM to Node and reaps zombies, fixing the 10s SIGKILL-grace footgun (Node has no default SIGTERM handler when PID 1); P3 `.env*` patterns added to `spa/.dockerignore` — defense-in-depth against secret leakage if a developer creates `spa/.env.local`; P4 `**/*.spec.ts` added to `spa/.dockerignore` — Vitest specs aren't bundled into the SSR output (Angular's `@angular/build:application` only includes files reachable from entry points), context-size optimization. **5 defers logged D154-D158 in `deferred-work.md`:** D154 build/deps stages run `npm ci`/`npm run build` as root — cross-cutting (BFF + RS Dockerfiles have same pattern), belongs to a build-hardening story; D155 `SPA_PUBLIC_ORIGIN`/`BFF_BASE_URL`/realm JSON URLs each read `${SPA_HOST_PORT:-4000}` independently with no compose-level anchor — cosmetic; D156 `container_name: spa` blocks parallel `docker compose -p <alt> up` — cross-cutting convention across all four services; D157 no `mem_limit`/`cpus` resource caps on any compose service — capacity-planning cross-cutting; D158 `restart: unless-stopped` masks crashloops in dev (5min grace via retries=30×interval=10s) — convention across services, dev-only overlay would be the right fix. **~50 dismissed as noise**, breakdown: (a) ~80% targeted `spa/src/server.ts` and other `spa/src/**` code that belongs to Story 6.1's review surface — Story 6.1's code-review pass already applied P2 (proxy hardening + 502 handler), P5 (trailing-slash normalization), P7 (Number(PORT) + server.on('error')); (b) speculative concerns about features that don't exist (HTTPS BFF, locales in SSR rendering, native modules in the bundle); (c) project-scope concerns (LAN/CI access, IPv6/IPv4 resolution) excluded by the local-only educational demo posture per PRD §6 + CLAUDE.md memory `BMAD_books scope`; (d) intentional behavior changes that AC2/AC5 explicitly verify (BFF :8000 host port removed); (e) misreadings of compose semantics (compose `${VAR:-default}` correctly handles empty AND unset, so `SPA_HOST_PORT=` empty → defaults to 4000); (f) forward-looking comment objections — codebase convention is to cross-reference future stories where it aids navigation. Live post-patch verification: `docker compose build spa` rebuilt clean with new image carrying the updated healthcheck CMD-SHELL (verified via `docker inspect bmad_books-spa:latest --format '{{json .Config.Healthcheck.Test}}'`); `docker compose up -d --no-deps --force-recreate spa` flipped the running container in-place with `init: true` confirmed via `docker inspect spa --format '{{.HostConfig.Init}}'` → `true`; AC3 `curl -L :4000/` returns SSR HTML with `<app-root` + `ngh=` + `ng-server-context="ssr"` markers; AC4 `curl :4000/api/me` returns byte-for-byte 401 anon envelope; AC8 `curl :4000/_health` returns `{"ok":true}`; image size unchanged at 350 MB (P4 spec exclusion shaves ~KB; `node:22-slim` base dominates). Note: the AC9 allowlist diff scope used by reviewers was the snapshot at code-review start; Story 6.3 dev work landed in parallel during the review session (touching `compose/app.yml` BFF build context revert + `docker-compose.yml` header refresh) — reviewers correctly did NOT include 6.3's changes in 6.2's review surface, and 6.3 carries its own review pass. Epic 6 progress: 2/4 stories done [6.1, 6.2]; 6.3 in review; 6.4 backlog. 6-2 status: review → done. Earlier on 2026-05-19: story 6.2 dev complete → review — 2 NEW files (`spa/Dockerfile` 3-stage Node multi-stage `deps`→`build`→`runtime` with non-root `app` user, `EXPOSE 4000`, Node-`fetch` HEALTHCHECK against `/_health`, `ENTRYPOINT ["node", "dist/spa/server/server.mjs"]`, image size 350 MB / `node:22-slim` base + 2.5 MB `dist/spa/` payload — runtime stage carries ONLY `dist/spa/`, no `node_modules` per Angular `@angular/build:application` outputMode=server bundling verified empirically; `spa/.dockerignore` first per-service `.dockerignore` excluding `node_modules`/`dist`/`coverage`/`.angular`/IDE cruft/VCS for ~5 MB build context) + 5 MODIFIED files (`compose/app.yml` adds `spa:` service block between `resource-server:` and `playwright:` with `build: { context: ../spa, dockerfile: Dockerfile }` + `ports: ["${SPA_HOST_PORT:-4000}:4000"]` + `environment: { NODE_ENV, PORT, BFF_INTERNAL_URL: http://bff:8000, SPA_PUBLIC_ORIGIN: http://localhost:${SPA_HOST_PORT:-4000} }` + `depends_on: { bff: service_healthy }` + Node-fetch healthcheck, drops BFF `ports: ["8000:8000"]` block + flips `BFF_BASE_URL: http://localhost:8000` → `http://localhost:${SPA_HOST_PORT:-4000}`, file-header comment refreshed; `compose/infra.yml` adds `SPA_HOST_PORT: ${SPA_HOST_PORT:-4000}` env pass-through on `keycloak` for realm-import substitution; `keycloak/realm-bmad-books.json` three URL substitutions `http://localhost:8000` → `http://localhost:${SPA_HOST_PORT:4000}` at line 80 `post.logout.redirect.uris` + line 84 `redirectUris[0]` + line 87 `webOrigins[0]` — Keycloak/Quarkus MicroProfile bare-colon `:` syntax confirmed working empirically; `.env.example` documents optional `#SPA_HOST_PORT=4000` block after test-reset; `docker-compose.yml` header refreshed). Live AC verification on `docker compose down -v && build && up -d --wait`: all 4 services healthy within the 120s window (`spa` shows `0.0.0.0:4000->4000/tcp`, `bff` shows only `8000/tcp` — no host port). AC3 `curl -L http://localhost:4000/` returns 200 at `/login?return_to=%2Fbooks` with `<app-root` + `ngh=` + `ng-server-context="ssr"` + `<script id="ng-state">` markers; AC4 `curl :4000/api/me` returns byte-for-byte BFF 401 envelope `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}`; AC5 `curl :8000/` returns curl exit 7 `Couldn't connect to server` AND BFF still reachable on compose network via `docker compose exec spa node -e "fetch('http://bff:8000/health')"` returning `{"status":"ok"}`; AC6 full OAuth happy path completed via scripted curl (not browser) at `:4000` — `/auth/login` 302 to Keycloak with decoded `redirect_uri=http://localhost:4000/auth/callback`, Keycloak form POST `testuser/testpassword` 302s to `http://localhost:4000/auth/callback?state=...&code=...`, `/auth/callback` through SPA edge returns 302 `Location: /` with three Set-Cookie (cleared `bff_auth_state=""` Max-Age=0, new `bff_session=...` HttpOnly, new `csrf_token=...`), authenticated `/api/me` returns `{"sub":"e1507290-...","preferred_username":"testuser"}`, `GET /` redirects to `/books` SSR-rendered, logout POST with `Origin: http://localhost:4000` + `X-CSRF-Token` returns 204 clearing both cookies (`Max-Age=0`), post-logout `/api/me` returns 401 anon envelope; AC7 `SPA_HOST_PORT=4100 down -v && up` re-verified end-to-end with admin API confirming `redirectUris=['http://localhost:4100/auth/callback']` + `webOrigins=['http://localhost:4100']` + `post.logout.redirect.uris='http://localhost:4100/*'` — all 5 substitution sites flowed; AC8 SPA `/_health` Node-fetch healthcheck verified, BFF healthcheck unchanged; AC9 scope-leak audit clean. One debug-log discovery (NO scope creep, NO code change): first `up -d --wait` after editing `keycloak/realm-bmad-books.json` showed Keycloak rejecting `redirect_uri=http://localhost:4000/auth/callback` with `LOGIN_ERROR / invalid_redirect_uri` because the Keycloak image **bakes** the realm JSON at image-build time (`keycloak/Dockerfile:25 COPY realm-bmad-books.json /opt/keycloak/data/import/`), so the host edit didn't take effect until `docker compose build keycloak`; resolved by rebuilding the image. **Failure-prevention follow-up surfaced for Story 6.4 docs sweep:** realm-JSON edit gotcha has TWO parts, not one — (1) Keycloak only re-imports on empty data volume → `down -v` (already documented in 6.2 AC2 wording + `.env.example`); AND (2) the realm JSON is baked at image build → `docker compose build keycloak` must run after host edits (NOT documented in 6.2 deliverables; the smoke-run docs Run Records owned by 5.4 / 6.4 are the right place for this operator note). 1 anomaly logged in Completion Notes: `_bmad-output/implementation-artifacts/6-3-bff-cleanup-supersede-story-1-14.md` appeared untracked mid-session (mtime 14:07) — NOT created by this 6.2 dev workflow; appeared from a parallel context-prep run. Surfaced per AC9 anomalies guidance; not patched in 6.2. Intentional regressions at 6.2 close (all in scope of later Epic-6 stories): SPA HTML responses carry NO CSP header (Story 6.4 A8 amendment moves CSP source BFF→SPA); `just e2e-up` indeterminate (Story 6.4 sweeps e2e config + `--host-resolver-rules`); host-side `cd spa && npm start` already broken by 6.1's `proxy.conf.json` deletion; Story 1.14 SPA-in-BFF static-serve code path stays LIVE in BFF image but unreachable from browser (Story 6.3 deletes). Epic 6 progress: 2/4 stories done [6.1, 6.2 pending review]; 6.3 ready-for-dev (already contexted in parallel during this session — see entry below); 6.4 still backlog. 6-2 status: in-progress → review. Earlier on 2026-05-19: story 6.3 contexted → ready-for-dev — file `6-3-bff-cleanup-supersede-story-1-14.md` written from Sprint Change Proposal 2026-05-19 §4 Story 6.3 (AC1-AC5 source) + §2.4 Story 1.14 supersession enumeration + §2.5 Compose / Docker / Keycloak impact + §6 success-criteria `git grep` audit + Story 1.14 actual landed surface at `services/bff/Dockerfile` (Stage 0 node-builder lines 1-14 + Stage 2 COPY-from line 56), `services/bff/src/bff/main.py` (`_SPA_DIR` constant + `_register_spa` 60-line helper with assets mount + catch-all `_spa_or_404` carrying path-traversal guard + Accept-driven 404 envelope branch + conditional mount block lines 28-149), `services/bff/tests/api/test_static.py` (4 tests — AC7a/b/c + path-traversal probe), `compose/app.yml:34-35` (`context: ..` + `dockerfile: services/bff/Dockerfile`), `docker-compose.yml:6` (SPA-in-BFF claim) + Story 6.2 dev outputs that 6.3 inherits (BFF lost host port, `BFF_BASE_URL: http://localhost:${SPA_HOST_PORT:-4000}`, Keycloak realm registers `:4000` redirect, `compose/app.yml` carries new `spa:` service block) + post-6.1 working-tree diff still uncommitted on `feat/containerization`. 9 ACs + 9 tasks. **Scope: BFF cleanup + compose context revert (full supersession of Story 1.14).** Deliverables: 4 deletions (Dockerfile Stage 0 node-builder + COPY-from-node-builder line; `main.py` SPA-mount machinery — 5 imports `Path`/`Request`/`JSONResponse`/`FileResponse`+`Response`/`StaticFiles` + `_SPA_DIR` constant + `_register_spa` function + conditional mount block ≈ 70 lines removed; `tests/api/test_static.py` entire file deleted; `compose/app.yml` BFF `dockerfile:` key deleted) + 4 reverts/refreshes (Dockerfile Stage 1 bind-mount sources `services/bff/uv.lock` → bare `uv.lock` + COPY `services/bff/ /app/` → `COPY . /app/` + Stage 1 comment block reverts Story 1.14 root-context rationale to Story 6.3 per-service revert; `compose/app.yml` BFF `build.context: ..` → `../services/bff` + file-header lines 5-7 swap Story 1.14 SPA-in-BFF claim for post-6.2/6.3 wording; `docker-compose.yml` lines 1-14 drop trailing "still in the BFF image but unreachable; Story 6.3 removes it" + retire `cd spa && npm start` mention if 6.2 didn't already; Story 1.14 frontmatter gets 3-line supersession block `superseded_by` + `superseded_on` + `supersession_rationale` — body byte-identical, codified history preserved). Post-6.3 BFF image is two-stage Python-only (~250-350 MB, down from ~600-700 MB), API surface byte-identical (`/health` 200, `/api/me` 401 envelope, `/auth/login` 302 to Keycloak unchanged, CSRF + middleware stack + router order + exception handlers untouched), pytest 343 tests green (down from 347 — the 4 `test_static.py` tests removed), `git grep -nE 'StaticFiles|_SPA_DIR|_register_spa|_spa_or_404|_json_404|_spa_fallback|FileResponse|/app/static|node-builder|node:22-slim' services/bff/` returns zero matches (AC5 + SCP §6 success criteria). **D16 partial-closure intentionally reopened**: Story 1.14's non-HTML 404 envelope branch in `_spa_or_404` (the project's `{errorCode:not_found,message:"Not found",detail:null}` shape for non-HTML clients hitting unknown paths) is removed; FastAPI's default `{"detail":"Not Found"}` shape resumes for `bff:8000/<unknown>` GET/HEAD probes. Deliberate per AC2 clarification — BFF is internal-only post-Epic-6, in-compose probes can read default 404 fine, reintroducing a catch-all creates ordering coupling with future router registrations. Real fix if envelope ever matters again: a Starlette `ExceptionHandler` for `404 Not Found` (no catch-all route required) — log as `D154` if explicit tracking wanted. **Out-of-scope blocklist (AC9, zero allowed)**: `services/bff/.dockerignore` (already suitable; only edit if AC1 verify reveals real gap — pre-emptive churn forbidden), root `.dockerignore` (5 `services/bff/...` exclusion lines become informational post-6.3 — keep them; RS still uses root context), `security_headers.py` (CSP source-move is Story 6.4), `csrf.py` (Story 6.2 already wired new `bff_base_url`), all `services/bff/src/bff/api/**` + `core/errors.py` (API surface byte-identical), all `services/resource-server/**` (zero Epic 6 changes), all `spa/**` (6.1+6.2 own), `compose/infra.yml` (6.2 owned `SPA_HOST_PORT` env), `compose/app.e2e.yml` (Story 6.4 sweeps e2e — overlay has no `build:` block so BFF context change propagates without overlay edit), `keycloak/realm-bmad-books.json` (Story 6.2 owns realm edits), `e2e/**` (Story 6.4), all planning artefacts + `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md` itself (historical), `README.md` + `docs/**` (6.4 doc sweep), `Justfile` + `.env.example` (no changes). **Post-6.3 stack state**: `just e2e-up` still fails until Story 6.4 sweeps `e2e/playwright.config.ts` E2E_BASE_URL `:8000` → `:4000` + `--host-resolver-rules` + `compose/app.e2e.yml` `E2E_BASE_URL` — explicitly handed off, AC9 names this as expected. Verification probes (Task 8): `docker build -t bff-cleanup-test services/bff/` two-stage clean exit (~250-350 MB image — record empirical size); `docker run` standalone serves `/health`; full compose `down && up -d --wait` all four healthy in 120s; in-compose `docker compose exec spa node -e "fetch('http://bff:8000/')"` → 404 FastAPI default; `curl http://localhost:4000/api/me` → 401 session-expired envelope (Story 6.2 AC4 byte-identical); OAuth happy path through `:4000` against `testuser/testpassword` end-to-end; `cd services/bff && uv run pytest` → 343 green + `bff/main.py` coverage ≥90%; AC5 narrow grep zero + wider safety-net `git grep 'SPA|spa|static' services/bff/` audited for factual accuracy. Failure-prevention notes embedded: (1) uv bind-mount path revert is load-bearing — Stage 1 lines must drop `services/bff/` prefix when context flips to per-service else build fails; (2) NO replacement catch-all in main.py (D16 intentional reopen, reintroducing creates ordering coupling); (3) CSP middleware stays attached to JSON responses (HTML responses no longer come out of BFF — middleware predicate `_attach_csp` simply never fires from outside compose network; D369 future-hardening unchanged); (4) RS's `compose/app.yml` `context: ..` is independent and NOT reverted in 6.3 (D4 closure for RS stylistic-alignment only — out-of-scope for 6.3); (5) `compose/app.e2e.yml` overlay has no `build:` block so BFF context change propagates without overlay edit; (6) Story 1.14 frontmatter gets supersession metadata but body stays byte-identical (codified history preserved); (7) `docker compose down -v` NOT required for 6.3 iterations (no volume-backed state changes — `down && up --build` sufficient; `--build` flag is load-bearing because BFF image must rebuild post-Dockerfile-edit, otherwise compose reuses cached image and cleanup change isn't reflected). Three failure modes to watch in AC1 verification: (a) uv bind-mount paths still prefixed `services/bff/` after Task 2 revert → build fails on missing files; (b) `/app/static` directory present in runtime image → Stage 2 COPY-from-node-builder not fully removed (audit `docker run --rm --entrypoint ls bff-cleanup-test /app`); (c) image size >450 MB → something larger than venv landed (audit Stage 2 COPY lines). Sequencing: 6.3 lands AFTER 6.2 (`done`); 6.4 (e2e + smoke + docs sweep) lands AFTER 6.3. Epic 6 progress: 1/4 stories done [6.1]; 6.2 ready-for-dev; 6.3 ready-for-dev (this entry); 6.4 still backlog. 6-3 status: backlog → ready-for-dev. Earlier on 2026-05-19: story 6.2 dev start → in-progress; story 6.2 contexted → ready-for-dev — file `6-2-spa-dockerfile-compose-service-keycloak-realm-port.md` written from Sprint Change Proposal 2026-05-19 §4 Story 6.2 + §2.5 Compose / Docker / Keycloak impact [primary source of truth — epics.md still does NOT carry Epic 6; Story 6.4 owns that append] + Story 6.1 dev completion notes [SSR runtime contract — `dist/spa/server/server.mjs` entrypoint, `BFF_INTERNAL_URL` + `PORT` + `SPA_PUBLIC_ORIGIN` env contract] + current compose/keycloak/Dockerfile state at HEAD `ed34ac7` (note: `feat/containerization` branch carries Story 6.1's uncommitted working-tree diff — 11 modified + 8 untracked files under `spa/` + planning artefacts — which 6.2 must NOT include in its `git diff` allowed-touch list per AC9). 9 ACs + 10 tasks. **Scope: compose + container + realm port pin.** Deliverables: 2 NEW files (`spa/Dockerfile` 3-stage Node multi-stage deps→build→runtime with non-root `app` user + `EXPOSE 4000` + `ENTRYPOINT ["node", "dist/spa/server/server.mjs"]` + Node-`fetch` healthcheck against `/_health` mirroring BFF's stdlib-only Python probe pattern; `spa/.dockerignore` first per-service `.dockerignore` live in the repo — root `.dockerignore` covers BFF + RS root-context builds, SPA uses tight `../spa` per-service context to keep build context ~5 MB vs ~250 MB root); 5 MODIFIED files (`compose/app.yml` adds `spa:` service block between `resource-server:` and `playwright:` with `build: { context: ../spa, dockerfile: Dockerfile }` + `ports: ["${SPA_HOST_PORT:-4000}:4000"]` + `environment: { NODE_ENV, PORT, BFF_INTERNAL_URL: http://bff:8000, SPA_PUBLIC_ORIGIN: http://localhost:${SPA_HOST_PORT:-4000} }` + `depends_on: { bff: service_healthy }` + Node-fetch healthcheck — AND drops BFF `ports: ["8000:8000"]` block + flips `BFF_BASE_URL: http://localhost:8000` → `http://localhost:${SPA_HOST_PORT:-4000}` because BFF's `redirect_uri` construction reads this env at `services/bff/src/bff/api/auth.py:149,232` + CSRF Origin check anchors on it at `services/bff/src/bff/auth/csrf.py:102`; `compose/infra.yml` adds `SPA_HOST_PORT: ${SPA_HOST_PORT:-4000}` to Keycloak `environment:` for realm-import substitution pass-through; `keycloak/realm-bmad-books.json` three URL substitutions `http://localhost:8000` → `http://localhost:${SPA_HOST_PORT:4000}` at line 80 `post.logout.redirect.uris`, line 84 `redirectUris[0]`, line 87 `webOrigins[0]` — note Keycloak/Quarkus MicroProfile syntax `:` bare-colon NOT `:-` bash-dashed; existing `${BFF_CLIENT_SECRET}` at line 71 proves substitution mechanism works against this realm; `.env.example` documents optional `#SPA_HOST_PORT=4000` override after the test-reset block; `docker-compose.yml` header comment refresh — Story 1.14's "SPA bundle baked into the BFF image" → Story 6.2's "SPA SSR edge runs as its own service; BFF static-serve code path still live in BFF image but unreachable from the browser; Story 6.3 will delete it"). Files explicitly NOT touched (AC9 blocklist): `services/bff/Dockerfile` (Story 6.3 drops Stage 0), `services/bff/src/bff/main.py` (Story 6.3 deletes `_register_spa`/`_SPA_DIR`/`_spa_or_404`/`StaticFiles`/`FileResponse`), `services/bff/tests/api/test_static.py` (Story 6.3 deletes), `services/bff/src/bff/middleware/security_headers.py` (Story 6.4 moves CSP source BFF→SPA per A8 amendment — Story 6.2 intentionally ships the SPA edge without CSP middleware, accepting the temporary state for a local-only educational demo), `services/bff/src/bff/auth/csrf.py` (no change — propagates via BFF_BASE_URL env), `services/resource-server/**` (zero Epic 6 changes), `spa/src/**` + `spa/angular.json` + `spa/package.json` + `spa/tsconfig.app.json` (Story 6.1's territory — `server.ts` proxy mount + `/_health` route + `app.config.server.ts` providers + the two SSR interceptors stay verbatim), `e2e/**` + `Justfile` + `compose/app.e2e.yml` (Story 6.4 sweeps e2e — `just e2e-up` is expected to be indeterminate between 6.2 close and 6.4 close because Playwright runner's `E2E_BASE_URL=http://localhost:8000` + `--host-resolver-rules MAP localhost:8000 bff:8000` no longer match the new topology), `README.md` + `docs/smoke-run.md` + `docs/security-review.md` + `docs/coverage-report.md` (Story 6.4), `_bmad-output/planning-artifacts/architecture.md` + `PRD.md` + `epics.md` (Story 6.4 owns F3/I6/A8 edits + F7/I9 inserts + §6 PRD clarification + Epic-6 epics.md block), `_bmad-output/implementation-artifacts/1-14-...md` (Story 6.3 owns the supersession status append). AC chain: AC1 `docker compose build spa` produces image with `EXPOSE 4000` + non-root `app` user + Node-fetch healthcheck + runtime stage carrying ONLY `dist/spa/` (no `node_modules` — Angular's `@angular/build:application` outputMode=server bundles all SSR deps into `dist/spa/server/main.server.mjs` at ~446 KB per Story 6.1 verification); AC2 `down -v && docker compose up -d --wait` brings all 4 services healthy within 120s (`spa` depends on `bff: service_healthy` only, transitively gated on Keycloak via BFF); AC3 `curl -L http://localhost:4000/` returns 200 with `<app-root` + `ngh="..."` + `ng-server-context="ssr"` + `<script id="ng-state">` markers (Probe 1 follows the 302→/login auth-guard redirect chain documented in Story 6.1 AC12 Probe 1); AC4 `curl http://localhost:4000/api/me` returns 401 byte-for-byte BFF envelope `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}`; AC5 `curl http://localhost:8000/` returns connection-refused (BFF host port removed) AND BFF still reachable on compose network as `http://bff:8000`; AC6 `down -v && up` then OAuth happy path against `testuser/testpassword` completes — `/auth/login` 302 to Keycloak with decoded `redirect_uri=http://localhost:4000/auth/callback`, Keycloak 302s back to `http://localhost:4000/auth/callback`, BFF exchanges code + sets cookies + 302s to `/books`, SPA renders `/books` authenticated via `provideAppInitializer` cookie-forward + URL-rewrite SSR interceptors; logout completes the BFF revoke + end-session + clear-cookie chain; AC7 `SPA_HOST_PORT=4100 down -v && up` works end-to-end with `redirect_uri=http://localhost:4100/auth/callback` (5 substitution sites must all use the env var: compose `spa.ports`, BFF `BFF_BASE_URL`, SPA `SPA_PUBLIC_ORIGIN`, Keycloak service `environment.SPA_HOST_PORT` pass-through, realm JSON three placeholders — missing #4 silent footgun: realm boots with literal `${SPA_HOST_PORT:4000}` placeholder yielding `Invalid parameter: redirect_uri`); AC8 SPA healthcheck on `/_health` via Node `fetch` one-liner mirroring BFF's stdlib pattern, BFF healthcheck unchanged; AC9 scope-leak audit — `git diff main..HEAD` allowed-touch list excludes all Story 6.1/6.3/6.4 territory. Load-bearing close gate: Task 9 live verification — `docker compose down -v && build && up -d --wait` + 4 probes against `:4000` + manual OAuth happy path against `testuser` + `SPA_HOST_PORT=4100` re-run for AC7. Verification IS the test surface in 6.2 (no new pytest/Vitest tests — Story 6.1 already added the SSR interceptor specs at 164 specs / 21 files; Story 6.3 will delete `test_static.py`). Known intentional regression at 6.2 close: SPA HTML responses carry NO CSP header until Story 6.4 moves CSP source BFF→SPA per A8 amendment; `just e2e-up` indeterminate until Story 6.4 sweeps e2e config + `--host-resolver-rules`; host-side `cd spa && npm start` already broken by Story 6.1's `proxy.conf.json` deletion. Story 1.14's SPA-in-BFF surface (BFF Dockerfile Stage 0 Node-builder + `_register_spa` static-serve code path + `test_static.py` 4-test surface) stays LIVE through 6.2 — unreachable from the browser post-6.2 (no BFF host port) but still in the BFF image; Story 6.3 deletes it. Three critical failure-prevention notes from this story: (1) Keycloak/Quarkus substitution syntax `${VAR:default}` bare-colon NOT `${VAR:-default}` bash-dashed — using bash form yields literal text `localhost:-4000` and OAuth breaks; (2) Keycloak only re-imports realm on empty data volume so `down -v` is mandatory after realm edit per Sprint Change Proposal §2.9 risk row 3; (3) `BFF_BASE_URL` must flip to `:4000` for the browser-facing OAuth `redirect_uri` AND for CSRF Origin check propagation, NOT to compose-DNS `http://bff:8000` (that's `BFF_INTERNAL_URL` on the SPA service, which is what `http-proxy-middleware` targets). Epic 6 progress: 1/4 stories done [6.1]; 6.2 ready-for-dev; 6.3 + 6.4 still backlog. 6-2 status: backlog → ready-for-dev. Earlier on 2026-05-19: story 6.1 code review pass → done — `bmad-code-review` ran Blind Hunter + Edge Case Hunter + Acceptance Auditor in parallel against the `spa/`-scoped 787-line diff at baseline `ed34ac7`. Acceptance Auditor PASS on AC1, AC3–AC10, AC13 (PARTIAL on AC1 doc-only: Express ^5.1.0 + http-proxy-middleware ^4.0.0 schematic-shipped vs AC1's ^4.18.x + ^3.0.x estimates, no functional impact; AC2/AC11/AC12 verified by dev report only). 1 decision-needed (resolved → log swallowed SSR errors via `console.warn`), **8 patches applied**, 5 deferred (D149-D153 logged), 12 dismissed as noise. Patches: P1 SSR cookie forwarder leak guard — `ssrCookieForwardInterceptor` now injects `SSR_API_TARGET` and only forwards `Cookie` when `req.url.startsWith(target)` so a third-party SSR fetch (analytics/CDN) can never exfiltrate `bff_session`; P2 proxy hardening — `createProxyMiddleware` gains `proxyTimeout: 30_000` + `timeout: 30_000` + `on.error` 502 handler so a slow/down BFF returns `{"errorCode":"bff_unreachable"}` rather than stalling the SSR render indefinitely; P3 empty-string env handling — both `BFF_INTERNAL_URL` reads (`server.ts` + `app.config.server.ts`) switched from `??` to `||` so an empty env value falls back to the default rather than producing `://api/me`; P4 same fix for `SPA_PUBLIC_ORIGIN` in `app.config.server.ts`; P5 trailing-slash normalization — both env reads now `.replace(/\\/+$/, '')` so `BFF_INTERNAL_URL=http://bff:8000/` doesn't produce `http://bff:8000//api/me` and break the transfer-cache origin-map lookup; P6 pathFilter prefix-collision fix + single-source — new `spa/src/app/shared/http/ssr-proxied-prefixes.ts` exports `SSR_PROXIED_PREFIXES = ['/auth/', '/api/', '/v1/']` consumed by both `ssr-api-url.interceptor.ts` AND `server.ts` (the server now uses `/**`-suffixed micromatch globs so `/apiOther`, `/v1foo`, `/authenticate` are no longer silently proxied to the BFF and the two prefix lists can't drift); P7 `app.listen` cleanup — `Number(process.env['PORT']) \\|\\| 4000` for proper numeric coercion + bind-failure handling moved off the dead `(error) =>` callback into a `server.on('error', ...)` handler matching Express 5's actual API; P8 SSR error visibility — `provideAppInitializer`'s server-platform `bootstrap.catch(() => undefined)` now logs via `console.warn` so swallowed errors (BFF 5xx, programmer bugs, network failures) surface in SSR logs instead of producing a silent anonymous render. Deferred items D149-D153 logged in `deferred-work.md`: D149 BFF_INTERNAL_URL read in two places (single-source refactor better folded into Story 6.2's compose wiring), D150 no Express `trust proxy` config (depends on Story 6.2's deployment topology), D151 no SIGTERM graceful shutdown (operational, Story 6.2 territory), D152 `import.meta.dirname` Node-version compat (Story 6.2's Dockerfile pins runtime), D153 `tsconfig.app.json` `"types": ["node"]` exposing Node globals to browser-target source (structural — needs separate `tsconfig.server.json` + `angular.json` wiring; outside one-line-patch scope). Post-patch: `npx ng test --watch=false` reports **166 specs / 21 files passing** (+2 net new: cookie-leak guard + cookie-no-op-when-target-absent), `npm run build` clean (server bundle 877.39 kB, browser initial 239.09 kB, well under 1 MB cap). 1 new file (`ssr-proxied-prefixes.ts`); 4 modified (`server.ts`, `app.config.ts`, `app.config.server.ts`, `ssr-cookie-forward.interceptor.ts` + `.spec.ts`, `ssr-api-url.interceptor.ts`). Story 6.1 status: review → done. Epic 6 progress: 1/4 stories done [6.1]; 6.2/6.3/6.4 still backlog. Earlier on 2026-05-19: story 6.1 dev complete → review — Angular 21.2 SSR scaffold landed inside `spa/` only via `npx ng add @angular/ssr@^21.2 --skip-confirmation --defaults` + manual `npm install http-proxy-middleware` (schematic-shipped Express ^5.1.0 + http-proxy-middleware ^4.0.0 — both newer than AC1's ^4.18.x + ^3.0.x doc-only estimates, recorded in story's Completion Notes for 6.4 doc sweep, no functional impact). Customized `src/server.ts` mounts the proxy at root with `createProxyMiddleware({ pathFilter: ['/auth','/api','/v1'], target: bffInternalUrl, changeOrigin: true, xfwd: true })` — two prior iterations (`app.use(['/auth','/api','/v1'], proxy)` array-path; three separate `app.use('/api', proxy)` mounts) both failed because Express 5 strips the mount-path before invoking the proxy, causing BFF's Story 1.14 SPA catch-all to swallow `/me` as `index.html`. Middleware order non-negotiable: `/_health` → `pathFilter`-proxy → static → Angular SSR catch-all. Three Angular-21-SSR gotchas surfaced + fixed: (1) schematic-default `RenderMode.Prerender` in `app.routes.server.ts` crashes the build at route-extraction time because `loadMe()` fires against unreachable BFF — flipped to `RenderMode.Server`; (2) `provideAppInitializer(() => loadMe())` still fires at build-time route extraction even under `RenderMode.Server`, wrapped in server-platform `.catch(() => undefined)` so a network error during extraction doesn't crash the build; (3) Angular 21's new anti-SSRF `security.allowedHosts: []` in `angular.json` rejects every SSR request with 400 — set to `["localhost"]` (validator strips port for the comparison, so `"localhost"` covers all `localhost:*` ports). NEW files (8): `src/server.ts` (customized), `src/main.server.ts` (schematic), `src/app/app.config.server.ts` (`provideServerRendering(withRoutes(serverRoutes))` + `{ provide: SSR_API_TARGET, useValue: bffInternalUrl }` + `{ provide: HTTP_TRANSFER_CACHE_ORIGIN_MAP, useValue: { [bffInternalUrl]: spaPublicOrigin } }`), `src/app/app.routes.server.ts` (`RenderMode.Server` catch-all), `src/app/shared/http/ssr-api-target.token.ts` (`InjectionToken<string>('SSR_API_TARGET')`), `src/app/shared/http/ssr-api-url.interceptor.ts` (server-platform-gated; rewrites `/auth/` `/api/` `/v1/` prefixed URLs to `${SSR_API_TARGET}${url}`; no-op on absent token / non-matching URL / browser platform), its `.spec.ts` (5 cases), `src/app/shared/http/ssr-cookie-forward.interceptor.ts` (server-platform-gated; `inject(REQUEST, { optional: true })` from `@angular/core` returning Fetch-API `Request`, reads `request.headers.get('cookie')`, attaches via `req.clone({ setHeaders: { cookie } })`), its `.spec.ts` (4 cases). MODIFIED (8): `package.json` + `package-lock.json` (`@angular/ssr` + `express` + `@types/express` + `http-proxy-middleware` + `serve:ssr:spa` script), `angular.json` (`build.options.server`/`outputMode`/`security.allowedHosts`/`ssr.entry` added, `serve.options.proxyConfig` removed), `tsconfig.app.json` (`"types": ["node"]` added by schematic), `src/app/app.config.ts` (added `provideClientHydration(withEventReplay())` from `@angular/platform-browser` + both new SSR interceptors after browser-time interceptors in `withInterceptors([...])` array + `provideAppInitializer` wrapped in `isPlatformServer(platformId) ? bootstrap.catch(() => undefined) : bootstrap`), `csrf-interceptor.ts` (early-return on `isPlatformServer(inject(PLATFORM_ID))`), `csrf-interceptor.spec.ts` (server-platform POST case), `with-credentials-interceptor.ts` (`router.navigateByUrl` wrapped in `isPlatformBrowser(platformId)` guard; `authService.clear()` stays outside the guard as signal-write-only), `with-credentials-interceptor.spec.ts` (server-platform 401 case asserting clear() fires but navigate does NOT). DELETED (1): `proxy.conf.json`. Live AC12 4-probe verification against `docker compose up -d --wait` default profile + `node dist/spa/server/server.mjs` with `PORT=4000 BFF_INTERNAL_URL=http://localhost:8000`: **Probe 1** `GET /` returns 302 → `/books` (root redirect) → auth-guard 401 chain → final SSR-rendered 200 at `/login?return_to=%2Fbooks` (7688 bytes) with verified hydration markers `<app-root ng-version="21.2.13" ngh="2" ng-server-context="ssr">` + nested `ngh="0"`/`ngh="1"` boundaries + `<script id="ng-state">` transfer-state payload + 872 bytes of rendered TopChrome+LoginView markup inside `<app-root>`; direct hit on `/login` returns identical markers — AC12 probe-1 wording expected 200 directly but the 302-then-200 chain is the correct SSR-aware behavior for an auth-gated `/` (Story 6.4 may want to soften AC wording in the docs sweep, no code change needed). **Probe 2** `GET /api/me` returns 401 + byte-for-byte BFF envelope `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}` matching Story 5.4 Run Record 2026-05-18. **Probe 3** `GET /auth/login` returns 302 → `http://localhost:8080/realms/bmad-books/protocol/openid-connect/auth?client_id=bmad-books-bff&response_type=code&scope=openid+reading-speed%3Aread+reading-speed%3Awrite&redirect_uri=http%3A%2F%2Flocalhost%3A8000%2Fauth%2Fcallback&state=<random>&nonce=<random>&code_challenge=<random>&code_challenge_method=S256` — full PKCE flow params, `redirect_uri` still points at `:8000` because BFF emits its own `BFF_BASE_URL=http://localhost:8000` (Story 6.2 owns the flip to `:4000`). **Probe 4** `GET /_health` returns 200 + `{"ok":true}`. Bonus CSRF passthrough probe `POST /v1/books {}` returns BFF's `403 csrf_invalid` envelope — proves browser-side state-changing requests reach the BFF unchanged AND BFF's CSRF middleware is intact end-to-end. SPA Vitest 152 specs / 19 files (Story 5.1 baseline) → **164 specs / 21 files** post-6.1 (12 net new: 5 in ssr-api-url interceptor spec + 4 in ssr-cookie-forward interceptor spec + 1 server-platform case in csrf-interceptor.spec + 1 server-platform case in with-credentials-interceptor.spec; +1 internal direct-injection-context case in ssr-api-url). All four NFR11 aggregates moved fractionally UP: statements 94.71% → 94.88%, branches 91.16% → 91.45%, functions 95.74% → 95.87%, lines 94.52% → 94.73%. Per-file coverage on new code 100%/100%/100% across `ssr-api-target.token.ts`, `ssr-api-url.interceptor.ts`, `ssr-cookie-forward.interceptor.ts`; patched `csrf-interceptor.ts` at 95.65% L / 75% B / 100% F; patched `with-credentials-interceptor.ts` at 93.75% L / 100% B / 100% F — all comfortably above ≥80% line floor. Production build output: `dist/spa/browser/index.csr.html` (3385 bytes — Angular 21 renamed from `index.html`) + `dist/spa/browser/main-*.js` (hashed browser bundle) + `dist/spa/server/server.mjs` (876.95 kB Express SSR runtime) + `dist/spa/server/main.server.mjs` (445.89 kB Angular server bundle); browser initial total 236.63 kB (well under 1 MB cap). AC13 scope-leak audit clean: zero `git status` entries under `services/`, `compose/`, `keycloak/`, `e2e/`, `README.md`, `docs/`, `.env.example`, `Justfile`; the only untracked planning artifact `sprint-change-proposal-2026-05-19.md` predates this dev session per the session-start git snapshot. Zero new defers logged in `deferred-work.md` — the four AC-vs-reality observations (Express v5 / http-proxy-middleware v4 schematic-shipped versions, probe 1 302 chain, schematic-default Prerender mode, allowedHosts new requirement) are documentation-only and recorded in story's Completion Notes for Story 6.4 to fold into its architecture/docs sweep. `ng serve --ssr` with custom middleware NOT exercised in 6.1's review per Sprint Change Proposal §2.9 risk row 1 — Story 6.2 (containerized dev) revisits the dev-loop story; until then, the documented dev path is `npm run build && node dist/spa/server/server.mjs` against `docker compose up`. Story 1.14's SPA-in-BFF surface stays LIVE through 6.1 — Story 6.3 owns its removal; the BFF still serves the SPA on `:8000` in parallel with the new SSR edge on `:4000`. Epic 6 progress: 1/4 stories in review [6.1]; 6.2/6.3/6.4 still backlog. 6-1 status: in-progress → review. Earlier on 2026-05-19: story 6.1 contexted → ready-for-dev — file `6-1-spa-angular-ssr-scaffold-proxy-cookie-forwarding.md` written from Sprint Change Proposal 2026-05-19 §4 Story 6.1 [primary source of truth — epics.md does NOT yet carry Epic 6; Story 6.4 owns that append] + Angular 21 SSR + `@angular/ssr` v21 + `http-proxy-middleware` v3 verification + current SPA codebase analysis at HEAD `ed34ac7`. 13 ACs + 10 tasks. **Scope: `spa/` only.** AC13 is the explicit anti-scope-creep close gate — zero entries permitted under `services/`, `compose/`, `keycloak/`, `e2e/`, `_bmad-output/planning-artifacts/`, `README.md`, `docs/`, `.env.example`, `Justfile`. Story 1.14's SPA-in-BFF runtime stays LIVE through 6.1 (Story 6.3 owns the BFF cleanup; 6.2 owns the new `spa` compose service + Keycloak realm port flip 8000→4000; 6.4 owns the architecture.md/PRD.md/epics.md/README.md/docs sweep + e2e config). Architecture decisions touched conceptually but NOT edited: F3 (line 436 SPA-in-BFF serving model — flips in 6.4), I6 (line 509 compose serving — flips in 6.4), A8 (line 354 CSP source — moves BFF → SPA in 6.4), line 178 + line 289 `--ssr=false` — flips in 6.4. Deliverables: ≈8 NEW files (`spa/src/server.ts` customized from `ng add @angular/ssr` output, `spa/src/main.server.ts` schematic default, `spa/src/app/app.config.server.ts` with `provideServerRendering()` + `SSR_API_TARGET` factory + `HTTP_TRANSFER_CACHE_ORIGIN_MAP` from `@angular/common/http`, `spa/src/app/app.routes.server.ts` schematic default `RenderMode.Server` catch-all, `spa/src/app/shared/http/ssr-api-target.token.ts` `InjectionToken<string>`, `spa/src/app/shared/http/ssr-api-url.interceptor.ts` server-platform-gated URL rewriter for `/auth/` `/api/` `/v1/` prefixes, its `.spec.ts`, `spa/src/app/shared/http/ssr-cookie-forward.interceptor.ts` reading `REQUEST` token from `@angular/core` per Angular 19.2+ contract — Web-API `Request` not Node Express req — using `request.headers.get('cookie')` and `req.clone({ setHeaders: { cookie } })`, its `.spec.ts`); ≈6 MODIFIED files (`spa/package.json` adds `@angular/ssr ^21.2.x` + `express ^4.18.x` from schematic + `http-proxy-middleware ^3.0.x` added manually + `@types/express` devDep + `serve:ssr:spa` script, `spa/angular.json` gains `server: "src/server.ts"` build option + Angular 21 serve-ssr target + loses `proxyConfig: "proxy.conf.json"` reference, `spa/tsconfig.app.json` schematic-extended for server entry, `spa/src/app/app.config.ts` gains `provideClientHydration(withEventReplay())` + extends `withInterceptors([...])` with the two new SSR interceptors keeping `withFetch()`, `spa/src/app/shared/http/csrf-interceptor.ts` early-returns on `isPlatformServer(inject(PLATFORM_ID))` to avoid `document.cookie` SSR-unsafe access, `spa/src/app/shared/http/with-credentials-interceptor.ts` wraps `router.navigateByUrl` in `isPlatformBrowser(platformId)` guard so SSR doesn't trigger Router state corruption, both .spec.ts files extended with server-platform cases); 1 DELETED file (`spa/proxy.conf.json` — proxy moves to `server.ts`). Middleware order in `server.ts` is non-negotiable per AC3 step 4: `/_health` → `createProxyMiddleware({ target: bffInternalUrl, changeOrigin: true, xfwd: true })` mounted at `['/auth', '/api', '/v1']` → Angular static-asset serving → Angular SSR catch-all LAST (mounting Angular before the proxy = `/api/me` renders as HTML, which is the primary LLM foot-gun). `http-proxy-middleware` v3 forwards `Cookie` + `X-CSRF-Token` headers by default — no extra config needed for browser→edge→BFF cookie passthrough. Explicit no-go list: do NOT use `pathRewrite` (paths 1:1 between SPA edge and BFF), do NOT use `pathFilter` (use Express-mount form), do NOT upgrade Express to v5 (schematic ships v4.18.x for Angular 21 — v5's middleware error semantics break `(req, res, next) => angularApp.handle(req).then(...).catch(next)`), do NOT import `provideServerRendering` from `@angular/platform-server` (NG0201 in Angular 20+ — must be from `@angular/ssr`), do NOT use `@nguniversal/express-engine/tokens` for `REQUEST` (deprecated; removed when `@nguniversal/*` packages retired in Angular 17.x — now lives in `@angular/core`), do NOT replicate CSP middleware in `server.ts` (the A8 CSP-source move BFF→SPA is Story 6.4 territory). SSR `/api/me` chain (AC4+5+6+7+8 composition documented step-by-step in Dev Notes): browser → SPA edge `:4000` → Angular SSR render → `provideAppInitializer(() => loadMe())` → `http.get('/api/me')` → `withCredentialsInterceptor` (server-platform-safe after AC8 patch) → `csrfInterceptor` (early-return on server) → `ssrApiUrlInterceptor` (rewrites `/api/me` → `${SSR_API_TARGET}/api/me`) → `ssrCookieForwardInterceptor` (reads inbound cookie from REQUEST token, clones req with `setHeaders: { cookie }`) → outbound fetch to BFF with cookie → BFF resolves session, returns `Me` → transfer cache stores it → SSR HTML ships with embedded transfer cache → browser hydrates with `provideClientHydration(withEventReplay())` → second `loadMe()` call hits the cache, no `/api/me` re-fire. `HTTP_TRANSFER_CACHE_ORIGIN_MAP` from `@angular/common/http` (Angular 19+) aligns SSR-side absolute URL `http://localhost:8000/api/me` with browser-side relative `/api/me` cache key. Load-bearing close gate AC12: four host-run probes against `node dist/spa/server/server.mjs` with `PORT=4000 BFF_INTERNAL_URL=http://localhost:8000` while `docker compose up` runs the existing default-profile stack (SPA-in-BFF on `:8000`) — Probe 1 `GET http://localhost:4000/` returns 200 + body contains `<app-root` + hydration boundary marker `ngh="..."`; Probe 2 `GET http://localhost:4000/api/me` returns BFF's exact anonymous 401 envelope `{"errorCode":"session_expired","message":"Session expired or not present","detail":null}` verified verbatim from `docs/smoke-run.md` 2026-05-18 Run Record; Probe 3 `GET http://localhost:4000/auth/login` returns 302 with `redirect_uri=http://localhost:8000/auth/callback` (Story 6.2 owns the flip to `:4000`); Probe 4 `GET http://localhost:4000/_health` returns 200 + `{"ok":true}`. Vitest baseline 152 specs / 19 files (Story 5.1) → target ≥156 specs / 21 files post-6.1 (2 new interceptor specs each with 4 cases minimum + server-platform extension on each of the two existing interceptor specs). Per-file coverage floor ≥80% line on both new interceptor files; aggregate at or above Story 5.1 baseline 94.71%/91.16%/95.74%/94.52% (statements/branches/functions/lines). Risk: Angular 21 `@angular/build:dev-server` SSR-mode + custom Express middleware integration is incomplete in 21.2.x (Sprint Change Proposal §2.9 risk row 1); Story 6.1 close-gate is the production-build AC2 + host-run AC12 probes, NOT `ng serve --ssr` — if dev SSR mode happens to work cleanly, document the working invocation as a positive signal for 6.2; if it doesn't, log as `low` severity defer (dev-only, not runtime). Epic 6 status auto-transitioned `backlog` → `in-progress` per workflow (first story in epic). 6-1 status: backlog → ready-for-dev. Earlier on 2026-05-19: Epic 6 introduced via `bmad-correct-course` Sprint Change Proposal at `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md` — splits the SPA out of the BFF image into its own Angular SSR Node container that serves the browser SSR-rendered HTML and reverse-proxies `/auth/*` `/api/*` `/v1/*` to the BFF over compose DNS via `http-proxy-middleware`. Same-origin model preserved; SSR Node process IS the edge (no extra Caddy/nginx layer). New host port `:4000` (SPA edge); BFF loses host port. Story 6.3 explicitly supersedes Story 1.14's SPA-in-BFF surface. Earlier on 2026-05-18: story 5.3 code review pass → done — `bmad-code-review` ran Blind Hunter + Edge Case Hunter + Acceptance Auditor in parallel against the README.md diff (baseline `fb751ec` → working tree at +235/-15 = 235-net-add). 15 patches applied, 1 defer logged (D136 OIDC env var enumeration vs discovery), 11 findings dismissed as noise. Material defects caught: P1 `profiles: [default]` → `[default, dev, e2e]` (verified via `compose/app.yml:57,102` + `compose/infra.yml:57`); P2 `just e2e-up` expansion was the retired `--abort-on-container-exit` single-phase form — replaced with the actual two-phase `up -d --wait keycloak bff resource-server` + `run --rm --build playwright` recipe per `Justfile:56-61` and the Justfile preamble's explicit Story 3.6 retirement note (J4 `killRs()` would SIGTERM the runner); P3 per-surface test-commands table E2E row had the same retired command AND omitted `-f compose/app.e2e.yml` (the exact foot-gun the README itself warns about 30 lines earlier) — replaced with `just e2e-up`; P4 architectural-decision range counts wrong on three of four ranges (C1–C6 → C1–C8, F1–F5 → F1–F6, I1–I7 → I1–I8 per architecture.md heading walk); P5 single-test-focus example used non-existent `test_csrf_token_mismatch` — replaced with real `test_post_header_cookie_mismatch_returns_403` from `services/bff/tests/auth/test_csrf.py:136`; P6 Keycloak realm-import causal claim was wrong (Keycloak service in `compose/infra.yml` has NO `volumes:` block — realm imports on every container (re-)creation; `-v` drops only the BFF/RS `bff_data`/`rs_data` named volumes) — Setup step 7 + Troubleshooting bullet both corrected; P7 Setup step 7 truncate list "books, sessions, reading-speeds" omitted BFF `auth_states` and conflated RS `reading_speeds` into the BFF wipe — aligned with README line 141's correct enumeration; P8 AC8 violation: line 170 inlined `≥70%` and `>90%` threshold numbers despite AC8's explicit "MUST NOT duplicate the threshold numbers" — parenthetical removed, cross-reference to `docs/coverage-report.md` retained. Internal-consistency defects: P9 `:8001` listed as host-port conflict but RS has NO host port (compose/app.yml line 60-62 "internal-only … NO `ports:` block is published") — removed and qualified for the host-direct `uv run` case; P10 cross-origin cookie wording technically wrong (the proxy makes the SPA appear same-origin to the BFF — `SameSite=Lax` HttpOnly cookies don't actually "flow naturally across origins") — reworded; P11 bare `ng serve` silently required a global `@angular/cli` install — replaced with `cd spa && npm start` using the script in `spa/package.json` ("start": "ng serve"); P12 local-against-dev E2E conflated `TEST_RESET_TOKEN` (auth bearer) with `ENABLE_TEST_RESET=true` (route mount flag) — corrected to use the e2e overlay bring-up so both vars are set; P13 Vitest single-file path missing `cd spa &&` prefix — added; P14 sprint-change-proposal entry dated 2026-05-15 cited file dated 2026-05-14 — redated entry to match file (verified file exists as `sprint-change-proposal-2026-05-14.md`); P15 Opus 4.7 vs Opus 4.7 (1M context) inconsistent — added disambiguation note at top of AI integration log explaining the (1M context) suffix marks when the Co-Authored-By trailer convention started noting the larger context window, not a model change. Dismissed 11 findings: byte-for-byte boundary diagram preserved per FP#3 (no paraphrase allowed); intentional `docs/smoke-run.md` forward link per FP#11; AI log self-reference to 2026-05-18 acceptable; "≥5 specs" is NFR11 floor wording not a count claim; misc cosmetic (`.gitignored` style, compose `include:` wording, `worktree-agent-…` ellipsis, JSON snake_case label, "triplet" applied to docs-only stories, Playwright 1.49 pin verified accurate). Post-patch verification: `wc -l README.md` = 259 (DoD #11 range 250–400 ✓); `grep -cE "^## "` = 10 H2 (AC1's 11 sections including H1 ✓); `grep -c '^```'` = 12 code fence markers = 6 balanced blocks ✓; `grep -cE "coverage-report\.md|security-review\.md|architecture\.md"` = 15 (AC12 floor 3 ✓); `grep -nE "BFF_CLIENT_SECRET=|KEYCLOAK_ADMIN_PASSWORD=|TEST_RESET_TOKEN=|change-me"` = no matches (FP#2 ✓); the remaining `:8001` and `--abort-on-container-exit` mentions in the file are now in the patched-explanation context (educational/historical) rather than as a recommended command or live port. Zero production code touched (every patch in README.md). D136 logged in deferred-work.md under new "Deferred from: code review of 5-3-readme-polish-ai-integration-log (2026-05-18)" section. Epic 5 progress: 2/4 stories done [5.1, 5.3], 1 in review [5.2], 5.4 still backlog. 5-3 status: review → done. Earlier on 2026-05-18: story 5.3 dev complete → review — pure-documentation REWRITE pass authored entirely against worktree baseline `fb751ec` (`Merge story 5.2 — security review document`). ZERO source / test / config / production-deliverable changes — `git status --short` shows only ` M README.md`, ` M sprint-status.yaml`, `?? 5-3-readme-polish-ai-integration-log.md` (new story file, not yet `git add`-ed). README grown from 33-line stub (which had: H1 + summary + Setup numbered list + E2E profile sidebar + Architecture overview with three reference lines at lines 24/26/28 + AI integration log placeholder at line 30) to **252-line 11-section structure** per AC1: Title+summary, ## Prerequisites, ## Setup, ## Architecture overview, ## Dev workflow, ## E2E workflow, ## Prod-shaped workflow, ## Per-surface test commands, ## Project structure, ## AI integration log, ## References. All 13 ACs satisfied + all 14 failure-prevention items observed. AC4 boundary diagram inlined byte-for-byte from architecture.md:1096–1121 inside a ```text fence (Option A — self-containment); zero paraphrase. AC10 AI integration log has **12 chronological entries** (well above the AC10 floor of 5) — every BMAD skill from the AC list named at least once (`bmad-create-prd` / `bmad-validate-prd` / `bmad-create-ux-design` / `bmad-create-architecture` / `bmad-check-implementation-readiness` / `bmad-create-epics-and-stories` / `bmad-create-story` / `bmad-dev-story` / `bmad-code-review` / `bmad-retrospective` / `bmad-technical-research` / `bmad-correct-course`) + **SIX notable AI-assisted decisions** (well above AC's "≥1" floor): (1) Angular v21 + Tailwind v4 starter choice via technical research, (2) backend archetype lock-in (`fastapi-archetype` Python 3.14 + uv + OTEL), (3) parallel-epic execution via `git worktree`s starting Epic 2/3, (4) coverage threshold soften (Story 5.1's DN1 — `precision=0` rounding ≥89.5% rather than tightening `fail_under=90`), (5) security review structure mirrors PRD §9 verbatim (Story 5.2 — refused STRIDE / LINDDUN / ASVS cross-walk), (6) J6 honest-failure contract (Story 4.2 — BFF never fabricates an estimate when RS is unavailable). Claude model attributions cross-verified against actual `Co-Authored-By:` trailers: Opus 4.7 (2026-05-14 early), Opus 4.7 + **Sonnet 4.6** for the code-review pass (2026-05-15 onward — "different LLM for review" convention from Story 1.10), Opus 4.7 (1M context) (2026-05-16 → 2026-05-18). AC12 verification: `grep -cE "coverage-report\\.md|security-review\\.md|architecture\\.md" README.md` returns **15 matches** (floor: 3); all three preserved reference targets present in both Architecture overview AND References sections. AC11 References has 7 internal targets (PRD / architecture / ux / epics / security-review / coverage-report / smoke-run-forward-pointing) + 2 external (`fastapi-archetype` GitHub URL + `just` task runner). Markdown lint sanity: 12 code fence markers = 6 balanced blocks (1 boundary diagram + 1 project tree + 2 dev-workflow bash + 1 E2E `just e2e-up` + 1 local E2E bash); 19/19 internal `[label](path)` links resolve to existing repo files EXCEPT `docs/smoke-run.md` (intentionally forward-pointing per failure-prevention #11 — Story 5.4 owns the file). NO secret values printed (failure-prevention #2 — env-var NAMES only for `KEYCLOAK_ADMIN_PASSWORD` / `BFF_CLIENT_SECRET` / `TEST_RESET_TOKEN`; seeded users `testuser`/`testpassword` + `freshuser`/`freshpassword` printed because they live in version-controlled `keycloak/realm-bmad-books.json`). No `python3` invocations in shell commands (CLAUDE.md compliance — the only `python3` in the file is inside the descriptive CLAUDE.md project-tree annotation noting the convention itself). One failure-prevention-#7 violation caught and reverted mid-pass: added `## What's deliberately out of scope` 12th-H2 to push line count toward the 250 floor, immediately reverted, folded the same content into the AI integration log's "AI-assisted decisions left as accepted scope" closing bullet block — no section creep in final file. Line count path: initial 211 → 220 → 223 → 227 → 238 → 242 → 248 → 252 via substantive expansions (diagram callouts explaining the three edges, two more notable decisions on archetype + worktree-parallel, Setup steps 6/7 for "open the app" + clean reset, 4-trip troubleshooting, single-test-focus iteration commands, Epic 1 story breakdown into chronological sub-bullets). Path stayed inside failure-prevention #6 ("do NOT extend the README to a tutorial") and #7 ("no extra sections beyond the 11 AC1 specifies"). Regression spot-checks (Task 8.2/8.3 BFF + SPA test runs) marked N/A per Story 5.2 precedent — diff touches zero production code so regression vector is mathematically empty. Zero new defers logged (Task 9.1 expected count: zero — AC10's "accepted scope" block consolidates existing defers D371 CSP hardening + D372 extra headers + AR1 plaintext token columns + AR3 no idle timeout + no-CI scope decision; no new defer IDs introduced). Epic 5 progress: 1/4 stories done [5.1], 2 in review [5.2, 5.3], 5.4 still backlog. 5-3 status: ready-for-dev → in-progress → review. Earlier on 2026-05-18: story 5.3 contexted → ready-for-dev — file `5-3-readme-polish-ai-integration-log.md` written from Epic 5 AC source [epics.md L1851-1890] + Story 5.1/5.2 README single-line-reference precedent + current README stub baseline at HEAD `fb751ec` (33 lines: H1+summary, Setup numbered list, E2E profile sidebar, Architecture overview with three reference lines [architecture/coverage-report/security-review], AI integration log placeholder). 13 ACs + 9 tasks. Pure-documentation REWRITE deliverable: ONE modified file `README.md` — the 33-line stub grows ~10x into the 11-section structure AC1 specifies (Title+summary, Prerequisites, Setup, Architecture overview, Dev workflow, E2E workflow, Prod-shaped workflow, Per-surface test commands, Project structure, AI integration log, References). ZERO production code / test / compose / planning-artifact changes (failure-prevention #1 + DoD #9). AC12 explicit: every reference target from the current stub (architecture.md, docs/coverage-report.md, docs/security-review.md at README lines 24/26/28) MUST survive into the new file. AC10 minimums: ≥5 chronological entries, each with ISO-8601 date + one-line summary; BMAD skills enumerated (create-prd / create-ux-design / create-architecture / check-implementation-readiness / create-epics-and-stories / create-story / dev-story / code-review / retrospective); ≥1 notable AI-assisted decision (Angular v21 + Tailwind v4 candidate per architecture.md:156, OR coverage-threshold calibration per Story 5.1's DN1 outcome); Claude model attribution cross-checked against `Co-Authored-By` trailers (Opus 4.6 → early epics; Opus 4.7 1M context → Epic 4+5). AC4 boundary-diagram has two acceptable options: (A) inline byte-for-byte from architecture.md:1096–1121 inside ```text fence, or (B) one-line link to the architecture.md anchor — pick one, do not partially paraphrase. AC7 forward-references `docs/smoke-run.md` (owned by Story 5.4 / does not exist yet) — intentional dangling link mirroring how the current README forward-pointed to 5.3 from 1.1. AC2 prerequisites: Node ≥20 LTS, Python ≥3.14 (verified against both pyproject.toml `requires-python` pins), `uv`, Docker + Compose v2.20+, `npx`, `just` (required for E2E profile due to Story 1.12 P1 foot-gun), optional `ng` CLI. AC8 per-surface test commands cite the four base commands + four coverage variants Story 5.1 already established. AC9 project-structure tree is TOP-LEVEL only — ≤25 lines, no recursion into services/spa/src; defers to architecture.md:863+ deep tree via link. Failure-prevention checklist 14 items: NO secret values printed (env-var NAMES only — admin password / BFF_CLIENT_SECRET / TEST_RESET_TOKEN never; seeded users `testuser`/`testpassword` + `freshuser`/`freshpassword` OK because they live in the version-controlled realm-bmad-books.json), do NOT paraphrase the boundary diagram (byte-for-byte if inlining), do NOT fabricate AI-log entries (each entry substantiable from git log + retro files + planning artifacts), do NOT misstate Claude model usage (cross-check Co-Authored-By trailers), do NOT extend the README to a tutorial (architecture.md / security-review.md own the conceptual treatment), do NOT add sections beyond the 11 AC1 specifies, do NOT inline the deep project tree, do NOT promise CI (PRD §4 / architecture I7 out-of-scope), do NOT silently break docs/smoke-run.md forward link (one-line "populated by Story 5.4" note required), do NOT mix worktree paths into the README (canonical name `bmad-books`; `E5S3` belongs in commit metadata only). DoD load-bearing close gate: line count in 250–400 range (target ~300), AC10 ≥5 entries + ≥1 notable decision + model attribution, AC12 ≥3 grep matches on the preserved reference targets, markdown lint balanced fences + all `[label](path)` links resolve except docs/smoke-run.md. Earlier on 2026-05-18: story 5.2 dev complete → review — bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran in parallel. Acceptance Auditor returned PASS on all 13 ACs + all 22 failure-prevention checklist items; the user-authorized `Justfile` + README "Compose-runner gotcha" scope expansion (Change Log v1.1) is the only AC-list deviation. 4 patches applied (1 was a decision-needed resolved by user before applying): DN1 strict apostrophe pin (replaced tolerant regex `/Couldn[’']t.../` with literal `Couldn’t...` U+2019 in AC6-J3 generic-failure test), P1 result-regex tightened to `/≈\s*[1-9]\d*\s*[hm]/` across both j3-estimate.spec.ts and j6-rs-unavailable.spec.ts (rejects fabricated `≈ 0 m`), P2 added positive `impliedMinutes(r1/r2) > 0` guards before the strict-inequality assertion in AC3 coupling test (surface parse failures explicitly), P3 added cell-stays-in-re-estimate assertion in AC5 after the second URL invariance check (catches a regression where Re-estimate silently regresses to idle Estimate state). 6 defers logged D130-D135 in deferred-work.md (Justfile `--build` unconditional, AC9 `.first()` no PUT verification, AC7 color robustness under forced-colors/rgba, AC8 RS-recovery flake risk on httpx pool + cold endpoint, AC4 RouterLink href edge cases, AC5 URL invariance only two-point snapshot). ~25 findings dismissed as noise / already-handled. Static gates re-verified post-patch: `tsc --noEmit` exit 0; `playwright --list` reports 26 tests in 6 files unchanged (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3). Epic 4 now fully done — only optional Epic 4 retro remains. NFR11 satisfied with 6 E2E specs covering J1–J6. Earlier on 2026-05-18: story 4.4 dev complete → review — created `e2e/tests/j3-estimate.spec.ts` (5 tests) + `e2e/tests/j6-rs-unavailable.spec.ts` (3 tests) + `e2e/README.md` addendum; ZERO SPA/BFF/RS/compose/helper changes. All static gates green: tsc --noEmit exit 0, playwright --list reports 26 tests in 6 files (J1×3, J2×8, J3×5, J4×5, J5×2, J6×3), compose config exit 0, helper-only grep clean. Live e2e-up (inlined Justfile recipe because host lacks `just` binary; `--build` flag required since playwright Dockerfile bakes specs via `COPY . .`): **26 passed (52.8s)** — NFR11's ≥5 J1-J6 satisfied with 6 specs. Regression: BFF 543/RS 324/SPA 152 unit tests green; SPA build green; SPA lint has 3 pre-existing `no-fallthrough` errors on baseline `bb15aca` in book-form/book-row/status-control (verified pre-existing; logged as D127). Two dev-time spec deviations during the first e2e-up run: AC2-alt `page.route` POST hold applied for J3 happy-path loading-state (round-trip sub-100ms outruns Playwright expect-poll — D128); AC3 second click switched from Re-estimate to Estimate because `/settings → /books` navigation destroys EstimateCell instance and resets component-local signals (D129, story-AC vs SPA-design inconsistency). Epic 4 backend + SPA + E2E now all dev-complete; only awaits code review + Epic 4 retro (optional). Earlier on 2026-05-18: story 4.4 → in-progress — dev started: authoring `e2e/tests/j3-estimate.spec.ts` [5 tests AC2-AC6-J3] + `e2e/tests/j6-rs-unavailable.spec.ts` [3 tests AC7-AC9] + `e2e/README.md` addendum. Earlier on 2026-05-18: story 4.4 contexted → ready-for-dev — file `4-4-e2e-specs-j3-estimate-j6-rs-unavailable.md` written from Epic 4 AC source [epics.md L1696-1740] + Story 3.6 J4 E2E precedent + Story 4.3 EstimateCell selectors/copy exports + Story 4.2 BFF POST `/v1/books/{id}/estimate` contract + Story 4.1 `format_duration` boundary table; 13 ACs + 6 tasks; creates 2 spec files [j3-estimate.spec.ts, j6-rs-unavailable.spec.ts] + README addendum; NO source/compose/helper changes — harness complete after 3.6; load-bearing close gate is live `just e2e-up` exits 0 with all 26 tests green [J1×3, J2×8, J3×5, J4×5, J5×2, J6×3] = NFR11's ≥5 J1-J6 satisfied. Earlier on 2026-05-18: story 4.3 merged into main — no-ff merge of `worktree-agent-a32e6a27249f65923` brings the dev (commit 3513e66) + code-review (commit 05b8de7) work back to main; 4.3 = done. The worktree's pre-merge YAML had 4.2 = review reflecting its branch-point timestamp; main has since progressed 4.2 → done (2026-05-18 code review), preserved here. Earlier on 2026-05-18: story 4.2 code review pass: 7 patches applied (P1 empty-body → RsUnavailable centralized in _do_rs_call w/ 401 carve-out for OAuth challenges, P2 per-cookie max-age=0 assertion, P3 NFR6 headers scan, P4 assert_all_called default-True on 24 happy-path tests / kept =False on 9 negative-path tests, P5 handler docstring, P6 expired-session call_count==0, P7 retry-401 token-rotation assertion); 8 defers logged D119-D126; 543 BFF tests passing (+2 new empty-body coverage), coverage 97.26%; rsc.py at 100%; story 4.2 → done. On 2026-05-17: story 4.3 code review pass → done (1 patch applied (P1 tightened test 4 with mid-flight pessimistic-UI assertions on the Re-estimate flow); 10 adversarial findings dismissed as noise; SPA suite 152/152 green, coverage 88.23%/78.57% on estimate-cell.ts and 100%/85% on books-service.ts. Story 4.3 dev complete → review: SPA EstimateCell real component + BooksService.requestEstimate landed in worktree; one new file (estimate.types.ts), six modified; 13 net new tests; audited app-error.types.ts / error-service.ts / error-service.spec.ts — all 4.3-relevant variants and tests already in place from Story 3.5, NO edits. Story 4.3 contexted — file `4-3-spa-estimatecell-real-component-apperror-extensions-bookrow-integration.md` written from 4.2 BFF contract + 3.5 AppError/ErrorService precedent + 2.6 BookRow integration + UX-DR8/DR12-19; 20 ACs + 5 tasks. Story 4.2 dev complete → review: BFF POST /v1/books/{id}/estimate + ResourceServerClient.compute_estimate landed; 4 files modified, 21 + 22 new tests added, per-file coverage 100%, project coverage 97.26%. Story 4.2 contexted from 4.1 + 3.5 + 2.2 precedents; 15 ACs + 10 tasks. Story 4.1 code review pass: 6 patches applied (P1-P6), 6 defers logged D113-D118.)
project: BMAD_books
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Foundational Login & Identity (J1, J5)
  epic-1: done
  1-1-repo-scaffold-compose-skeleton: done
  1-2-keycloak-realm-as-code-compose-service: done
  1-3-bff-scaffold-from-archetype-baseline-health-lint-test-gates: done
  1-4-bff-session-and-auth-state-schema-alembic-migration: done
  1-5-bff-cookie-session-oidc-plugin-pkce-synthetic-idp-test-harness: done
  1-6-bff-csrf-middleware-csp-header: done
  1-7-bff-logout-endpoint-revoke-end-session-degrade-honestly: done
  1-8-spa-scaffold-tailwind-v4-design-tokens: done
  1-9-spa-authservice-interceptors-functional-guards: done
  1-10-spa-loginview-topchrome-route-table: done
  1-11-playwright-project-setup-fixtures-helpers: done
  1-12-bff-post-v1-test-reset-endpoint: done
  1-13-e2e-spec-j1-first-time-login-j5-logout: done
  1-14-bff-multi-stage-build-serves-spa-bundle: done
  epic-1-retrospective: done

  # Epic 2: Personal Book List (J2)
  epic-2: in-progress
  2-1-bff-book-sqlmodel-migration-pydantic-boundary-models: done
  2-2-bff-full-books-crud-v1-books-v1-books-id: done
  2-3-bff-extend-v1-test-reset-to-truncate-books: done
  2-4-spa-booksservice-types: done
  2-5-spa-booklist-page-bookform-add-variant-states: done
  2-6-spa-bookrow-statuscontrol-with-optimistic-ui-edit-delete: done
  2-7-e2e-spec-j2-manage-books: done
  epic-2-retrospective: done

  # Epic 3: Reading Speed Settings (J4)
  epic-3: in-progress
  3-1-rs-scaffold-from-archetype-baseline-health-rs-in-compose-default-dev: done
  3-2-rs-oidc-bearer-auth-plugin-jwks-scope-enforcement-synthetic-idp-test-harness: done
  3-3-rs-reading-speed-model-migration-v1-reading-speed-get-put-scope-gated: done
  3-4-rs-post-v1-test-reset-endpoint: done
  3-5-bff-resourceserverclient-refresh-replay-reading-speed-proxy-spa-settingsview-route: done
  3-6-e2e-spec-j4-adjust-reading-speed-compose-e2e-profile-killrs-startrs-resetstate-helpers: done
  epic-3-retrospective: optional

  # Epic 4: Reading-Time Estimate & Honest Failure (J3, J6)
  epic-4: in-progress
  4-1-rs-post-v1-estimate-endpoint-estimate-service-format-duration-helper: done
  4-2-bff-post-v1-books-id-estimate-resourceserverclient-compute-estimate: done
  4-3-spa-estimatecell-real-component-apperror-extensions-bookrow-integration: done
  4-4-e2e-specs-j3-estimate-j6-rs-unavailable: review
  epic-4-retrospective: optional

  # Epic 5: Final Coverage Push & Security Review
  epic-5: done
  5-1-coverage-audit-gap-fill: done
  5-2-security-review-document: done
  5-3-readme-polish-ai-integration-log: done
  5-4-final-docker-compose-up-smoke-default-profile: done
  epic-5-retrospective: done

  # Epic 6: Frontend Split & SSR Edge
  # Added 2026-05-19 via bmad-correct-course; Sprint Change Proposal at
  # _bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md.
  # Splits the SPA out of the BFF image into its own Angular SSR Node
  # container that serves the browser SSR-rendered HTML and reverse-proxies
  # /auth/* /api/* /v1/* to the BFF over compose DNS via http-proxy-middleware.
  # Same-origin model preserved; the SSR Node process IS the edge (no extra
  # Caddy/nginx layer). New host port :4000 (SPA edge); BFF loses host port.
  # Story 6.3 explicitly supersedes Story 1.14's SPA-in-BFF surface.
  epic-6: done
  6-1-spa-angular-ssr-scaffold-proxy-cookie-forwarding: done
  6-2-spa-dockerfile-compose-service-keycloak-realm-port: done
  6-3-bff-cleanup-supersede-story-1-14: done
  6-4-revalidation-e2e-smoke-docs-sweep: done
  epic-6-retrospective: optional

  # Epic 7: ACME-TS Principles Alignment (PKCE removal + audit gaps)
  # Added 2026-05-21 via bmad-correct-course; Sprint Change Proposal at
  # _bmad-output/planning-artifacts/sprint-change-proposal-2026-05-21.md.
  # PKCE removal landed inline in the change proposal as edits to PRD,
  # architecture, epics, Keycloak realm, BFF source + tests, security-review,
  # and Story 1.5 implementation-artifact — see the proposal's Groups A–H.
  # The three stories below close the remaining principles-audit gaps:
  # P4 (separation authN/authZ — claim→role mapping in BFF),
  # P6 (discovery over hardcoding — fetch .well-known at startup),
  # P8 (observability — ACME-schema {timestamp, sub, decision, reason} logs).
  # Story 1.5 retains status `done`; the implementation-artifact carries an
  # amendment header noting PKCE was removed post-hoc.
  epic-7: in-progress
  7-1-bff-claim-to-role-mapping-demo: ready-for-dev
  7-2-oidc-discovery-bootstrap: review
  7-3-structured-auth-decision-logs: done
  epic-7-retrospective: optional
