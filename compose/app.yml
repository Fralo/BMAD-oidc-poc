# compose/app.yml
# Included from the top-level docker-compose.yml via the `include:` directive.
# This file owns application-tier services: BFF (Story 1.3), Resource Server
# (Story 3.1), and the SPA build/serve container (Story 1.8). Empty for the
# scaffold story so `docker compose config` validates cleanly.
services: {}
