# Story 3.1 preempts deferred item D22 (Story 1.3 review): enforce LF line
# endings on shell scripts so a Windows checkout cannot break the
# entrypoint shebang. The BFF has the same item deferred; this RS-side
# inclusion costs nothing and prevents a class of cross-platform bugs.
*.sh text eol=lf
